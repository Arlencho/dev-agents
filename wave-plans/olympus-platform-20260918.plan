# Wave plan for olympus-platform — 2026-09-18T20:48:34Z
# Generated by dispatch.sh

1 | go-backend | Branch fix/2898-question-store-scrub from origin/main. Read issue 2898 in full and the comment on issue 2340 whose first line reads CRITIC IRIS SEARCH WALK FIXES ROUND 2, which is where the same class was fixed for a carrier and a flight number. A free-text hotel area is already kept off the search record and out of the API. It still rides the per-binding open-question store with its value, because the ratified rule in docs/prd/pages/assistant-channel.md constrains the record and the API and says nothing about the store. Hold the store to the same rule: a value a traveller typed, which can name a person or a street, is replaced by the same presence sentinel the carrier and flight-number fields already use, so the store carries the fact that a narrowing existed and never the words. Amend the PRD section 3.3.3 to say the store is held to the record rule, marked PROPOSED since it widens an accepted rule, and cite issue 2898. Tests: a search whose brief parses to a free-text hotel area writes no part of that text to the store, the open question can still be posed and answered, and the sentinel path is exercised for both the area and a board that is not breakfast. VERIFY: go test with the race flag on services/iris/internal/tools and services/iris/internal/current, plus go vet on services/iris, real exit codes in the PR body. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Open a draft PR with Closes 2898, Refs 2800, PRD line ticked. | fix/2898-question-store-scrub
1 | web-frontend | Branch fix/2905-snapshot-after-booking from origin/main. Read issue 2905 in full and the comment on issue 2340 whose first line reads SECURITY LOYALTY TO THE AIRLINE, which found it. The per-device chat snapshot holds the traveller record verbatim, including date of birth, passport number and now the loyalty membership. It is cleared when a trip is copied to another account but not after a booking completes, so on a shared device those details stay readable once the ticket is issued. Clear the identity the moment a confirm succeeds: at the point the booking is known good, either clear the snapshot for that session or strip the identity fields from the stored traveller record, whichever leaves the traveller able to return to their trip without re-reading anything they no longer need. Use the existing helper rather than a second mechanism. Nothing else about the snapshot changes, and a booking that fails leaves it alone so a traveller can retry without retyping. Update docs/prd/pages/07-checkout.md where the snapshot is described, citing issue 2905. Tests: after a successful confirm the stored snapshot carries no date of birth, passport number or membership number for any traveller; a failed confirm leaves the snapshot untouched; returning to the trip after a booking still renders. VERIFY: vitest on the touched test files, web lint and web typecheck, real exit codes in the PR body. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Open a draft PR with Closes 2905, Refs 2424, PRD line ticked. | fix/2905-snapshot-after-booking
2 | security-reviewer | SECURITY REVIEW of both PRs, on branches fix/2898-question-store-scrub and fix/2905-snapshot-after-booking. Read-only; tmp writes only. Record both head SHAs at start and at report. For the store: trace what is written for a search and a refine carrying a free-text area, and confirm no fragment of the traveller words reaches the store, the logs or any response, while the question still works. For the snapshot: enumerate every device-side write of the traveller record, confirm the identity fields are gone after a successful confirm, that a failed confirm keeps them so nobody has to retype, and that nothing else a traveller needs was cleared with them. Say plainly if either fix leaves the data somewhere else. Post ONE comment on issue 2340 whose first line reads SECURITY IDENTITY DATA TWO PLACES with a final SAFE or BLOCK line. HOUSE STYLE: no long dash, no AI tool or vendor name. | fix/2898-question-store-scrub
2 | backend-critic | READ-ONLY REVIEW of the PR on branch fix/2898-question-store-scrub. Tier A, round 1. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. Check the sentinel is the one the carrier and flight-number fields already use rather than a second mechanism, that an answered question still resumes the right search, that nothing else stored alongside it carries traveller words, and mutate the scrub to confirm a test goes red. Post ONE comment on issue 2340 whose first line reads CRITIC QUESTION STORE SCRUB with SAFE-TO-MERGE, BLOCK-FIX or BLOCK-ESCALATE and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | fix/2898-question-store-scrub
2 | frontend-critic | READ-ONLY REVIEW of the PR on branch fix/2905-snapshot-after-booking. Tier A, round 1. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. Check the clear fires exactly once on a successful confirm and never on a failure, that returning to a booked trip still renders everything a traveller expects, that no other device store keeps the same identity, and mutate the clear to confirm a test goes red. Post ONE comment on issue 2340 whose first line reads FRONTEND CRITIC SNAPSHOT AFTER BOOKING with SAFE-TO-MERGE, BLOCK-FIX or BLOCK-ESCALATE and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | fix/2905-snapshot-after-booking
