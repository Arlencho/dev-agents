# Wave plan for olympus-platform — 2026-09-18T13:51:46Z
# Generated by dispatch.sh

1 | web-frontend | Branch fix/walk2-landing from origin/main. Read issues 2883 and 2891 in full and the comment on issue 2800 whose first line reads Owner walk 2026-09-18. ONE (2883): on an assistant-origin landing where the record carries a selected offer present in the snapshot, that card is revealed rather than hidden behind the See more fold, scrolled into view and highlighted, before anything else is actionable; the existing highlight rules in docs/prd/pages/06-conversation-results.md section 6.6.4 already require the scroll, so this is drift, not new behaviour. Tests: a landing whose selected offer sits beyond the initial three reveals and scrolls to it; a landing with no selected offer is unchanged; a website-origin landing is unchanged. TWO (2891): a round-trip card names both legs where it names one, so the header carries the return flight number as well as the outbound, or neither; update the section 6.6.1 element table in the same PR. Tests: a round trip renders both numbers; a one-way renders one; two cards that share an outbound but differ on the return are distinguishable from the header alone. VERIFY: vitest on the touched test files, web lint and web typecheck, real exit codes in the PR body. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Open a draft PR with Closes 2883, Closes 2891, Refs 2800, PRD line ticked. | fix/walk2-landing
1 | web-frontend | Branch fix/walk2-trust from origin/main. Read issues 2890 and 2888 in full, and the comment on issue 2340 whose first line reads CO-FOUNDER SIGN-OFF 2026-09-18 15:35 for the ratified strings. ONE (2890): remove from the traveller details step the five inputs that are transmitted nowhere: loyalty programme and number, dietary choices, accessibility needs, emergency contact name and phone, and the pre-ticked flight-day updates checkbox, together with the state they feed in the step machine; nothing else about the step changes and the fields that do reach the supplier stay exactly as they are. Update docs/prd/pages/07-checkout.md and the travellers step spec to match, citing the sign-off comment. Tests: the step renders only fields that are transmitted; a booking payload is unchanged for the fields that do travel; no removed field remains in the step machine state. TWO (2888): add a secondary button beside the primary on the Confirm and book summary with the ratified label See other flights, which returns to the option list with the current selection intact; 44 px minimum touch target on mobile; PRD section for the summary updated in the same PR citing the same comment. Tests: the button renders on flights and trip summaries, returns to the list, and the selection survives. VERIFY: vitest on the touched test files, web lint and web typecheck, real exit codes in the PR body. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Open a draft PR with Closes 2890, Closes 2888, Refs 2800 and 2424, PRD line ticked. | fix/walk2-trust
2 | frontend-critic | READ-ONLY REVIEW of the PR on branch fix/walk2-landing. Tier B, round 1. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. Check the saved option is revealed and scrolled on a landing where it sits beyond the fold, that nothing else about the list order changes, that both flight numbers render on a round-trip card and the PRD table matches, and mutate each fix to confirm a test goes red. Post ONE comment on issue 2340 whose first line reads FRONTEND CRITIC WALK2 LANDING with SAFE-TO-MERGE, BLOCK-FIX or BLOCK-ESCALATE and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | fix/walk2-landing
2 | frontend-critic | READ-ONLY REVIEW of the PR on branch fix/walk2-trust. Tier B, round 1. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. Check that every removed input is gone from the step, its state and the PRD, that no field which does reach the supplier was removed by accident, that the booking payload is byte-identical for the surviving fields, that the See other flights button carries the ratified label and returns to the list with the selection intact at 400 px and desktop, and mutate each fix to confirm a test goes red. Post ONE comment on issue 2340 whose first line reads FRONTEND CRITIC WALK2 TRUST with SAFE-TO-MERGE, BLOCK-FIX or BLOCK-ESCALATE and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | fix/walk2-trust
