# Wave plan for olympus-platform — 2026-09-13T18:50:06Z
# Generated by dispatch.sh

1 | frontend-critic | READ-ONLY REVIEW ROUND 2 of the web half of PR 2845 on branch feat/ac-handover-identity. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. THE QUESTION THAT MATTERS: is the transition smooth. Walk the three cases as a traveller (owner signed in, owner signed out, another signed-in account) and look for any full reload, interstitial, flash of the interview or of the 404 page, lost selection, or wrong URL after the copy; confirm typecheck and build are green on this head; confirm the S20 line renders once and only after a copy; confirm a web-origin record behaves exactly as before; confirm the round 1 B1 fixture errors are gone. Write failing tests where the PR falls short. Post ONE comment on issue 2340 whose first line reads FRONTEND CRITIC HANDOVER IDENTITY ROUND 2 with SAFE-TO-MERGE, BLOCK-FIX or BLOCK-ESCALATE and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/ac-handover-identity
1 | api-critic | READ-ONLY REVIEW ROUND 2 of the api.yaml diff and generated client on PR 2845. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. Confirm the generated client compiles against every existing usage, the contract matches PRD 06 section 13.3.3 and 13.6 including the corrected note sentence, the clone answers 201, 401, 404 and 409 already_owner as specified, and make check-api-spec exits 0. Post ONE comment on issue 2340 whose first line reads API CRITIC HANDOVER IDENTITY ROUND 2 with SAFE-TO-MERGE, BLOCK-FIX or BLOCK-ESCALATE and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/ac-handover-identity
1 | security-reviewer | SECURITY REVIEW ROUND 2 of PR 2845. Read-only; tmp writes only. Record head SHA at start and at report. Confirm the round 1 MEDIUM is closed (no source free text on the copy), re-check the guest and non-owner answers on GET, PATCH and clone on this head, check the web half stores nothing about the source after a copy, and check the handover cache commit at 6aef472e never exposes a record before access succeeds. Post ONE comment on issue 2340 whose first line reads SECURITY HANDOVER IDENTITY ROUND 2 with a final SAFE or BLOCK line. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/ac-handover-identity
