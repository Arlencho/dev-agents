# Wave plan for olympus-platform — 2026-09-13T19:39:55Z
# Generated by dispatch.sh

1 | api-designer | On branch feat/ac-handover-identity (PR 2845): apply the one-sentence fix in the comment on issue 2340 whose first line reads API CRITIC HANDOVER IDENTITY ROUND 2: the clone operation description in api.yaml carries the corrected note sentence (the assistant note stays with the source and is never copied), matching PRD 06 section 13.6 as amended on the branch; run make generate and commit every generated file; make check-api-spec exit 0. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Commit and push. | feat/ac-handover-identity
2 | web-frontend | On branch feat/ac-handover-identity after wave 1, in apps/web: fix sections 3 and 4 of the comment on issue 2340 whose first line reads FRONTEND CRITIC HANDOVER IDENTITY ROUND 2. Section 3: the S20 line This trip is now saved in your account. renders once on the copy after the URL moves to the new id, never on the source URL; follow the fix the critic describes (one call or one storage key keyed by the new id, plus the harness change) and make the C1 test in that comment green. Section 4: the app shell swaps its wrapper on sign-in and remounts the /r/[id] subtree, so the two mechanics the PR body claims (shell stays mounted, re-fetch in place) do not hold in production; either keep the subtree mounted across the wrapper swap or make the landing survive the remount without any visible reset (no interview flash, no 404 flash, no lost selection, the modal closing into the trip) and state truthfully in the PR body which of the two it is. Keep every existing test green. VERIFY: web unit tests for the touched files, web lint, web typecheck and web build, real commands and exit codes in the PR body under a Round 4 heading. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Commit and push. | feat/ac-handover-identity
3 | frontend-critic | READ-ONLY REVIEW ROUND 3 of the web half of PR 2845. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. Re-run the round 2 C1 test and the three traveller cases in a real browser; confirm S20 renders once on the copy and never on the source; confirm the remount finding is either closed or truthfully described in the PR body with no visible reset in any case. Post ONE comment on issue 2340 whose first line reads FRONTEND CRITIC HANDOVER IDENTITY ROUND 3 with SAFE-TO-MERGE, BLOCK-FIX or BLOCK-ESCALATE and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/ac-handover-identity
