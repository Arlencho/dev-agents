# Wave plan for olympus-platform — 2026-09-13T21:36:54Z
# Generated by dispatch.sh

1 | devops | Branch feat/ac-brief-and-entry from origin/main. Amend docs/prd/pages/assistant-channel.md per the comment on issue 2340 whose first line reads CO-FOUNDER SIGN-OFF 2026-09-13 22:30: section 1 gains one sentence that the host plans in its own voice and Olympus is called only when the traveller asks to book or accepts the host offer to book through Olympus; add section 7.6, When the host calls Olympus, stating the two triggers, that every tool description carries the rule in the manifest, that the search tool is never a substitute for the host asking its own questions, and the manifest wording from that comment quoted verbatim for search and the one-line rule for the other tools. Then per the comment whose first line reads CO-FOUNDER SIGN-OFF 2026-09-13 22:40: section 3.3.1 gains the required-facts table per intent (flights: origin, destination, dates or date flexibility, travellers; trip: those plus nights or stay dates plus budget or stay class band, budget preferred; stays: destination, dates, guests plus budget or class band) and the needs_input outcome (missing field names, no options, no ranking on defaults); section 3.4 gains needs_input with the host action ask the traveller in your own words; section 7.6 gains the accuracy sentence, Olympus needs these facts for an accurate answer, ask for them in your own words before calling, and Olympus never asks the traveller; a new section 10.1, The learning record, states that every tool call leaves a structured event with intent, facts supplied, facts missing, refinement fields, selected option kind and rank, and whether the handover was reached, normalised facts only, no chat or brief text, retained per docs/compliance/RETENTION.md, in the Metis pipeline, purpose to improve the Atlas interview. Date every amendment, name the co-founder, cite both comments. VERIFY: make check-prd-index exit 0. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Commit and push. | feat/ac-brief-and-entry
2 | go-backend | On branch feat/ac-brief-and-entry after wave 1, in services/iris: ONE, issue 2847, read it first: search accepts brief by passing it to the API parse path the web uses for the homepage prompt (POST /api/v1/parse) and merging the parsed fields under the precedence rule (an explicit structured field always wins, the brief only fills absent fields), keeping the section 3.1 scan (a brief carrying a forbidden field is refused with invalid_input and nothing is stored); refine accepts brief by passing it as the prompt of POST /result-sessions/{id}/refine under the same precedence rule, and a brief that is not a narrowing is intent_change with the hint as today; remove the brief_not_supported refusal. TWO, the accuracy minimum: search validates the required facts per intent from PRD section 3.3.1 after merging the brief, and answers needs_input with the missing field names and no options when any is absent; never a default budget, class or date; a trip or stay search with neither budget nor class band is needs_input naming budget. THREE, the manifest: every tool description carries the wording from PRD section 7.6 verbatim (search gets the full paragraph including the accuracy sentence, the six others the one-line rule) and the search schema marks the required facts per intent in its description; annotations unchanged. FOUR, the learning record: every tool call emits one structured api_event through the existing Olympus API event path (the assistant channel event kind), carrying intent, the facts supplied, the facts missing, refinement fields, selected option kind and rank, handover reached, binding id, never the brief text, never a note, never a name; document the event in docs/operations/analytics-pipeline.md under the Metis sinks. Tests: the exact brief from the 2026-09-13 journey (nonstop flights, mid-range central hotels) narrows a trip search and the structured fields it does not name are untouched; a brief that states a date the host also stated keeps the host value; a brief with a date of birth is refused and the record is unchanged; a brief adding a stay to a flights-only search is intent_change; a trip search with no budget and no class band is needs_input naming budget and serves no options; the 2026-09-13 Rome journey inputs (trip, dates, two adults, no budget) now answer needs_input; the manifest carries the section 7.6 text for every tool; one learning event per call with the named fields and none of the forbidden ones. VERIFY: go test ./... with the race flag in services/iris, real exit codes in the PR body; paste the served manifest description of search. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Open a draft PR with Closes 2847 and Refs 2800 and 2340, PRD line ticked. | feat/ac-brief-and-entry
3 | backend-critic | READ-ONLY REVIEW of the PR on branch feat/ac-brief-and-entry. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. THE QUESTION THAT MATTERS: can a brief ever override what the host stated, and can a brief smuggle a forbidden field or a supplier id into the record. Probe: brief and structured field disagree on dates, origin, party; brief carries a date of birth, a passport number, a Duffel offer id; brief over 1000 characters; brief on refine that widens instead of narrows. Then the accuracy minimum: prove no search can rank on a default budget, class or date (feed a trip search without budget or class band; feed flights without dates and without flexibility) and that needs_input names every missing fact at once. Then the manifest: every tool carries the section 7.6 text, no tool description contradicts it. Then the learning record: one event per call, the named fields present, no brief text, note, name or supplier handle in it. Post ONE comment on issue 2340 whose first line reads CRITIC BRIEF AND ENTRY with SAFE-TO-MERGE, BLOCK-FIX or BLOCK-ESCALATE and citations, and a last line TRIAL: producer rounds to SAFE so far <n>. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/ac-brief-and-entry
3 | security-reviewer | SECURITY REVIEW of the PR on branch feat/ac-brief-and-entry. Read-only; tmp writes only. Record head SHA at start and at report. The change lets free text from a third-party host reach our parse and refine paths. Check: the section 3.1 scan runs before anything is stored or sent to a supplier; the parse call is bounded (length, rate, quota per binding); a brief cannot name the record of another traveller; log lines never carry the brief text; the refusal reveals nothing beyond the code; the learning event carries no brief text, note, name, email or supplier handle, and is retained per docs/compliance/RETENTION.md. Post ONE comment on issue 2340 whose first line reads SECURITY BRIEF AND ENTRY with a final SAFE or BLOCK line. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/ac-brief-and-entry
