# Wave plan for olympus-platform — 2026-09-13T13:06:10Z
# Generated by dispatch.sh

1 | devops | Branch feat/ac-handover-landing from origin main. Amend docs/prd/pages/06-conversation-results.md per the ratification on issue 2340 (comment SELF-RATIFICATION 2026-09-13): in section 4 the Direct URL rows gain the rule that when the record carries committed results (non-empty result_snapshot) the page lands on the results picker and never asks an unanswered optional step such as budget, because committed results prove the search ran; in section 6.6.5 the Highlighted state gains a third trigger, the record selected_offer_id when that offer is present in result_snapshot, rendered exactly like the existing triggers (aria-current true, scrolled into view, click proceeds to checkout), and one sentence that an id absent from the snapshot lands on the picker with nothing selected. Date the amendment 2026-09-13, cite issue 2340 and issue 2842. Do not touch section 13.3; sign-in stays at Book. VERIFY: make check-prd-index exit 0. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Commit and push. | feat/ac-handover-landing
2 | web-frontend | On branch feat/ac-handover-landing after wave 1, in apps/web, implement the amended PRD 06 sections 4 and 6.6.5 for the trip page at app/r/[id]. Read issue 2842 first; it names the record, the two root causes and the files. ONE, lib/chat-step-machine/project-snapshot.ts buildRehydratedResultsSnapshot: when the server carries committed results, an unanswered budget must not return null; land on the picker with the budget left empty and no budget recap pill; keep the location gate and the hotel checkout reroute exactly as they are. TWO, thread the record selected_offer_id from useAgentSources in app/r/[id]/atlas-result-session-page.tsx into the rehydrated landing: when the id matches a flight in result_snapshot.flights, the landing snapshot carries that flight as the pick (the same FlightSelection shape the picker writes in components/chat/result-picker-steps.tsx onPickResult, built from the mapped result so the frozen booking facts are present) so the card renders selected with aria-current true, is scrolled into view on first paint, and the right panel summary shows it; when the id matches nothing, land with no selection. Do not add a query parameter; the record is the source. Tests, in the existing test files next to the code: a record with 40 committed flights and no budget lands on the flight step and never renders the budget ask; the same record with selected_offer_id present renders exactly one card with aria-current true and it is the matching offer; selected_offer_id absent from the snapshot renders no aria-current card; a record with no result_snapshot still walks the funnel (the existing 1929 test keeps passing); a budget-bearing record is unchanged. VERIFY: run the web unit tests for the touched files, the web lint and the web typecheck through the Makefile targets, paste the real commands and exit codes in the PR body. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Open a draft PR with Closes 2842 and Refs 2800 and 2340, tick the PRD line in the template. | feat/ac-handover-landing
3 | frontend-critic | READ-ONLY REVIEW of the PR on branch feat/ac-handover-landing. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. THE QUESTION THAT MATTERS: does a traveller who arrives from an assistant with a chosen flight land on that flight, and does nothing else regress. Write failing tests where the PR falls short, in a tmp copy, and cite them. Check: a no-budget record with committed results lands on the picker and never shows the budget ask; the chosen card is the one with aria-current true and it is scrolled into view; a stale selected_offer_id lands with no selection and no crash; a record with no results still walks the funnel; a budget-bearing record is unchanged; a guest clicking the selected card still gets the sign-in modal and never auto-navigates to checkout (conventions 9.3); the frozen booking facts on the seeded FlightSelection are present, not undefined, because checkout fails loud without them; the PRD amendment matches the code and the ratification on issue 2340. Post ONE comment on issue 2340 whose first line reads CRITIC HANDOVER LANDING with SAFE-TO-MERGE or BLOCK-FIX and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/ac-handover-landing
