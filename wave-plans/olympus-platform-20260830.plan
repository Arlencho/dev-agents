# Wave plan for olympus-platform — 2026-08-30T21:51:54Z
# Generated by dispatch.sh

1 | devops | Fix issue 2751, which you must read in full first. CONTEXT: 2747 turned off golangci-lint config verification with verify false in .github/workflows/ci.yml because the action fetched its JSON schema from the golangci-lint.run website on every run, which made a blocking merge gate depend on an external site being up. That removed a flake but it also gave up real coverage, and 2751 tracks getting the coverage back without the network call. WHAT WE LOST, measured during the 2747 review and reproduced, so do not re-derive it: with verification off, an unrecognised key in apps/api/.golangci.yml is silently ignored. A typo inside linters.settings is the dangerous direction: writing min_complexity instead of min-complexity under gocognit is ignored and the threshold silently falls back from greater than 15 to greater than 30, so any function scoring 16 to 30 passes unnoticed. A top level typo is the safe direction, it makes the config stricter and fails loud. YOUR TASK: vendor the schema for the pinned golangci-lint version into the repo and run config verification against the local copy so there is no network fetch. FIRST ESTABLISH WHETHER THIS IS EVEN POSSIBLE before writing anything: check the golangci-lint v2.11.4 CLI to confirm whether config verify accepts a flag pointing at a local schema file, and what that flag is actually called. Do not assume the flag exists and do not invent a name. IF NO SUCH FLAG EXISTS on v2.11.4, STOP, do not improvise a workaround, and report that finding as a comment on issue 2751 explaining exactly what you checked and what the CLI offers instead. A wrong answer here is worse than no answer, because a verification step that silently does nothing is the same blind spot we are trying to close. IF THE FLAG EXISTS: commit the schema for exactly the pinned version, wire verification to the local file, and keep verify false on the action itself so the action never fetches. THE SCHEMA IS VERSION COUPLED, so document at the version pin site that bumping the golangci-lint version requires refreshing the vendored schema, and say where the schema came from. PROVE IT WORKS, do not assert it. In a THROWAWAY WORKTREE, commit a deliberate typo of min-complexity to min_complexity under gocognit in apps/api/.golangci.yml, push that throwaway branch, and show the job FAILING on it. That is the exact defect class this issue exists to catch, so a proof against any other kind of broken config does not count. Then confirm a real lint violation still fails and that a clean tree still passes. Paste every run URL. Commit before you break anything. Never run git stash, which is repo global, and never run git checkout to revert, which zeroes new files unrecoverably. Delete every throwaway branch and prove it with git ls-remote. Confirm no network call to golangci-lint.run remains in the job. Run make check-ci-wiring and paste the real exit code captured directly rather than read after a pipe, and confirm Go Lint is still in both the ci-passed and report-main-red needs lists. Open a PR with Closes 2751. Omit any Co-Authored-By trailer. Do not merge. | ci/2751-vendor-lint-schema
2 | backend-critic | READ-ONLY REVIEW of the wave 1 change on branch ci/2751-vendor-lint-schema. You are not a producer: no commits, no push, no gh pr review, no gh pr merge, no labels, no test writing. Permitted writes are a slash tmp worktree only. Record the PR head SHA at start and re-check at report; if it moved report BLOCKED with both SHAs. IF WAVE 1 REPORTED THAT NO LOCAL SCHEMA FLAG EXISTS and opened no PR, verify that claim yourself against the v2.11.4 CLI and report whether it is correct, then stop. THE QUESTION THAT MATTERS when a PR does exist: does verification actually verify. A step that runs, exits zero and checks nothing is the worst possible outcome here, because it converts a known gap into an invisible one. Enumerate the ways it could be vacuous rather than trusting the diff: a schema path that does not resolve and is silently skipped, a schema file that is empty or permissive or the wrong version, an exit code swallowed by a pipe or a wrapper or a shell that continues on error, and a verification step that runs on a different working directory or a different config file than the one CI lints. SECOND, verify the proof is the RIGHT proof: the deliberate break must be a typo inside linters.settings, specifically the min-complexity case that silently relaxes the gocognit threshold, since that is the defect class 2751 names. A proof using a top level typo does NOT count, because that direction fails loud anyway and would pass even without this change. Fetch the run from the API and confirm Go Lint specifically failed, at the verification step specifically, not somewhere else. THIRD, confirm the clean tree still passes and a real lint violation still fails, so we did not trade a blind spot for a broken gate. FOURTH, confirm no network fetch to golangci-lint.run remains anywhere in the job. FIFTH, confirm the vendored schema matches the pinned version v2.11.4 rather than being whatever was current, and that the coupling between the pin and the schema is documented where someone bumping the version will actually see it. SIXTH, confirm Go Lint is still in both the ci-passed and report-main-red needs lists by parsing both, and that apps/api/.golangci.yml itself is unchanged. Post ONE comment on issue 2340 whose first line reads CRITIC 2751 VENDORED SCHEMA, with a verdict of SAFE-TO-MERGE or BLOCK-FIX or BLOCK-ESCALATE, per-finding severity and file and line citations. No vendor names, emails, booking ids or gcloud payloads. | ci/2751-vendor-lint-schema
3 | devops | Fix issue 2755, which you must read in full first. THE GAP, already verified so do not re-derive it: docs/prd/pages/atlas-chat.md is absent from docs/prd/00-INDEX.md. There are 31 page files on disk and 30 referenced in the index, atlas-chat.md is the only one missing, and no index entry points at a file that does not exist. It matters because that page is 3603 lines and is cited as authoritative by .claude/skills/deployment/SKILL.md line 31 for the Duffel Balance settlement policy, by infra/monitoring/orphan-alerts.sh line 200, and by docs/TRIP_STATE_SSOT.md line 192, while the root CLAUDE.md instructs everyone that the page list is in 00-INDEX.md. So the repo tells people to find specs through an index that omits the spec governing the chat surface and the settlement policy. TWO PARTS, both required. PART ONE: add atlas-chat.md to 00-INDEX.md, placed in the existing ordering and described in the existing style rather than appended at the end as an afterthought. Read the surrounding entries and match them. PART TWO: add a check script under scripts/ that makes this drift impossible to repeat, following the BIDIRECTIONAL pattern that scripts/check-openapi-routes.py already establishes in this repo, which you should read first and imitate rather than inventing a new shape. Both directions are required: every docs/prd/pages/ markdown file must appear in 00-INDEX.md, and every page referenced in 00-INDEX.md must exist on disk. Wire it into make lint. PROVE BOTH DIRECTIONS FAIL, do not assert them. Temporarily create a new page file that is not in the index and show the script exiting non zero, then remove it. Temporarily add an index entry naming a file that does not exist and show the script exiting non zero, then remove it. Paste the real exit codes captured directly rather than read after a pipe, since a pipe returns the exit code of the last command and not the one you are testing. Also show the script exiting zero on the clean tree. DECIDE AND JUSTIFY whether this belongs in ci.yml as a job at all: it is a docs consistency check, so a make lint only treatment may well be the right answer, and you should say which you chose and why in the PR body. IF you do add it to ci.yml it must appear in BOTH the ci-passed and report-main-red needs lists, or go in EXEMPT in scripts/check-ci-job-wiring.py with its reason, per docs/operations/branch-protection.md. Run make check-ci-wiring either way and paste the real exit code. OUT OF SCOPE, do not fold in: docs/qa/prd-6.0-audit.md line 231 reports that ratified copy strings are scattered across three files with no single catalogue. Real problem, different fix, different owner. Open a PR with Closes 2755. Omit any Co-Authored-By trailer. Do not merge. | ci/2755-prd-index-gate
4 | backend-critic | READ-ONLY REVIEW of the wave 3 change on branch ci/2755-prd-index-gate. You are not a producer: no commits, no push, no gh pr review, no gh pr merge, no labels, no test writing. Permitted writes are a slash tmp worktree only. Record the PR head SHA at start and re-check at report; if it moved report BLOCKED with both SHAs. THE QUESTION THAT MATTERS: does the new check actually catch drift, in BOTH directions, or does it merely appear to. Run it yourself against a deliberately drifted tree in a throwaway worktree rather than accepting the producer proof: add an unindexed page and confirm non zero, then add an index entry for a nonexistent file and confirm non zero, then confirm zero on the clean tree. A check that passes on everything is worse than no check, because it looks like a guard. SECOND, probe the parsing rather than the happy path, because that is where this class of script fails: confirm it is not fooled by a page referenced only inside a code block or a comment, by a relative path written differently from the convention the index uses, by case differences, by an anchor or query suffix on the link, or by a file that is not a page such as a README or a template living in the same directory. Say explicitly which of these it handles and which it does not, and whether any gap is acceptable. THIRD, confirm atlas-chat.md is now genuinely in the index, placed in the existing ordering with a description matching the surrounding entries rather than dumped at the end. FOURTH, confirm the script would have caught the original defect: check out the parent commit content of 00-INDEX.md, run the new script against it, and confirm it exits non zero naming atlas-chat.md. That is the single most important check in this review, because a guard that would not have caught the thing that caused it is not a guard. FIFTH, confirm make lint actually invokes it and that a failure propagates rather than being swallowed by a pipe or a shell that continues on error. SIXTH, if it was added to ci.yml confirm it is in both the ci-passed and report-main-red needs lists, or correctly in EXEMPT with a reason, by parsing rather than by trusting the PR body, and confirm make check-ci-wiring exits zero. SEVENTH, confirm the diff did not widen into the copy catalogue problem that issue 2755 explicitly rules out of scope. Post ONE comment on issue 2340 whose first line reads CRITIC 2755 PRD INDEX GATE, with a verdict of SAFE-TO-MERGE or BLOCK-FIX or BLOCK-ESCALATE, per-finding severity and file and line citations. No vendor names, emails, booking ids or gcloud payloads. | ci/2755-prd-index-gate
