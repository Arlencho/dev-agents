# Wave plan for olympus-platform — 2026-08-30T21:12:17Z
# Generated by dispatch.sh

1 | devops | Fix exactly three findings raised against PR 2754 on branch docs/prd-baggage-card-and-route-map. Read the review comment on issue 2340 whose first line reads CRITIC PRD BAGGAGE AND ROUTE MAP before you touch anything, and treat its file and line citations as the specification. DOCS ONLY. Change nothing under apps/, write no code, and do not restructure or reword anything the review did not raise. Three edits, nothing more, because a spec under review that grows new content invalidates the review that just passed it. FINDING ONE, MEDIUM, docs/prd/pages/atlas-chat.md around line 3687. The label rule ends with an absolute claim that a bare airport code alone is never rendered, citing section 24.1. That trailing absolute contradicts docs/prd/01-conventions.md around lines 1431 to 1434, which states the single permitted exception: when no locality is available at all the airport slot renders the bare code and the gap is logged, because an empty slot is worse than a code. Note carefully that the two rules concern DIFFERENT missing halves. The 24.1 exception is about a missing CITY. The route map sentence is about a missing CODE. The first half of the sentence is correct and must stay. Rewrite only the trailing absolute so it is scoped to the missing-code case, and defer the missing-city case to the 24.1 exception explicitly rather than silently. An implementer reading the current line renders an unlabelled dot for an endpoint that has a code but no city, which is precisely the outcome 24.1 exists to prevent. FINDING TWO, MEDIUM, docs/prd/pages/flight-detail-fare.md around line 211. The text asserts that the strip already wraps and that a pair never breaks across lines and never separates from its count, presenting all of it as existing behaviour. Half of that is true and half is not, so verify both halves yourself in the code before rewriting. The container at apps/web/components/widgets/flight-widget.tsx around line 591 does set flexWrap wrap, so the wrapping half is real. But the facts are rendered as ONE joined text node at apps/web/components/widgets/flight-widget.tsx around line 611, where flightCardFactParts is joined and placed inside a single span, so there are no per-fact flex items and the no-breaking-between-icon-and-count guarantee is NOT inherited from anything shipped. Rewrite the passage so the wrapping half is stated as existing and the pair-integrity half is stated as a REQUIREMENT ON THE NEW MARKUP that the implementer must build, not as a property already present. Do not attempt to fix the inline style on that container and do not change any code: that is a separate pre-existing concern for the implementation PR. FINDING THREE, LOW, docs/prd/pages/flight-detail-fare.md around line 209. The parenthetical cites 01-conventions.md section 20 and section 11. Section 20 is correct and stays. Section 11 is wrong: 01-conventions.md around lines 586 to 598 is generic accessibility baselines and says nothing about glyph labelling. The rule actually being invoked is in docs/prd/pages/atlas-chat.md around lines 3588 to 3589, that each condition glyph carries an informative aria-label rather than aria-hidden. Correct the pointer only. The rule is already stated correctly inline so no behaviour changes. AFTER THE EDITS: verify each cited line number in your rewritten text still points where you say it points, by opening the target file and reading the line, because a stale cross reference in a contract document is the same class of defect you are fixing. Keep every block marked PROPOSED. Never write ACCEPTED, never write approved, never write signed off, never stamp a co-founder name. Push to the SAME branch docs/prd-baggage-card-and-route-map so PR 2754 updates in place. Do not open a new PR. Do not merge. Add one comment to PR 2754 listing what you changed for each of the three findings. Omit any Co-Authored-By trailer. | docs/prd-baggage-card-and-route-map
2 | frontend-critic | READ-ONLY VERIFICATION of the three fixes on branch docs/prd-baggage-card-and-route-map, PR 2754. You reviewed this branch already and passed it with three findings; this wave checks only that those three were fixed and that nothing else moved. You are not a producer: no commits, no push, no gh pr review, no gh pr merge, no labels, no edits. Permitted writes are a slash tmp worktree only. Record the PR head SHA at start and re-check at report time, and if it moved during your review report BLOCKED with both SHAs. FIRST, diff the current head against the SHA you passed previously and confirm the ONLY changes are the three findings. Any other edit, however sensible, is scope creep on a document that was already reviewed, and you should report it. SECOND, verify finding one: the atlas-chat.md label rule no longer contradicts 01-conventions.md section 24.1, the missing-code case and the missing-city case are now distinguished, and an implementer can no longer conclude that an endpoint with a code but no city renders unlabelled. Read 01-conventions.md around lines 1431 to 1434 yourself rather than trusting the fix comment. THIRD, verify finding two: the wrapping behaviour is stated as existing and the pair-integrity behaviour is stated as a requirement on new markup, and the passage can no longer be read as already handled. Confirm against apps/web/components/widgets/flight-widget.tsx that the joined-text-node fact still holds, so the spec is describing reality. FOURTH, verify finding three: the pointer now resolves to the atlas-chat.md glyph aria-label rule and no longer to the generic accessibility section. FIFTH, spot check every cross reference the producer touched by opening the target file and reading the cited line, since a corrected citation that is itself wrong is worse than the original. SIXTH, confirm approval hygiene still holds: everything PROPOSED, nothing stamped ACCEPTED or approved or signed off, no co-founder named as approver. SEVENTH, confirm the diff is still docs only with nothing under apps/. Post ONE comment on issue 2340 whose first line reads CRITIC PRD FIXES VERIFIED, with a verdict of SAFE-TO-MERGE or BLOCK-FIX or BLOCK-ESCALATE and per-finding file and line citations. No vendor names, emails, booking ids or gcloud payloads. | docs/prd-baggage-card-and-route-map
