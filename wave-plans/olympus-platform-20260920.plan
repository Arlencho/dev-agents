# Wave plan for olympus-platform — 2026-09-20T21:40:45Z
# Generated by dispatch.sh

1 | backend-critic | READ-ONLY REVIEW ROUND 1 of pull request 2472 on branch fix/2182-api-validation, which was parked since August and has just been rebased onto current main. Tier B, round 1. No commits, no push, no review, no labels; temporary writes only. Record the head SHA at start and at report. The change makes POST /price-alerts reject an alert whose stored search_input cannot be re-run by the worker: a blank search_input.flow is derived from the parent flow, a conflicting one is rejected, and the shared validateSearchInput then demands the sub-object that flow needs. Judge these questions against the code and real runs, not the pull request text. First, is the derive-then-validate order actually safe, given that the request is normalised through a pointer and the normalised value is what gets persisted; look for a path where a client value is overwritten silently. Second, does the parent flow ever disagree with what the worker reads at apps/api/internal/worker/price_alert.go, and does a stored alert from before this change still behave the same way. Third, does any writer in the tree create a price alert with a partial payload, including the web form, the assistant channel and any test helper that would now start failing for real users rather than in tests. Fourth, does the shared validateSearchInput change alter the search handler behaviour in any way, since both call it. Fifth, run go build and the handler and worker packages yourself and say whether the quoted results hold. Post ONE comment on issue 2340 whose first line reads BACKEND CRITIC PRICE ALERT VALIDATION ROUND 1 followed by SAFE-TO-MERGE, BLOCK-FIX or BLOCK-ESCALATE, with citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | fix/2182-api-validation
