# Wave plan for veto — 2026-09-20T21:47:31Z
# Generated by dispatch.sh

1 | mobile | This is the final round before merge. Continue on the EXISTING branch feat/mobile-owner-screens. Read the round 2 critic comment on PR 38 and close the two reopened findings, nothing else. FINDING 7, the serious one: attachSignatures in app/lib/ring.ts around lines 110 to 123 pairs a decision to a signature by kind, nonce and amount, with no timestamp, and it matches a newest-first signature list against an oldest-first ring. Two refusals of the same charge swap signatures, and that is not a corner case: it is the retry-after-override path the program is specifically designed to support, so it will happen in the demo. The same 50-signature window over a 32-entry ring also lets the signature of an evicted decision attach to a later one after the ring wraps. Match on blockTime as well, which the critic notes closes both. The review includes a red test that fails today with the older refusal opening the wrong signature; make it green without weakening it. FINDING 3: cold start is fixed, but when the chain read FAILS, every screen prints the error and then says there is no mandate on chain for this owner yet and invites the user to open one on the Mandate tab. That is an unchecked absence with a call to action attached, and acting on it would create a duplicate mandate. Distinguish not read yet, read and empty, and read and failed, and only the middle one may invite creating a mandate. The critic suggests a checkedOwner field set in a finally block, which also removes a one frame gap between wallet restore and the refresh effect. Re-run npx tsc --noEmit, npm test and npx expo config yourself and paste the results into the PR. Forbidden: touching programs/veto/src, weakening a test, reordering the polyfill imports, styling a refusal as an error, hardcoding an RPC url or program id. Acceptance: both findings closed, the critic red test green, typecheck clean, tests green, PR 38 updated. No em dash anywhere. | feat/mobile-owner-screens
2 | frontend-critic | This is the final round before merge. Round 3 review of feat/mobile-owner-screens. Only findings 7 and 3 are in scope; the other eight were verified closed in round 2 and the decoding was verified in round 1. State whether anything outside that scope changed. Confirm that your own red test for the signature pairing is green rather than deleted, that two refusals of the same charge now open their own transactions, that a decision evicted by the ring cannot lend its signature to a later one, and that a failed read is now visually and textually distinct from an empty one with no invitation to create a duplicate mandate. Run npx tsc --noEmit, npm test and npx expo config yourself. Your first line must carry the word CRITIC and exactly one verdict word per the fleet rule in CLAUDE.md. No em dash anywhere. | feat/mobile-owner-screens
