# Wave plan for olympus-platform — 2026-08-22T21:18:58Z
# Generated by dispatch.sh

1 | go-backend | Two fixes on branch fix/settlement-guard-live-mode, updating PR 2730 in place. ITEM ONE, the critical leak. apps/api/internal/service/standalone_payment.go lines 292 to 305 return Balance settlement BEFORE the new guard at line 321 is reached, so the guard is never consulted on that arm. The path uses only client-controlled request fields and no flag that is off by default: POST /api/v1/bookings with flow trip and settlement balance mints no PaymentIntent because FEATURE_TRIP_BALANCE_SETTLEMENT defaults true at config.go line 1000, then POST /api/v1/bookings/{id}/confirm-duffel reaches the standalone confirm with tripLeg false, and inside authorizeStandalonePayment the branch for a trip flow with an empty payment id returns nil nil at line 305 without ever consulting liveMode. The provider then issues a real order against the live flights token with nobody charged. The critic reproduced this and the service log itself calls the row misrouted, with alert trip_booking_misrouted_to_standalone_confirm. Extend the liveMode refusal to that misrouted-trip arm at line 301 as well as the standalone arm at line 308. Be careful about what you must NOT break: a genuine trip cart never reaches authorizeStandalonePayment at all, because ConfirmTrip goes through ConfirmDuffelFlightLeg in trip_booking.go around line 570 and ConfirmDuffelStayWithAcceptance around line 851, neither of which calls it. So refusing on the misrouted arm does not touch the real trip flow. Verify that yourself before you change anything rather than taking it on trust. ITEM TWO, a test that currently locks the leak open. apps/api/internal/service/standalone_payment_test.go lines 1346 to 1379, named TestStandalonePayment_TripBalanceRow_UnaffectedByLiveModeGuard, calls ConfirmDuffelWithPayment, which is the standalone confirm, and asserts with require NoError and require NotNil on the Duffel order id that under live mode the standalone endpoint MUST still book a trip row from the Balance. That is the defect asserted as an invariant, and it will go red on the correct fix. Re-point it at ConfirmDuffelFlightLeg or ConfirmTrip, which is the claim actually worth protecting, and make its name and docstring say what it really tests. PROVE THE FIX: write a test that reproduces the leak exactly as the critic did, Create with flow trip and settlement balance then the standalone confirm under live mode, and show it FAILS on the current head and passes after. Paste both exit codes captured directly rather than read after a pipe. Also keep the existing guarantees green: sandbox unaffected, a real trip cart unaffected in both modes, no status code changed outside the refusal, isRecoverableUserError untouched, and nothing touching whether a Stripe authorisation is voided or captured. Run go build, go vet, the Go suite with race, golangci-lint and make check-api-spec with real exit codes. Push to the existing branch, do not open a new PR, do not merge. | fix/settlement-guard-live-mode
2 | backend-critic | READ-ONLY REVIEW of the wave 1 change on branch fix/settlement-guard-live-mode. You are not a producer: no commits, no push, no gh pr review, no gh pr merge, no labels, no test writing. Permitted writes are a /tmp worktree only. Record the PR head SHA at start and re-check at report; if it moved, report BLOCKED with both SHAs. ONE QUESTION, answered by reproduction rather than by reading: with a live flights token, can any sequence of client-controlled requests still cause a real Duffel order with no PaymentIntent. Re-run the exact leak you found before, Create with flow trip and settlement balance then the standalone confirm under live mode, and confirm it is now refused. Then look for siblings of the same shape rather than only the one arm that was patched: any other early return in authorizeStandalonePayment or its callers that precedes the guard, any other confirm endpoint reachable with a Balance-settled row, and the Hermes path if it can reach booking creation. SECOND: confirm the rewritten test now exercises ConfirmDuffelFlightLeg or ConfirmTrip rather than the standalone confirm, that its name and docstring match what it asserts, and that it would fail if the guard were removed. THIRD: confirm sandbox is genuinely unaffected and a real trip cart is unaffected in both modes, by test and by reading the condition. FOURTH: confirm no status code changed outside the refusal, isRecoverableUserError is untouched, and nothing in the diff decides whether an authorisation is voided or captured. Do not re-litigate the parts of the guard you already verified as correct. Post ONE comment on issue 2340 whose first line reads CRITIC SETTLEMENT GUARD 2, with a verdict of SAFE-TO-MERGE or BLOCK-FIX or BLOCK-ESCALATE, per-finding severity and file and line citations. No vendor names, emails, booking ids or gcloud payloads. | fix/settlement-guard-live-mode
