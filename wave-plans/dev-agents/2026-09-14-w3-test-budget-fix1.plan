# Fleet optimization W3: right-sized verification, round 2 of PR 94. Tier C, final round under the two-round cap. 2026-09-14
#
# DISPATCH: ./scripts/dispatch.sh git@github.com:Arlencho/dev-agents.git wave-plans/dev-agents/2026-09-14-w3-test-budget-fix1.plan --detach --retries 1
# FIX-ROUND: 2026-09-14-fix-round-test-budget.plan
# ────────────────────────────────────────────────────────────────────────────

1 | web-frontend | On branch feat/fix-round-test-budget (PR 94), rebase onto origin/main first, then fix the four findings of the comment on PR 94 whose first line reads CRITIC FIX ROUND TEST BUDGET, following its fix line: add case-insensitive matching to the make test matcher; match the full-suite phrases against the joined task text (newlines and backslash continuations collapsed) as well as per line; add fixtures for the case variants and the split VERIFY shapes; qualify the older before-committing test lines in roles/go-backend.md and every other charter that says the whole suite must pass, so they apply only outside fix rounds. This is a fix round: run only tests/run-plan-check-tests.sh and the routing test file; no full suite, no mutation run. VERIFY: those two test files with real exit codes in the PR body under a Round 2 heading. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Commit and push. | feat/fix-round-test-budget

2 | plan-critic | READ-ONLY REVIEW ROUND 2 of PR 94. Tier C, final round: after this the orchestrator merges or stops. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. Re-run only the fixtures of the four round 1 findings plus one regression check that a round 1 plan with make test still passes. Report only a finding that lets a fix-round plan run the full suite unnoticed. Post ONE comment on PR 94 whose first line reads CRITIC FIX ROUND TEST BUDGET ROUND 2 with SAFE-TO-MERGE, BLOCK-FIX or BLOCK-ESCALATE and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/fix-round-test-budget
