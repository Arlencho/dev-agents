# Routing: every development producer runs on the first-party Opus 5 model; critics, security and plan critic unchanged; Floor work stays on the cross-vendor seats. Owner decision 2026-09-14 08:25. 2026-09-14
#
# DISPATCH: ./scripts/dispatch.sh git@github.com:Arlencho/dev-agents.git wave-plans/dev-agents/2026-09-14-routing-opus-producers.plan --detach --retries 1
# Why: the owner asked that development moves to Opus. It replaces the cross-vendor producer trial of 2026-09-13 (PR 88) for product work.
# ────────────────────────────────────────────────────────────────────────────

1 | web-frontend | Branch feat/routing-opus-producers from origin/main. Config, role headers and docs only, no launcher code. ONE, config/workers.yaml provider_preferences: go-backend, web-frontend, db-architect, api-designer, devops, test-engineer, mobile, investigate and docs-writer become claude; every critic, security-reviewer, cto, orchestrator, retro and default unchanged; plan-critic and devops-critic stay grok. TWO, config/routing.yaml model_routing: every producer role listed above pins claude-opus-5; critics, security-reviewer, orchestrator, cto, retro, pr-sentinel and default keep claude-fable-5-1; remove the ignored-while-provider comments that no longer apply. THREE, config/routing.yaml provider_failover: every producer role above fails over to kimi then grok; critic, plan-critic and devops-critic entries unchanged. FOUR, roles: the model line of every producer role file reads model: claude-opus-5; critic and security role files unchanged. FIVE, a documented exception that stays in force: Floor work in this repo (scripts/desk_live.py, scripts/floor_tty.py, scripts/notify.sh, templates/experience, scripts/experience_build.py) is planned on the kimi and grok seats by plan authoring, per the owner rule of 2026-09-13; state it in README and in the routing.yaml header. SIX, update tests/run-routing-tests.sh to the new rules: every producer primary is claude with model claude-opus-5; every producer failover is kimi then grok; every critic primary is claude with claude-fable-5-1; security-reviewer has no failover; role file model lines agree with the tables; remove the assertions from PR 88 that producers are never claude. SEVEN, README seats section and providers/grok/README.md producer section state the change and that the producer trial of 2026-09-13 ended on 2026-09-14 by owner decision. VERIFY: tests/run-routing-tests.sh and make test pass with real exit codes in the PR body; resolve the provider and model for every role with the dispatcher resolver and paste the table. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name in commit messages or PR text (role and seat names only; vendor and model values inside config files are values), no Co-Authored-By trailer. Open a draft PR titled Routing: development producers on the first-party top tier, critics unchanged. | feat/routing-opus-producers

2 | plan-critic | READ-ONLY REVIEW of the PR on branch feat/routing-opus-producers. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. Resolve every role through resolve_provider in scripts/dispatch.sh and the model lookup: every producer resolves to claude with claude-opus-5, falls over to kimi then grok when cooling; every critic still resolves to claude-fable-5-1; security-reviewer has no failover; plan-critic and devops-critic unchanged; no role file disagrees with the tables; the tests fail when any rule is broken in a tmp copy. Post ONE comment on the PR whose first line reads CRITIC ROUTING OPUS PRODUCERS with SAFE-TO-MERGE, BLOCK-FIX or BLOCK-ESCALATE and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/routing-opus-producers
