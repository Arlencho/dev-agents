# Floor terminal fix round: five findings from CRITIC FLOOR TERMINAL BLOCK-FIX on PR 83. 2026-09-13
#
# DISPATCH: ./scripts/dispatch.sh git@github.com:Arlencho/dev-agents.git wave-plans/dev-agents/2026-09-13-floor-terminal-fix1.plan --detach --retries 1
# ────────────────────────────────────────────────────────────────────────────

1 | devops | On branch feat/floor-terminal (PR 83), fix all five findings of the comment on PR 83 whose first line reads CRITIC FLOOR TERMINAL BLOCK-FIX, following its fix lines: ONE, the refresh loop rejects anything that is not a dict with schema live/1, paints the same Could not read line and never leaves the last counts on screen; TWO, render INITIATIVES in the section 4 order with the empty copy of the page when empty; THREE, missing and empty keys tell one story that matches the page, never a zero on the status line for a key the section calls absent; FOUR, the same schema gate as the page, then the honest empty copy; FIVE, print checkout-relative file and line only, drop the citation when the path is absolute. Add a test per finding with the critic fixtures and keep every existing test green. VERIFY: the test suite with real exit codes in the PR body under a Round 2 heading and a pasted --once render of the live file. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Commit and push. | feat/floor-terminal

2 | plan-critic | READ-ONLY REVIEW ROUND 2 of PR 83 on branch feat/floor-terminal. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. Re-run the five round 1 fixtures through both renderers and compare the five answers again; feed a live.json that is a list, a string and an object with the wrong schema. Post ONE comment on PR 83 whose first line reads CRITIC FLOOR TERMINAL ROUND 2 with SAFE-TO-MERGE, BLOCK-FIX or BLOCK-ESCALATE and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/floor-terminal
