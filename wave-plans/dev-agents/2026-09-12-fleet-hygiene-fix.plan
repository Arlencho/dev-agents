# Fix wave for fix/fleet-hygiene (PR 67) after plan-critic BLOCK-FIX. 2026-09-12
#
# DISPATCH: ./scripts/dispatch.sh git@github.com:Arlencho/dev-agents.git wave-plans/dev-agents/2026-09-12-fleet-hygiene-fix.plan --auto --retries 1
# ────────────────────────────────────────────────────────────────────────────

1 | devops | Fix branch fix/fleet-hygiene (PR 67) per the critic comment on the PR whose first line reads CRITIC FLEET HYGIENE BLOCK-FIX. READ IT IN FULL FIRST. Five items, one commit each. ONE, Ctrl-C must release the lock while a wave is blocked in wait or sleep: make the wave wait interruptible (poll the child pids with kill -0 in a loop that sleeps in short slices, or run the INT trap immediately), and add a test to tests/run-dispatch-lock-tests.sh that sends process-group SIGINT during a wave wait and asserts the lock file is removed; the suite must fence the trap code it needs, so widen the dispatch-lock begin and end markers in scripts/dispatch.sh to include the trap re-arm at the lines the critic cites. TWO, preserve exit codes: in the EXIT trap capture rc first and exit with it, so an interrupted run still exits 130 and a terminated run 143; add the assertion to the suite. THREE, the lock path must be machine-global, not per fleet clone: put it under a fixed per-user directory (the same base the launchd jobs and agent logs use under the home directory), keyed by repo name, and update README to say so; keep issue 66 as the within-wave worktree follow-up. FOUR, remove handoff.md from the branch; it is working notes and must never be committed (add it to .gitignore in this repo if it is not already, the critic lists it among the changed files). FIVE, re-run tests/run-dispatch-lock-tests.sh and tests/run-launcher-tests.sh and bash -n on every touched script, and paste the real exit codes in the PR body. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name in code, comments, commit or PR text, no Co-Authored-By trailer. Push to the same branch. Do not merge. | fix/fleet-hygiene

2 | plan-critic | READ-ONLY RE-REVIEW of branch fix/fleet-hygiene (PR 67) after the fix wave. No commits, no push, no edits, no labels; tmp writes only. Record head SHA at start and at report. Your prior comment starts CRITIC FLEET HYGIENE BLOCK-FIX and listed five numbered findings; grade each CLEARED or STILL OPEN with proof: send process-group SIGINT during a wave wait in a tmp clone with the stubbed run-remote and show the lock file gone and exit 130; SIGTERM shows 143; two fleet clones on one host dispatching the same repo now contend on one lock; handoff.md absent from the tree and ignored; both test suites green with exit codes pasted. Then re-confirm the FIX B launcher proofs still hold at the new head. Post ONE comment on the PR whose first line reads CRITIC FLEET HYGIENE ROUND 2 with SAFE-TO-MERGE or BLOCK-FIX and line citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | fix/fleet-hygiene
