# Routing: Grok on trial as the producer for Go, Python, API contract, migrations, devops and docs; Kimi stays on web; Claude holds every critic and the security seat; producers never fail over to Claude. Owner decision 2026-09-13 21:20. 2026-09-13
#
# DISPATCH: ./scripts/dispatch.sh git@github.com:Arlencho/dev-agents.git wave-plans/dev-agents/2026-09-13-routing-grok-producers.plan --detach --retries 1
# Why: first-party seats hit the subscription spend limit on 2026-09-13 and killed every seat for three hours; producers are the larger share of that spend and the critics are where the quality lives.
# ────────────────────────────────────────────────────────────────────────────

1 | web-frontend | Branch feat/routing-grok-producers from origin/main. Config and docs only, no launcher code changes. ONE, config/workers.yaml provider_preferences: go-backend, db-architect, api-designer, devops, test-engineer, docs-writer, mobile and investigate become grok; web-frontend stays kimi; every critic (backend-critic, frontend-critic, database-critic, api-critic), security-reviewer, cto, orchestrator, retro and default stay claude; plan-critic and devops-critic stay grok. TWO, config/routing.yaml provider_failover: every producer role lists only kimi and grok in some order and never claude; backend-critic, frontend-critic, database-critic and api-critic list claude then grok; security-reviewer, cto and orchestrator have no failover entry; keep the existing plan-critic and devops-critic entries and their comments. THREE, roles: the model line of every producer role now on grok reads model: grok (the CLI default, always the latest, as the plan-critic charter already does); critic and security role headers stay claude-fable-5-1; the routing table comments in routing.yaml model_routing are updated so no comment still says opus or sonnet. FOUR, README seats section and providers/grok/README.md producer section state the new assignment, the trial (five tasks, rounds to SAFE counted against the Kimi and Claude baselines in wave-plans/ab-metrics.csv) and the exit rule (a producer whose median rounds to SAFE exceed the baseline by one goes back). FIVE, tests in tests/run-routing-tests.sh: no producer primary is claude; no producer failover chain contains claude; every critic primary is claude; security-reviewer has no failover; every role file model line agrees with the tables. VERIFY: tests/run-routing-tests.sh and any provider sync check pass with real exit codes in the PR body; run scripts/dispatch.sh with --dry-run or its equivalent on one existing plan and paste the resolved provider per seat. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name in commits or PR text (role and seat names only; vendor names inside config files are values, not prose), no Co-Authored-By trailer. Open a draft PR titled Routing: trial producers on the cross-vendor seats, critics stay first-party. | feat/routing-grok-producers

2 | plan-critic | READ-ONLY REVIEW of the PR on branch feat/routing-grok-producers. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. THE QUESTION THAT MATTERS: can any producer seat still reach a first-party launcher, by primary, by failover, by the default entry, by a role model line, or by an absent entry falling through to default. Trace resolve_provider in scripts/dispatch.sh for every producer role with the new tables, including a role with no failover entry and a role whose primary is cooling. Confirm the critic and security seats still resolve to first-party. Confirm the tests fail if any of the five rules is broken (break each in a tmp copy). Post ONE comment on the PR whose first line reads CRITIC ROUTING GROK PRODUCERS with SAFE-TO-MERGE, BLOCK-FIX or BLOCK-ESCALATE and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/routing-grok-producers
