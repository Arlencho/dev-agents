# Floor v3-C fix round 5, re-issued on the web-frontend seat (owner rule 2026-09-13: Floor work runs on the web-frontend and plan-critic seats only). The toast carries fixed phrases and identifiers, never free text. PR 79. 2026-09-13
#
# DISPATCH: ./scripts/dispatch.sh git@github.com:Arlencho/dev-agents.git wave-plans/dev-agents/2026-09-13-floor-v3c-fix5.plan --detach --retries 1
# Why: rounds 1 to 4 each found one more encoding of a path inside a PR title or comment. Scrubbing free text is a losing game; the toast does not need it.
# ────────────────────────────────────────────────────────────────────────────

1 | web-frontend | On branch feat/floor-v3c (PR 79, base main), Python in scripts/desk_live.py and scripts/notify.sh: first rebase the branch onto origin/main (the page merged as one squash; take main for any page file, keep the yesterday toggle and notification files). Then the macOS notification text is built from fixed phrases and identifiers only, never from a PR title, a comment body, a task line or any other free text: shape <repo> <item type phrase> PR <number>, for a critic block: <repo> blocked by <critic stem word> round <n> PR <number>; examples: olympus-platform ready to merge PR 102; dev-agents blocked by frontend critic round 2 PR 80. The page rows keep their scrubbed text as today, only the toast changes. Remove the toast-side scrubbing that only existed for free text; keep task_path and has_operator_path for the page. Replace the round 1 to 4 path fixtures with one test that feeds every earlier shape (plain, colon-glued, parentheses, percent-encoded, double and triple encoded, glued to a word) through the toast builder and asserts the output equals the fixed phrase exactly. Document the rule in docs/experience-data.md. VERIFY: the desk test suite with real exit codes in the PR body under a Round 5 heading. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Commit and force-push. | feat/floor-v3c

2 | plan-critic | READ-ONLY REVIEW ROUND 5 of PR 79 on branch feat/floor-v3c. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. Feed every path shape from rounds 1 to 4 plus three of your own through the toast builder; the only acceptable output is the fixed phrase. Confirm the double notification fix and the yesterday payload honesty marks still hold, and that no page file differs from main after the rebase. Post ONE comment on PR 79 whose first line reads CRITIC FLOOR V3C ROUND 5 with SAFE-TO-MERGE, BLOCK-FIX or BLOCK-ESCALATE and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/floor-v3c
