# Fix wave for feat/floor-plain (PR 70) after frontend-critic BLOCK-FIX. 2026-09-12
#
# DISPATCH: ./scripts/dispatch.sh git@github.com:Arlencho/dev-agents.git wave-plans/dev-agents/2026-09-12-floor-plain-fix.plan --auto --retries 1
#
# Round one on PR 70: CRITIC FLOOR PLAIN DATA SAFE-TO-MERGE with four residuals;
# FRONTEND CRITIC FLOOR PLAIN BLOCK-FIX with four blocks. The CI check named
# Fleet unit tests is FAILURE on the PR head.
# ────────────────────────────────────────────────────────────────────────────

1 | devops | Data-side fixes on branch feat/floor-plain (PR 70), scope scripts/desk_live.py, the seat-progress reader, tests/, docs/experience-data.md. READ FIRST both round-one comments on the PR in full. ONE, the failing CI check Fleet unit tests: open the run log for the PR head, find the failing test, fix the cause, and paste the green run. TWO, frontend block 3 needs data: today[] must carry an outcome the page can name: landed (every seat succeeded), failed (at least one seat failed and the run ended by itself), aborted (stopped by the operator); derive it from the seat exits and the dispatch_end status, document it in the schema, and keep status for compatibility. THREE, frontend block 1 needs data: the summary must carry the last event timestamp, not a precomputed age, so the page can compute the age live and agree with the rest of the chrome; document it. FOUR, data residual 1: the program token must pass the same secret scrub the Almanac uses before it is written (a basename that looks like a token or key is replaced by the literal redacted); add the test case the critic used. FIVE, residuals 3 and 4: fix the plan_purpose doc line and document the eight-assignment cap, or raise the cap and say so. VERIFY: python3 -m py_compile on touched files, the tests under tests/ green locally with the exit code pasted, desk_live.py --once against real streams of today. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Push to the same branch. | feat/floor-plain

2 | web-frontend | Page-side fixes on branch feat/floor-plain (PR 70) after wave 1, scope templates/experience/floor.js, templates/experience/site.css and the Floor renderer in scripts/experience_build.py; site/ is build output, rebuild with make experience. READ FIRST the comment on the PR whose first line reads FRONTEND CRITIC FLOOR PLAIN, all four blocks and the non-blocking notes, then the wave 1 fields. BLOCK 1: compute the top-line last-event age in the browser from the timestamp wave 1 now provides, through the same liveState the rest of the chrome uses, so the two lines can never disagree; the build-time renderer prints the same value the page would show at build time. BLOCK 2: the top line and every seat sentence take the page state and degrade with it: under stale or offline the top line says so in place, the heartbeat clause becomes last heartbeat at the time of the last event, the phase clause is past tense or dropped, and the elapsed clause freezes; prove it with the stale and offline fixtures. BLOCK 3: render the wave 1 outcome word (landed, failed, aborted) with failed visibly distinct. BLOCK 4: restructure the seat sentence so a first-time reader can see where the purpose ends and the status begins: purpose on its own line or in its own element, then a short status clause with nouns (for example: devops, testing with make, wave 2 of 3, running 15 min, heartbeat 48 s ago), never editing (node) style parentheticals; try it against the four real purposes in logs/fleet-queue.json, which all contain colons and commas. Also take the non-blocking notes: the outcome word is a fixed column at 400 wide, one clock per fact (drop the duplicate duration format), and reduce the mirrored JS and Python function pairs where you can without changing the contract, otherwise keep both sides identical and say so in the PR body. VERIFY: make experience, serve the site, load the Floor at 1280 and 400 wide with the live, stale and offline fixtures plus real streams of today, no horizontal scroll, and paste what the top line and one seat sentence read in each state; real exit codes. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Push to the same branch, keep the PR a draft. | feat/floor-plain

3 | frontend-critic | READ-ONLY RE-REVIEW of the Floor page on branch feat/floor-plain (PR 70) after the fix waves. No commits, no push, no edits; tmp writes and the gitignored site build only. Record head SHA at start and at report. Grade your four blocks CLEARED or STILL OPEN with proof: watch the top line and the chrome age for 30 seconds of ticks and confirm they agree; age the projection to stale and offline and paste the top line and a seat sentence in each; render a run with one failed seat and confirm it reads failed and is visibly distinct; feed the four real purposes and paste the resulting sentences. Then read the whole page cold again at both widths and re-run everything that passed in round one. Post ONE comment on the PR whose first line reads FRONTEND CRITIC FLOOR PLAIN ROUND 2 with SAFE-TO-MERGE or BLOCK-FIX and line citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/floor-plain

4 | plan-critic | READ-ONLY RE-CHECK of the data half on branch feat/floor-plain (PR 70) after wave 1. No commits, no push, no edits; tmp writes only. Record head SHA at start and at report. Confirm: the program token is scrubbed for token-shaped basenames (feed the critic case and paste the output); the outcome derivation matches the seat exits for a run with one failed seat, an all-success run and an operator-stopped run; the summary carries a timestamp and the schema says so; the CI check Fleet unit tests is green on the head. Post ONE comment on the PR whose first line reads CRITIC FLOOR PLAIN DATA ROUND 2 with SAFE-TO-MERGE or BLOCK-FIX. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/floor-plain
