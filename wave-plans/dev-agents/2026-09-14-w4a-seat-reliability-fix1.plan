# Fleet optimization W4a round 2: a seat with a tool still running is never killed as hung. Escalation answered by the orchestrator on PR 102 at 18:08. Tier C, final round. 2026-09-14
#
# DISPATCH: ./scripts/dispatch.sh git@github.com:Arlencho/dev-agents.git wave-plans/dev-agents/2026-09-14-w4a-seat-reliability-fix1.plan --detach --retries 1
# FIX-ROUND: 2026-09-14-w4a-seat-reliability.plan
# TIER: C
# ────────────────────────────────────────────────────────────────────────────

1 | web-frontend | On branch feat/w4a-seat-reliability (PR 102), rebase onto origin/main first. Fix finding 1 of the comment on PR 102 whose first line reads CRITIC W4A SEAT RELIABILITY ROUND 1, per the decision posted on the PR: in scripts/seat-watchdog.py an open tool_use (a tool call whose tool_result has not arrived) keeps the seat in flight; while any tool is in flight the quiet timer does not fire on the normal quiet period and instead uses a separate, longer tool ceiling, default 90 minutes, configurable, after which the seat is stopped as hung with the tool name in the stop row; a tool_result closes the call and the normal quiet period applies again; heartbeats, wait tickers, rate limit events and hook ticks still never count as progress. Add the critic fixture as a test (tool_use, silence longer than the quiet period, then a tool_result: exit 0) and one for the ceiling (tool_use, silence longer than the ceiling: exit 124 with the tool name). A limit line that does not match the table stays a normal failure; do not change that. This is a fix round: run only the watchdog test file and tests/run-worktree-tests.sh; nothing else is run. VERIFY: those files with real exit codes in the PR body under a Round 2 heading. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Commit and push. | feat/w4a-seat-reliability

2 | plan-critic | READ-ONLY REVIEW ROUND 2 of PR 102. Tier C, final round. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. Re-run finding 1 and the ceiling fixture, plus one regression check each that a heartbeat-only seat still exits 124 and a limit exit is still held. Report only a finding that kills a working seat or leaves a dead seat running. Post ONE comment on PR 102 whose first line reads CRITIC W4A SEAT RELIABILITY ROUND 2 with SAFE-TO-MERGE, BLOCK-FIX or BLOCK-ESCALATE and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/w4a-seat-reliability
