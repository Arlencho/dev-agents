# Fleet optimization W1 round 2: four findings from CRITIC W1 LEDGER ROUND 1 on PR 96, plus the work share above 100 percent. Tier C, final round. 2026-09-14
#
# DISPATCH: ./scripts/dispatch.sh git@github.com:Arlencho/dev-agents.git wave-plans/dev-agents/2026-09-14-w1-ledger-fix1.plan --detach --retries 1
# FIX-ROUND: 2026-09-14-w1-ledger.plan
# TIER: C
# ────────────────────────────────────────────────────────────────────────────

1 | web-frontend | On branch feat/w1-ledger (PR 96), rebase onto origin/main first. Fix the four findings of the comment on PR 96 whose first line reads CRITIC W1 LEDGER ROUND 1: BLOCK-FIX: ONE, a first-party seat with a recorded result line is never marked cost unknown because its run log is missing or the seat_log event names a later file; read the cost from wherever the result line is; TWO, round is the round of the plan itself (its file name or its own ROUND or FIX-ROUND header), never the highest round number mentioned anywhere in the plan text; THREE, a rollup whose seats are all cost unknown prints cost unknown, never a zero, and a mixed rollup prints the known sum plus the count unknown; FOUR, correct docs/ledger.md so every traced example names the file its line really comes from. FIVE, added by the orchestrator: work share is active time divided by elapsed time per seat or per wave, never active time summed across parallel seats divided by elapsed wall time; the day and PR rollups report seat hours and wall hours as separate columns and a work share that cannot exceed 100 percent. This is a fix round: run only tests/run-ledger-tests.sh, with a fixture per finding; nothing else is run. VERIFY: tests/run-ledger-tests.sh real exit code, and the per-day rollup for 2026-09-13 pasted in the PR body under a Round 2 heading. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Commit and push. | feat/w1-ledger

2 | plan-critic | READ-ONLY REVIEW ROUND 2 of PR 96. Tier C, final round: after this the orchestrator merges or stops. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. Re-run only the fixtures of the four round 1 findings and the work share rule, plus one regression check that a rebuild is still byte-identical. Report only a finding that makes a number wrong or misleading. Post ONE comment on PR 96 whose first line reads CRITIC W1 LEDGER ROUND 2 with SAFE-TO-MERGE, BLOCK-FIX or BLOCK-ESCALATE and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/w1-ledger
