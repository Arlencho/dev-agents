# Fix wave for feat/floor-context (PR 74) after both critics BLOCK-FIX. 2026-09-13
#
# DISPATCH: ./scripts/dispatch.sh git@github.com:Arlencho/dev-agents.git wave-plans/dev-agents/2026-09-13-floor-context-fix.plan --auto --retries 1
# Frontend critic: one block (replay claims live in the NOW card head). Data critic:
# four items, the first a redaction leak (task_line publishes a home path).
# ────────────────────────────────────────────────────────────────────────────

1 | devops | Fix the data half of PR 74 on branch feat/floor-context per the comment on the PR whose first line reads CRITIC FLOOR CONTEXT DATA. READ IT IN FULL FIRST. ONE, the redaction leak: task_line must pass the same scrub as now.program plus a path scrub: any token containing a slash is replaced by the literal outside-repo unless it is a path inside the worktree, and the first-sentence cut must never fall through to the rest of the body: cut at the first sentence end regardless of length, else at 120 characters; add the three critic inputs as tests and paste the outputs, which must contain no path. TWO, replay seats keep issue, task_line and pr as the schema says and the page already reads. THREE, a dispatch still running whose dispatch_start fell on the previous local date is followed: read yesterday streams for dispatches with no dispatch_end. FOUR, document the issue field in the fleet-queue/1 schema. VERIFY: python3 -m py_compile, tests under tests/ green, desk_live.py --once against real streams, real exit codes. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Push to the same branch. | feat/floor-context

2 | web-frontend | Fix the page block of PR 74 on branch feat/floor-context per the comment whose first line reads FRONTEND CRITIC FLOOR CONTEXT: under the REPLAY watermark the NOW card head still says seats live; follow the same branch the seat sentence already uses (drop the word live under replay) in both mirrors, templates/experience/floor.js near line 501 and the Floor renderer in scripts/experience_build.py near line 1277, and make the card head and the repo group header agree in every state. Rebuild with make experience and paste the NOW card head under live, stale, offline and replay. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Push to the same branch. | feat/floor-context

3 | frontend-critic | READ-ONLY RE-REVIEW of the Floor page on branch feat/floor-context (PR 74) after the fix waves. No commits, no push, no edits; tmp writes and the gitignored site build only. Record head SHA at start and at report. Re-run your replay repro and paste the NOW card head and the first repo header under replay, stale, offline and live; then re-run your done-when checks 1 to 5 and the cold read with two repos live. Post ONE comment on the PR whose first line reads FRONTEND CRITIC FLOOR CONTEXT ROUND 2 with SAFE-TO-MERGE or BLOCK-FIX. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/floor-context

4 | plan-critic | READ-ONLY RE-CHECK of the data half on branch feat/floor-context (PR 74) after wave 1. No commits, no push, no edits; tmp writes only. Record head SHA at start and at report. Feed the three leaky seat lines from your round-one comment plus a fourth with a short opener and a home path in the second sentence, and paste task_line for each; any path is a BLOCK. Confirm replay seats carry the three fields, a dispatch started yesterday and still running is followed, and the queue schema documents issue. Post ONE comment on the PR whose first line reads CRITIC FLOOR CONTEXT DATA ROUND 2 with SAFE-TO-MERGE or BLOCK-FIX. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/floor-context
