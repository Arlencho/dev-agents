# Floor v3-C: the yesterday toggle and the notification path. Owner approval 2026-09-12 (docs/proposals/floor-v3-purpose.md section 5). 2026-09-13
#
# DISPATCH: ./scripts/dispatch.sh git@github.com:Arlencho/dev-agents.git wave-plans/dev-agents/2026-09-13-floor-v3c-yesterday-notify.plan --detach --retries 1
# Why: a BLOCK at 22:00 waits until someone opens the page, and yesterday failures live only in the Almanac.
# ────────────────────────────────────────────────────────────────────────────

1 | devops | Branch feat/floor-v3c from origin/feat/floor-v3b. Implement docs/proposals/floor-v3-purpose.md section 5. ONE, a yesterday toggle on the status strip: scripts/desk_live.py exposes the previous day read the same way as today (same schema, same honesty rules, marked as yesterday in the payload) and the strip offers today and yesterday; history deeper than that stays in the Almanac. TWO, push: extend scripts/notify.sh so a NEEDS YOU item with no action taken for N minutes sends one macOS notification with the same one-line text, once per item, off by default, enabled by one env var documented in docs/experience-data.md and the README. Tests: the yesterday payload for a fixture day matches the today shape; an item older than N minutes notifies once and never twice; the env var unset sends nothing. VERIFY: the desk test runner passes with real exit codes in the PR body. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Open a draft PR against feat/floor-v3b titled Floor v3-C: yesterday and notifications, Refs 72. | feat/floor-v3c

2 | plan-critic | READ-ONLY REVIEW of the PR on branch feat/floor-v3c. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. Check: yesterday never renders as live; a notification can never fire for a replay or a stale item; the env var unset is truly silent; a notification repeats never, even across desk restarts; nothing from a prompt, path or secret reaches the notification text. Post ONE comment on the PR whose first line reads CRITIC FLOOR V3C with SAFE-TO-MERGE or BLOCK-FIX and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/floor-v3c
