# Floor v3-C: yesterday toggle and the notification path. docs/proposals/floor-v3-purpose.md section 5. 2026-09-12
#
# DISPATCH: ./scripts/dispatch.sh git@github.com:Arlencho/dev-agents.git wave-plans/dev-agents/2026-09-12-floor-v3c-yesterday-notify.plan --auto --retries 1
# FIRE AFTER v3-B IS MERGED.
# ────────────────────────────────────────────────────────────────────────────

1 | devops | Section 5 of docs/proposals/floor-v3-purpose.md, branch feat/floor-v3c. ONE, a yesterday toggle: desk_live.py can project the previous local day (a --day argument and a query parameter on the SSE server), the strip gets a Today and Yesterday control, and a yesterday projection is watermarked as history exactly the way replay is, never live. TWO, notifications: extend scripts/notify.sh so a NEEDS YOU item that has been present with no action for N minutes (env var, default 10) sends one macOS notification with the same one-line text and the one action, at most once per item, off by default behind one env var documented in README; the check runs inside desk_live.py on each projection, never as a second daemon. Scope: scripts/desk_live.py, scripts/notify.sh, templates/experience/floor.js, the Floor renderer, docs/experience-data.md, README.md. VERIFY: project yesterday from real streams and paste the watermark line; simulate a needs-you item ageing past the threshold with the env var on and show exactly one notification fired (paste the notify.sh log line), and none with it off; tests green; real exit codes. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Open a draft PR with Refs 73. | feat/floor-v3c

2 | plan-critic | READ-ONLY REVIEW of branch feat/floor-v3c. No commits, no push, no edits; tmp writes only. Record head SHA at start and at report. Check: a yesterday projection can never render as live (watermark present, needs_you and now empty or marked history); the notification fires at most once per item, never when the env var is off, never for an item that already has an action taken, and never carries a prompt, task body or path; no second process is started; README documents the two env vars. Post ONE comment on the PR whose first line reads CRITIC FLOOR V3C with SAFE-TO-MERGE or BLOCK-FIX. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/floor-v3c
