# Floor v3-B fix round 3: R2-1 and R2-2 from CRITIC FLOOR V3B ROUND 2 BLOCK-FIX (posted on PR 78, the branch now lives as PR 80). 2026-09-13
#
# DISPATCH: ./scripts/dispatch.sh git@github.com:Arlencho/dev-agents.git wave-plans/dev-agents/2026-09-13-floor-v3b-fix2.plan --detach --retries 1
# ────────────────────────────────────────────────────────────────────────────

1 | web-frontend | On branch feat/floor-v3b (PR 80; the round 2 comment is on the closed PR 78, same branch), fix the two BLOCK findings of the comment whose first line reads CRITIC FLOOR V3B ROUND 2 BLOCK-FIX, following its fix shapes: R2-1, a replay receipt opened on a static desk (no live server) must be a visible state, watermark stating the replay is not available on this desk, LED off, strip figures hidden, elapsed not live, with an Exit to live Floor action; never a silent keep of the live LED. R2-2, replace the per-card pixel offsets with one rule, scroll-padding-top on html driven by the measured header height on load and resize (or breakpoints that follow the header wrap points), so every anchor target including queue rows lands below the sticky header at 400, 768 and 1280. Add a test per finding with the critic repro and keep every existing test green. Then rebase the branch on origin/main (the v3-A squash is there) and resolve conflicts keeping the page. VERIFY: make experience builds clean and the page tests pass, real commands and exit codes in the PR 80 body under a Round 3 heading. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Commit and push. | feat/floor-v3b

2 | frontend-critic | READ-ONLY REVIEW ROUND 3 of PR 80 on branch feat/floor-v3b. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. Re-run the round 2 repros for R2-1 and R2-2 at 400, 768 and 1280; re-run the five questions on the live fixture; confirm the branch is rebased on main and builds. Post ONE comment on PR 80 whose first line reads CRITIC FLOOR V3B ROUND 3 with SAFE-TO-MERGE or BLOCK-FIX and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/floor-v3b
