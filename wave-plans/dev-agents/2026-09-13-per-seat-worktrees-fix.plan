# Fix wave for feat/per-seat-worktrees (PR 75) after plan-critic BLOCK-FIX. 2026-09-13
#
# DISPATCH: ./scripts/dispatch.sh git@github.com:Arlencho/dev-agents.git wave-plans/dev-agents/2026-09-13-per-seat-worktrees-fix.plan --auto --retries 1
# Critic: git isolation of tree, index and HEAD holds. Three items block: dispatch
# id collisions within one second, the worktree test suite red (learning
# contamination through the stub), and the fetch point still parked on main.
# ────────────────────────────────────────────────────────────────────────────

1 | devops | Fix PR 75 on branch feat/per-seat-worktrees per the comment on the PR whose first line reads CRITIC PER SEAT WORKTREES. READ IT IN FULL FIRST. The branch carries two WIP commits (490c4e9 and 52c5d35) with partial round-two work from a seat that was interrupted (run-remote.sh, the launchers, the two test suites, the docs); read it, keep what is right, finish it, and squash nothing. ONE: two dispatches started in the same second share a dispatch id, therefore one runtime directory and one events file, and the first to finish deletes the runtime under the other; make the dispatch id unique (append the dispatcher pid or a nanosecond component in scripts/fleet-events.sh where the id is minted, and everywhere it is parsed), and prove two dispatches started in the same second get two runtimes and two event files. TWO: tests/run-worktree-tests.sh is red, 9 of 72, because a failure learning written after the noauth row carries the auth phrase, the next seat preamble injects it, the stub CLI echoes the prompt, and the launcher classifies the echoed phrase as a rate-cap or auth exit; fix the root cause, not the test: the learning summary must never carry the raw classifier phrase (normalise it to a fixed code), and the classifier must look at the CLI output stream only, never at injected preamble text; then the suite must be green with the exit code pasted; also fix the CI check named Fleet unit tests on the PR head, paste the green run. THREE: the fetch point must be detached or bare, never parked on main, so a seat whose branch is main can start; adjust the clean-tree check so its error names the real reason. VERIFY: bash -n on touched scripts; tests/run-worktree-tests.sh, tests/run-dispatch-lock-tests.sh and tests/run-launcher-tests.sh green with exit codes pasted; re-run the two-dispatches-same-second proof and paste the two ids. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Push to the same branch. Do not merge. | feat/per-seat-worktrees

2 | plan-critic | READ-ONLY RE-REVIEW of branch feat/per-seat-worktrees (PR 75) after the fix wave. No commits, no push, no edits; tmp writes only. Record head SHA at start and at report. Grade your three items CLEARED or STILL OPEN with proof: start two dispatches in the same second and paste both ids, runtime directories and event files; run tests/run-worktree-tests.sh and paste the totals; check the fetch point after a run is detached or bare and that a seat on branch main starts. Re-run the isolation proofs from round one (two seats one wave, two dispatches different branches, same branch serialises, SIGKILL recovery, sweep) and confirm they still hold. Confirm the CI check Fleet unit tests is green on the head. Post ONE comment on the PR whose first line reads CRITIC PER SEAT WORKTREES ROUND 2 with SAFE-TO-MERGE or BLOCK-FIX and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/per-seat-worktrees
