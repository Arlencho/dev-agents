# Floor: simple, straightforward, self-explanatory. Issue 69. 2026-09-12
#
# DISPATCH: ./scripts/dispatch.sh git@github.com:Arlencho/dev-agents.git wave-plans/dev-agents/2026-09-12-floor-plain.plan --auto --retries 1
# ────────────────────────────────────────────────────────────────────────────

1 | devops | Data half of issue 69, on branch feat/floor-plain (if the branch already exists on origin, continue from it; if it holds partial commits from an interrupted seat, keep what is correct and finish). Scope: scripts/desk_live.py, the seat_progress reader that feeds it (the launcher streaming from PR 68), scripts/queue.sh, docs/experience-data.md. Not the page. READ FIRST the issue, docs/experience-data.md honesty rules and live schema, and the reader. DELIVER: ONE, the progress reader adds the program name of the last shell command to seat_progress (first token only, after stripping env assignments and sudo or nohup or time wrappers; never arguments, never paths outside the repo; a token that looks like a path is reduced to its basename), documented in the event envelope section. TWO, desk_live.py projects, per live seat, everything the page needs to write one sentence: role, phase, program name, plan purpose (from the queue entry or the plan header), wave x of N (N from the plan file), elapsed seconds, seconds since last heartbeat; and a top-level summary object with running count, queued count, landed-today count and seconds since the last event; all documented in the live schema section. THREE, tests under tests/ for the token reduction (env assignment, sudo, a path, a plain program) and for the summary counts. VERIFY: python3 -m py_compile on touched files, the tests green, desk_live.py --once against real streams of today validates, real exit codes pasted. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Open a draft PR with Closes 69. | feat/floor-plain

2 | web-frontend | Page half of issue 69, on branch feat/floor-plain after wave 1 landed the data. Scope: templates/experience/floor.js, templates/experience/site.css, and the Floor renderer inside scripts/experience_build.py; site/ is build output, never edit it by hand, rebuild with make experience. READ FIRST the issue (the seven done-when items), the wave 1 fields in live.json and docs/experience-data.md, and the current page as built. DELIVER the page items of the issue: a one-line plain-words header from the summary object; each running seat as one sentence built from the wave 1 fields, for example devops is committing (git) on the Iris scaffold, wave 1 of 4, 12 min, heartbeat 20 s ago, with ids and schema words demoted to a dim secondary line or a title attribute; up next as position, purpose, repo with the plan filename dim; landed today as purpose, outcome in words with failed visibly distinct, duration in minutes, branch dim; the three watermark states explained in place with one short sentence each when they apply. Keep the watermark, the queued-never-running rule and the frozen ticker exactly as they are; keep the existing visual language, do not introduce a second style. VERIFY: make experience, serve the site, load the Floor with a projection from real streams of today at 1280 and 400 wide, no horizontal scroll, and write in the PR body what each block reads verbatim; real exit codes pasted. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Push to the same branch, keep the PR a draft. | feat/floor-plain

3 | frontend-critic | READ-ONLY REVIEW of branch feat/floor-plain in dev-agents. No commits, no push, no edits; tmp writes and the gitignored site build only. Record head SHA at start and at report. THE TEST THAT MATTERS: read the page cold. Build the site, load the Floor with a projection from real streams of today plus a fixture with two live seats, three queued plans and three landings including one failed, at 1280 and at 400 wide, and for every block write down in one line what a first-time reader would take it to mean; anything that needs the docs to be understood is a BLOCK. Then check: the header line matches the counts in live.json; each seat sentence matches its seat object (phase, program name, plan purpose, wave x of N, elapsed, heartbeat age); no argument, path outside the repo, prompt text or id appears in the activity line (grep the stream for the task text and assert absent); failed landings are visibly distinct; no horizontal scroll at 400; keyboard focus visible on anything interactive; stale and offline states still degrade every new element. Post ONE comment on the PR whose first line reads FRONTEND CRITIC FLOOR PLAIN with SAFE-TO-MERGE or BLOCK-FIX and line citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/floor-plain

4 | plan-critic | READ-ONLY REVIEW of the data half on branch feat/floor-plain in dev-agents, independent of the page. No commits, no push, no edits; tmp writes only. Record head SHA at start and at report. Check against docs/experience-data.md: the program-name token can never carry an argument, a secret or a path outside the repo (feed the reducer a command line with a token, a home-directory path, an env assignment and a sudo wrapper, and paste what came out); the summary counts equal what the streams and the queue file say for today; a queued plan still never projects as running; the schema docs match the fields emitted. Post ONE comment on the PR whose first line reads CRITIC FLOOR PLAIN DATA with SAFE-TO-MERGE or BLOCK-FIX and line citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/floor-plain
