# Floor v3-C fix round: two findings from CRITIC FLOOR V3C BLOCK-FIX on PR 79. 2026-09-13
#
# DISPATCH: ./scripts/dispatch.sh git@github.com:Arlencho/dev-agents.git wave-plans/dev-agents/2026-09-13-floor-v3c-fix1.plan --detach --retries 1
# ────────────────────────────────────────────────────────────────────────────

1 | devops | On branch feat/floor-v3c (PR 79), fix both findings of the comment on PR 79 whose first line reads CRITIC FLOOR V3C BLOCK-FIX, following its fix lines: ONE, an absolute path can reach the notification text; run the notification line and the PR title through the same slash-token rule as task_path before add, and make scripts/notify.sh refuse an absolute path so a hand-written live.json cannot bypass it; TWO, ready_to_merge notifies twice for the same PR; key the seen file on the stable identity (type plus source kind plus PR number, or the comment id for a block), never on the comments array. Add a test per finding with the critic fixtures and keep every existing test green. VERIFY: the desk test suite passes with real exit codes in the PR body under a Round 2 heading. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Commit and push. | feat/floor-v3c

2 | plan-critic | READ-ONLY REVIEW ROUND 2 of PR 79 on branch feat/floor-v3c. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. Re-run the round 1 fixtures for the path leak and the double notification; try a path hidden in the PR title and a block whose comment is edited; confirm the yesterday payload still holds its honesty marks. Post ONE comment on PR 79 whose first line reads CRITIC FLOOR V3C ROUND 2 with SAFE-TO-MERGE or BLOCK-FIX and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/floor-v3c
