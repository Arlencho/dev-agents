# Floor v3-B: the page in the order of the proposal. docs/proposals/floor-v3-purpose.md section 4. 2026-09-12
#
# DISPATCH: ./scripts/dispatch.sh git@github.com:Arlencho/dev-agents.git wave-plans/dev-agents/2026-09-12-floor-v3b-page.plan --auto --retries 1
# FIRE AFTER v3-A IS MERGED.
# ────────────────────────────────────────────────────────────────────────────

1 | web-frontend | The Floor v3 page, branch feat/floor-v3b. READ FIRST docs/proposals/floor-v3-purpose.md section 4 (the order is the spec), the v3-A fields in live.json and docs/experience-data.md, and the page as merged after issue 72. Scope: templates/experience/floor.js, templates/experience/site.css, the Floor renderer in scripts/experience_build.py; site/ is build output, rebuild with make experience. DELIVER, top to bottom: the status strip (4.1) with each figure linking to its section and needs-you red when non-zero, stale and offline stated in words before any number; NEEDS YOU (4.2) one row per item with its one action as a link or a copyable command, empty state Nothing needs you; NOW by repo (4.3) as merged in issue 72 plus the quiet mark; UP NEXT (4.4) with the blocked reason in place; INITIATIVES (4.5) one row per milestone as specified, with the fallback row marked when gh was unavailable; LANDED and FAILED today (4.6) with failed first when non-empty and the critic verdict count when known; everything else behind one details control at the bottom, closed by default (4.7). Keep the visual language; keep every honesty rule in section 6; no ids in headers; one screen at 1280, one scroll at 400, no horizontal scroll, focus visible. VERIFY: make experience, load at 1280 and 400 with a fixture of two repos live, two needs-you items (one BLOCK, one ready-to-merge), three queued plans one of them blocked, two initiatives, three landings one failed, and with real streams of today; paste what the strip, the first NEEDS YOU row, one seat card and one initiative row read verbatim; real exit codes. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Open a draft PR with Refs 73. | feat/floor-v3b

2 | frontend-critic | READ-ONLY REVIEW of the Floor page on branch feat/floor-v3b. No commits, no push, no edits; tmp writes and the gitignored site build only. Record head SHA at start and at report. THE ACCEPTANCE TEST is section 7 of docs/proposals/floor-v3-purpose.md: read the page cold, with two repos live and one BLOCK outstanding, at 1280 and at 400, and answer the five questions of section 1 from the page alone; write each answer down with the element you read it from; any question you cannot answer in one glance is a BLOCK. Then: every strip figure links to the right section, needs-you rows carry exactly one action, the empty state reads Nothing needs you, stale and offline degrade every section in words, replay hides the live sections, no ids in headers, no horizontal scroll, focus visible on the details control and every link. Post ONE comment on the PR whose first line reads FRONTEND CRITIC FLOOR V3B with SAFE-TO-MERGE or BLOCK-FIX and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/floor-v3b
