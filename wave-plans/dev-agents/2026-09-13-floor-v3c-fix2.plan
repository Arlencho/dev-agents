# Floor v3-C fix round 3: one finding from CRITIC FLOOR V3C ROUND 2 BLOCK-FIX on PR 79 (a path hidden inside a token still reaches the toast). 2026-09-13
#
# DISPATCH: ./scripts/dispatch.sh git@github.com:Arlencho/dev-agents.git wave-plans/dev-agents/2026-09-13-floor-v3c-fix2.plan --detach --retries 1
# ────────────────────────────────────────────────────────────────────────────

1 | devops | On branch feat/floor-v3c (PR 79), fix finding 1 of the comment on PR 79 whose first line reads CRITIC FLOOR V3C ROUND 2 BLOCK-FIX: in task_path and has_operator_path a token counts as a path when an absolute, home, variable or parent-escape path sits anywhere inside it (fix:/Users/x, `~/.ssh`, $HOME/x, ../x), and backticks are stripped the way quote wrappers are; the toast and the projector both use it. Add the critic fixture as a test (the PR title carrying a path after a colon) and keep every existing test green. VERIFY: the desk test suite passes with real exit codes in the PR body under a Round 3 heading. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Commit and push. | feat/floor-v3c

2 | plan-critic | READ-ONLY REVIEW ROUND 3 of PR 79 on branch feat/floor-v3c. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. Re-run the round 2 fixture for the embedded path and two more shapes of your choice (a path inside parentheses, a URL-encoded path), confirm the double notification fix still holds. Post ONE comment on PR 79 whose first line reads CRITIC FLOOR V3C ROUND 3 with SAFE-TO-MERGE or BLOCK-FIX and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/floor-v3c
