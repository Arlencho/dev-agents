# Routing: every first-party seat that pins claude-fable-5-1 moves to claude-opus-5 to save spend; providers unchanged (producers stay on the cross-vendor seats, critics and security stay first-party). Owner decision 2026-09-14 08:40. 2026-09-14
#
# DISPATCH: ./scripts/dispatch.sh git@github.com:Arlencho/dev-agents.git wave-plans/dev-agents/2026-09-14-routing-first-party-model-opus.plan --detach --retries 1
# Why: the owner wants to save spend for the time being; the top first-party tier is replaced by the one below it everywhere it is pinned. Provider routing from PR 88 stays exactly as it is.
# ────────────────────────────────────────────────────────────────────────────

1 | web-frontend | Branch feat/routing-model-opus from origin/main. Replace the model id claude-fable-5-1 with claude-opus-5 everywhere it is pinned as a model value: config/routing.yaml model_routing (every entry, including default), config/workers.yaml, every roles/*.md model line, every providers/claude/agents/*.md model line, and any script, test fixture or doc that names it as the model a seat runs. Do not change provider_preferences or provider_failover in any way: producers stay on their current cross-vendor providers, critics and security-reviewer stay first-party, plan-critic and devops-critic stay as they are. Update comments that describe the tier. Update tests/run-routing-tests.sh so every first-party seat asserts claude-opus-5 and every PR 88 provider rule still asserts as before. Historical records (wave-plans, logs, handoffs, dated notes) are not edited. VERIFY: grep -rn claude-fable-5-1 over config roles providers scripts tests README.md CLAUDE.md returns nothing; tests/run-routing-tests.sh and make test pass with real exit codes in the PR body; resolve provider and model for every role with the dispatcher and paste the table. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name in commit messages or PR text (role names only; model and vendor values inside config files are values), no Co-Authored-By trailer. Open a draft PR titled Routing: first-party seats move one tier down to save spend, providers unchanged. | feat/routing-model-opus

2 | plan-critic | READ-ONLY REVIEW of the PR on branch feat/routing-model-opus. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. Confirm: no pinned claude-fable-5-1 remains outside historical records; every first-party seat resolves to claude-opus-5; provider_preferences and provider_failover are byte-identical to main; every producer still resolves to its cross-vendor provider and never fails over to first-party; the tests fail if any of those is broken in a tmp copy. Post ONE comment on the PR whose first line reads CRITIC ROUTING MODEL OPUS with SAFE-TO-MERGE, BLOCK-FIX or BLOCK-ESCALATE and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/routing-model-opus
