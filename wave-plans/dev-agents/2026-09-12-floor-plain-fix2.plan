# Round three for feat/floor-plain (PR 70): one page block left. 2026-09-12
#
# DISPATCH: ./scripts/dispatch.sh git@github.com:Arlencho/dev-agents.git wave-plans/dev-agents/2026-09-12-floor-plain-fix2.plan --auto --retries 1
# Data half SAFE-TO-MERGE (round 2). Frontend critic round 2: blocks 1, 3, 4 CLEARED; block 2 STILL OPEN.
# ────────────────────────────────────────────────────────────────────────────

1 | web-frontend | Close the last block on branch feat/floor-plain (PR 70), page only: templates/experience/floor.js, templates/experience/site.css, the Floor renderer in scripts/experience_build.py; site/ is build output, rebuild with make experience. READ FIRST the comment on the PR whose first line reads FRONTEND CRITIC FLOOR PLAIN ROUND 2, block 2 and the two not-blocking notes. BLOCK 2: under stale and offline the sentence says at the last event and before the last event but the numbers are measured from projection time, so they are wrong by the age of the projection. Compute both clauses from timestamps the projection already carries: elapsed as last_event_ts minus seats[].started_at, heartbeat as last_event_ts minus seats[].last_heartbeat_ts, on both the browser path and the build-time renderer; or drop both clauses when degraded and let the top line carry the age. Verify with the exact fixture the critic used (seat_dispatch 890 s before the last event, heartbeat 5 s before) and paste the sentence under stale and under offline: it must read 15 min in at the last event and last heartbeat 5 s before the last event, or omit them. NOTES: give the timer column a fixed width so the outcome word does not jitter between rows; floor instead of round in the age formatter on both sides so the top line and the LED agree between 90 and 119 seconds. VERIFY: make experience, load the Floor at 1280 and 400 with the live, stale and offline fixtures, no horizontal scroll, paste the top line and one sentence per state, real exit codes. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Push to the same branch, keep the PR a draft. | feat/floor-plain

2 | frontend-critic | READ-ONLY RE-REVIEW of the Floor page on branch feat/floor-plain (PR 70) after round three. No commits, no push, no edits; tmp writes and the gitignored site build only. Record head SHA at start and at report. Re-run your block 2 fixture exactly and paste the sentence under stale and offline; confirm the numbers are measured from the last event or the clauses are gone. Check the outcome column no longer moves between rows at 1280 and 400, and the age formatter agrees with the LED between 90 and 119 seconds. Re-run everything that passed in round two. Post ONE comment on the PR whose first line reads FRONTEND CRITIC FLOOR PLAIN ROUND 3 with SAFE-TO-MERGE or BLOCK-FIX. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/floor-plain
