# Floor: repo-first seats with issue, milestone, task line and PR; legacy chrome collapsed. Issue 72. 2026-09-12
#
# DISPATCH: ./scripts/dispatch.sh git@github.com:Arlencho/dev-agents.git wave-plans/dev-agents/2026-09-12-floor-context.plan --auto --retries 1
# ────────────────────────────────────────────────────────────────────────────

1 | devops | Data half of issue 72, branch feat/floor-context. Scope: scripts/desk_live.py, scripts/queue.sh, tests/, docs/experience-data.md. Not the page. READ FIRST the issue, docs/experience-data.md (honesty rules, live schema, gh enrichment section which already says optional and never fatal), scripts/desk_live.py as merged in #70. DELIVER: ONE, the projector follows every live dispatch of the day, and each seat, queue entry and landing carries repo as a first-class field. TWO, per seat and per queue entry: the issue number parsed from the plan header line that names it (the pattern Issue NNNN), the milestone title fetched through gh for that repo and issue when gh is available, cached per run, never fatal, never blocking the projection, and a field saying whether it was verified or skipped. THREE, per seat: task_line, the first sentence of that seat line in the plan file cut at 120 characters and passed through the same secret scrub as the program token; never more of the task body. FOUR, per seat and per landing: the open or merged PR number and title for the branch through gh, same rules. FIVE, per repo: a summary object with seats live and dispatches live, plus the global one from #70. SIX, schema documented, tests for the issue parse, the task line cut and scrub, the repo grouping and the enrichment skip path. VERIFY: python3 -m py_compile on touched files, tests under tests/ green, desk_live.py --once against real streams of today with two repos live if present, real exit codes pasted. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Open a draft PR with Closes 72. | feat/floor-context

2 | web-frontend | Page half of issue 72 on branch feat/floor-context after wave 1. Scope: templates/experience/floor.js, templates/experience/site.css, the Floor renderer in scripts/experience_build.py; site/ is build output, rebuild with make experience. READ FIRST the issue (done-when items 1 to 5), the wave 1 fields, and the page as built. DELIVER: the summary line first on the page with everything that used to sit above it (replay scrubber, schema intro, trail block, four pipeline tiles) inside one details control closed by default; NOW grouped by repo with a header and per-repo counts; each seat card in the order the issue specifies (repo, issue and milestone with the unverified mark when skipped, purpose, seat task line, status clause, branch dim, PR number and title when present); UP NEXT and LANDED TODAY rows with the repo first and the issue or PR number when present. Keep the existing visual language, the watermark, the degraded states from #70, no horizontal scroll at 400. VERIFY: make experience, load the Floor at 1280 and 400 with a fixture of two repos live (two seats each), three queued plans across both repos, three landings including one failed, and with real streams of today; paste what the summary, one repo header and one seat card read verbatim; real exit codes. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Push to the same branch, keep the PR a draft. | feat/floor-context

3 | frontend-critic | READ-ONLY REVIEW of the Floor page on branch feat/floor-context. No commits, no push, no edits; tmp writes and the gitignored site build only. Record head SHA at start and at report. THE TEST THAT MATTERS: read the page cold with two repos live and say, for each seat, which repo it is in and what it is fixing, from the page alone; if that cannot be answered in one glance it is a BLOCK. Then check the done-when items 1 to 5 of the issue one by one at 1280 and 400, no horizontal scroll, keyboard focus visible on the details control, stale and offline degrade the new elements, replay hides them. Post ONE comment on the PR whose first line reads FRONTEND CRITIC FLOOR CONTEXT with SAFE-TO-MERGE or BLOCK-FIX and line citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/floor-context

4 | plan-critic | READ-ONLY REVIEW of the data half on branch feat/floor-context. No commits, no push, no edits; tmp writes only. Record head SHA at start and at report. Check against docs/experience-data.md: the enrichment is optional and never fatal (run the projector with gh absent and with gh failing, paste the outcome and the unverified marks); the task line never exceeds the first sentence or 120 characters and passes the secret scrub (feed a seat line with a token and a home path); every live dispatch of the day is followed; the per-repo counts equal the streams; a queued plan never projects as running; the schema docs match the fields. Post ONE comment on the PR whose first line reads CRITIC FLOOR CONTEXT DATA with SAFE-TO-MERGE or BLOCK-FIX. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/floor-context
