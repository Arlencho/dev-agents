# Wave plan for dev-agents — 2026-07-29T22:02:44Z
# Generated by dispatch.sh

1 | web-frontend | REVISE ONLY on PR #59 feat/fleet-desk-v2-phase-b after frontend-critic REVISE. Read handoff.md PR #59 comment and tests/critic/phase-b-honesty-repro.sh. Fix D1 D2 and pin M1. D1: experience_build.py must derive live/stale/offline from last_event_ts at build time for floor AND home teaser (same thresholds as desk_live.py / floor.js) — never trust stored staleness.state alone for age display; update tests so fixtures with old last_event_ts assert offline/stale not led live. D2: dispatch.sh must close fleet events on SIGINT as well as EXIT (trap INT TERM EXIT or equivalent so Ctrl-C writes dispatch_end aborted). M1: add executing test that runs the close-out path (not grep-only) so deleting the trap turns suite RED. Wire critic repro into make test once green. Prove bash tests/critic/phase-b-honesty-repro.sh green and make test green. Push same branch leave PR open. No redesign. No schema invent. | feat/fleet-desk-v2-phase-b
