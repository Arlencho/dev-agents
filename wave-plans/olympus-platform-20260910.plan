# Wave plan for olympus-platform — 2026-09-10T21:52:09Z
# Generated by dispatch.sh

1 | devops | Write the Wave 0 contract for the assistant channel: one PRD page plus the tool schemas. READ FIRST, in this order, before writing a word: issue 2797 in full, which carries the four closed decisions and twenty verified facts by file and line; issue 2798, which is your acceptance criteria; docs/prd/00-INDEX.md; docs/prd/01-conventions.md; docs/prd/02-shells.md; docs/prd/pages/concierge-telegram-pay-link.md, which is the closest existing shape, a spec for a non-web surface marked PROPOSED; docs/prd/pages/24-account-concierge.md, which is the settings panel the Connected Assistants tile will extend; and docs/prd/pages/atlas-chat.md section 10 for settlement. THIS IS DOCS ONLY. Change nothing under apps/ or services/, write no code, no schemas in api.yaml, no tests. THE DELIVERABLE is a new page under docs/prd/pages/ for the assistant surface, indexed in 00-INDEX.md in the correct place and style, and it must make make check-prd-index exit 0. MARK EVERY BLOCK PROPOSED PENDING CO-FOUNDER SIGN-OFF. Never write ACCEPTED, approved or signed off, never stamp a co-founder name. A block you wrote and stamped in the same commit was approved by nobody, and this repo has been burned by exactly that. WHAT THE PAGE MUST CONTAIN, each as its own section. One, the division of labour table from the epic verbatim: their assistant owns the conversation, Olympus owns place resolution, search, ranking, price, the trip record, checkout and fulfilment, Atlas continues on our surfaces and never speaks inside theirs, the connector holds no supplier, payment or ranking logic. Two, the four closed decisions from the epic, stated as decisions and marked closed. Three, the tool schemas for all eight tools: search, clarify, refine, get_option, get_trip, list_trips, update_trip in Wave 2, and create_checkout, get_checkout_status in Wave 3. For each tool: its inputs with types, its outputs, which wave it belongs to, and whether it writes. Inputs carry counts of travellers, never names; passport, date of birth and card must not appear anywhere in any schema. Options are opaque identifiers we issue, bound to traveller, trip and search revision, and the schema must say a raw supplier offer id is never accepted from this channel. search takes structured fields plus an optional free-text brief that we normalise internally. Ambiguity returns needs_clarification with options and never a guessed airport. Every result carries web_url. Four, the money gate verbatim from the epic: five distinct testers complete assistant, then website, then a shortlist they say they would book, plus expired offer, duplicated checkout, revoked token and invented identifier all behave correctly. Write it as the exit criterion for Wave 2 and the entry criterion for Wave 3. Five, one sentence each on what Wave 2 tools may write, which is trip records, and what none of them may do, which is spend. Six, the two pilot hosts. Do not name them from memory. State that they are to be named by the founders and record, for each, a placeholder row for its authorization flow and its consequential-action behaviour to be filled from that host current documentation before W1 builds against it. Seven, the loading, empty and error states per 01-conventions.md, reusing ratified copy wherever one fits. Eight, the service directory name proposed, under services/ as a sibling of hermes, marked PROPOSED like everything else. ANY new string must be marked PROPOSED and collected in the PR body in one table so a co-founder can approve all copy in one pass. Reuse existing ratified copy rather than inventing near duplicates. HOUSE STYLE, not optional: no long dash character anywhere in the page, the commit message or the PR body; use a comma, a colon, parentheses or a new sentence. No AI tool or vendor name anywhere in the page, the commit or the PR text. VERIFY, pasting real exit codes captured directly rather than read after a pipe: make check-prd-index, and make lint if it covers docs. Confirm git diff shows only files under docs/prd/. Open a PR titled as the Wave 0 contract, referencing issue 2798 and issue 2797 with Refs and no closing keyword, because the issue closes on co-founder sign-off and not on merge. State in the PR body that it is PROPOSED and awaiting sign-off, and list the open placeholders for the pilot hosts. Omit any Co-Authored-By trailer. Do not merge. | docs/prd-assistant-channel-w0
2 | api-critic | READ-ONLY REVIEW of the wave 1 change on branch docs/prd-assistant-channel-w0. You are not a producer: no commits, no push, no gh pr review, no gh pr merge, no labels, no edits to the spec. The harness pushes the branch it checks out, so any commit on a producer ref is a production incident. Permitted writes are a slash tmp worktree only. Record the PR head SHA at start and re-check at report; if it moved report BLOCKED with both SHAs. THE QUESTION THAT MATTERS: could an engineer implement these tool schemas literally, without asking anyone anything, and ship something that leaks traveller documents, accepts a fabricated selection, or spends money in a wave that must not. Read the schemas as an adversary would. FIRST, walk every input field of every tool and confirm none can carry a name, a passport, a date of birth or a card, directly or through a free-form object. The search brief is free text by design, so confirm the spec says it is normalised server-side and never persisted as raw traveller data. SECOND, confirm option identifiers are specified as opaque, bound to traveller, trip and search revision, and that the spec states a raw supplier offer id is rejected from this channel. Cross-check against api.yaml createBooking, where selected_result is an open object today, and confirm the spec does not silently inherit that. THIRD, confirm no Wave 2 tool can reach a pay-link, by reading the schema and the wave assignment, not the prose. FOURTH, confirm the money gate is verbatim from issue 2797 and is stated as both the Wave 2 exit and the Wave 3 entry criterion. FIFTH, approval hygiene: nothing stamped ACCEPTED, approved or signed off, no co-founder named as approver, every new string marked PROPOSED and collected in the PR body. SIXTH, the pilot hosts must NOT be named from memory: confirm they are placeholders to be filled by the founders from current host documentation. SEVENTH, confirm the page is indexed correctly in 00-INDEX.md, that make check-prd-index exits 0 on the branch, and that the diff is docs only. EIGHTH, no long dash character in any added line, PR body or commit message, and no AI tool or vendor name anywhere. Post ONE comment on issue 2340 whose first line reads CRITIC ASSISTANT CHANNEL W0, with a verdict of SAFE-TO-MERGE or BLOCK-FIX or BLOCK-ESCALATE, per-finding severity and file and line citations. Note in the verdict that SAFE-TO-MERGE here means safe for co-founder review, since merge itself waits on sign-off. No vendor names, emails, booking ids or gcloud payloads. | docs/prd-assistant-channel-w0
