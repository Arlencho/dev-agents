# Wave plan for olympus-platform — 2026-09-15T22:40:25Z
# Generated by dispatch.sh

1 | go-backend | On branch feat/2863-two-ticket-round-trips (PR 2870). Read the comments on issue 2340 whose first lines read CRITIC TWO TICKET ROUND TRIPS BACKEND ROUND 2 and FRONTEND CRITIC TWO TICKET ROUND TRIPS ROUND 2. Pull first; main is already merged into the branch (commit 666ad24e) with both describe blocks kept in flight-widget.test.tsx. Do not rebase. R2-1: in searchFlightsWithTwoTicket build the round-trip request and return its error before taking the breaker probe, the same order as the stream path; add the critic fixture to duffel_two_ticket_test.go. N1: keep one name for the closed-breaker check. This is a fix round: run only go test with the race flag on apps/api/internal/provider and apps/api/internal/service; nothing else is run. VERIFY: those with real exit codes in the PR body under a Round 3 heading. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Commit and push. | feat/2863-two-ticket-round-trips
2 | web-frontend | On branch feat/2863-two-ticket-round-trips, pull first. N4 from the frontend round 2 comment: when the assistant-selected offer named by continueOfferId is a two-ticket result, do not render the Continue with this flight button, since checkout cannot book it yet; add a test. N5: add a direct test for the buildTripCart fallback in useTripController. This is a fix round: run only vitest on flight-widget, flight-continue-button, handover-landing, package-card, TripComboList, useTripController and two-ticket-critic test files, web lint and web typecheck; nothing else is run. VERIFY: those with real exit codes appended to the PR body. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Commit and push. | feat/2863-two-ticket-round-trips
3 | backend-critic | READ-ONLY REVIEW ROUND 3 of PR 2870, whole PR. Tier B, the owner lifted the round cap on 2026-09-16. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. Re-run R2-1 and TestCritic_Blocking_OneWayStealsHalfOpenProbe, confirm the PR is mergeable with main and both describe blocks are present, and check N4 and N5 tests exist and pass. Post ONE comment on issue 2340 whose first line reads CRITIC TWO TICKET ROUND TRIPS ROUND 3 with SAFE-TO-MERGE, BLOCK-FIX or BLOCK-ESCALATE and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/2863-two-ticket-round-trips
