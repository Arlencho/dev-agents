# Wave plan for olympus-platform — 2026-09-16T10:51:02Z
# Generated by dispatch.sh

1 | go-backend | On branch fix/2846-copy-cache-and-clone (PR 2879), pull first. Read the comments on issue 2340 whose first lines read CRITIC HANDOVER COPY CACHE ROUND 1, SECURITY HANDOVER COPY CACHE and SELF-RATIFICATION 2026-09-16 12:40. ONE: add the critic fixture that kills the surviving mutant, so disabling the deferred Release on a failed clone turns the test red; place it in apps/api/internal/service/result_sessions_clone_idempotency_test.go as the critic wrote it. TWO: Go Lint fails on the head with cognitive complexity 21 in the clone method at apps/api/internal/service/result_sessions.go:1078; split it into named helpers that keep the booking pattern recognisable rather than raising the threshold or adding a suppression comment. THREE: change the three PRD blocks the critic listed from PROPOSED to ACCEPTED citing the self-ratification comment. This is a fix round: run only go test with the race flag on apps/api/internal/service and apps/api/internal/handler, plus the Go lint target for apps/api; nothing else is run. VERIFY: those with real exit codes in the PR body under a Round 2 heading. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Commit and push. | fix/2846-copy-cache-and-clone
2 | backend-critic | READ-ONLY REVIEW ROUND 2 of PR 2879. Tier B, round 2. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. Re-run only your new fixture plus TestResultSessionsHandler_Clone_RepeatKeyReturnsTheOriginalCopy, confirm the lint job passes on the head, and confirm the refactor did not change behaviour by re-running your round 1 mutants on the claim, replay owner check and complete sites. Post ONE comment on issue 2340 whose first line reads CRITIC HANDOVER COPY CACHE ROUND 2 with SAFE-TO-MERGE, BLOCK-FIX or BLOCK-ESCALATE and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | fix/2846-copy-cache-and-clone
