# Wave plan for olympus-platform — 2026-09-15T21:55:36Z
# Generated by dispatch.sh

1 | go-backend | On branch fix/2860-2861-iris-search (PR 2872), rebase onto origin/main first. Read the comments on issue 2340 whose first lines read CRITIC IRIS SEARCH WALK FIXES ROUND 1 and SELF-RATIFICATION 2026-09-16 00:10. F1: a free-text hotel area parsed from a brief is never stored on the search record and never sent to the API; withdraw the PROPOSED 3.3.3 amendment in docs/prd/pages/assistant-channel.md; restore the earlier critic test with the name and address case and make it green. F5: send only fields the API refine acts on; add a not_applied list to the search and refine outcome naming each narrowing understood from the brief but not applied (hotel area, board other than breakfast), document it in assistant-channel.md section 3.3 as ACCEPTED citing the self-ratification comment, and assert it in the walk refine test. F2, F3, F4, F6, F7 exactly as the critic wrote them; the four TestR1 repro tests from the critic comment must be green. This is a fix round: run only go test with the race flag on services/iris/internal/tools and services/iris/internal/olympus and on the apps/api packages you touched; nothing else is run. VERIFY: those with real exit codes in the PR body under a Round 2 heading. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Commit and push. | fix/2860-2861-iris-search
2 | backend-critic | READ-ONLY REVIEW ROUND 2 of PR 2872. Tier B, round 2 of at most 3. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. Re-run only TestR1 F1 to F4, check F5 not_applied against the self-ratification, F6 and F7, plus the regression TestWalkRefineSendsDeltasAndLearningRecord. Post ONE comment on issue 2340 whose first line reads CRITIC IRIS SEARCH WALK FIXES ROUND 2 with SAFE-TO-MERGE, BLOCK-FIX or BLOCK-ESCALATE and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | fix/2860-2861-iris-search
