# Wave plan for olympus-platform — 2026-09-12T21:41:10Z
# Generated by dispatch.sh

1 | go-backend | Implement the six read tools in services/iris and the quota they run under. READ FIRST docs/prd/pages/assistant-channel.md sections 3.1 to 3.4 in full (the fields that must never appear, identifiers and revisions and web_url, the tool specs for search, clarify, refine, get_option, get_trip, list_trips, the error codes), then issue 2800 for the six verified facts, then the API surface each tool maps to in api.yaml and apps/api/internal/handler/routes.go (result sessions, search, refine stream), and how hermes calls the same API (services/hermes, olympus.go near line 398 for the refine stream). Rules that are BLOCKS if broken: every result carries web_url to /r/{id}; no traveller name, email, phone or payment field appears in any argument or result in either direction; no ranking, pricing or supplier call happens inside iris, it only translates and forwards to the API under the caller token; an invented identifier returns not_found with no other information; the airport resolver is invoked explicitly on the structured path and an ambiguous place returns needs_clarification with options, never a guess (issue 2800 fact 2); refine buffers or polls the server-sent stream and returns one payload; tool outputs are facts and short factual sentences with no Atlas voice (section 1). Quota: per account, denominated in cost per section 3 and issue 2800 fact 4 (read docs/LLM_ROUTING.md near line 46 for the cost model), hard block at the cap with quota_exhausted and the website link, never degrading to cached results; implement the counter against the API or Redis in a way that survives instance restarts and say which in the README. VERIFY: go test ./... in services/iris with the race flag, with a table test per tool covering the happy path, not_found on an invented id, needs_clarification, and quota_exhausted under a tight retry loop; paste real exit codes. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Open a draft PR with Refs 2800. Do not merge. | feat/ac-w2b-read-tools
2 | backend-critic | READ-ONLY REVIEW of the draft PR on branch feat/ac-w2b-read-tools. No commits, no push, no review, no labels, no edits; tmp writes only. Record head SHA at start and at report. Verify against the contract, not the PR text: run each of the six tools through a test harness with an invented identifier, an ambiguous place, an expired result session, a revoked token, and an assistant retry loop against the quota, and paste what came back; grep every argument and result struct for the forbidden fields of section 3.1; confirm web_url is present on every result shape; confirm refine returns a single payload; confirm no ranking or pricing code exists in the service. Write failing tests for anything that fails. Post ONE comment on issue 2340 whose first line reads CRITIC W2B READ TOOLS with SAFE-TO-MERGE or BLOCK-FIX and file and line citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/ac-w2b-read-tools
