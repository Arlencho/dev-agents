# Wave plan for dev-agents — 2026-07-29T16:16:28Z
# Generated by dispatch.sh

1 | devops | Fleet Desk polish OPS only. Branch feat/fleet-desk-polish-ops. (1) MUTE TEST RATECAP NOTIFY: scripts/notify.sh must skip macOS osascript (and stay stdout-only) when FLEET_NOTIFY_SILENT=1 or NOTIFY_SILENT=1; tests/run-failover-tests.sh must export that when exercising the ratecap notify path so make test never pops Provider Rate-Capped toasts (feat/x). Still assert notify path returns success / stdout ok. (2) GH NON-JSON HONESTY in scripts/experience_data.py gh_index: if gh repo view or pr/issue list exits 0 but body is not valid JSON or slug is not owner/repo, set status error (or bad_payload) with clear reason — never status ok with garbage in gh_enrichment.repo; keep never-fatal contract; add/extend a unit/fixture test with a fake gh on PATH that exits 0 and prints not json. (3) Register companies/dev-agents.md as active fleet product (github_repo Arlencho/dev-agents, repo path ., short charter: multi-vendor fleet orchestration tooling). Do not invent joins that invent companies. make test green. Open PR. No visual redesign. | feat/fleet-desk-polish-ops
1 | web-frontend | Fleet Desk polish N5 a11y only. Branch feat/fleet-desk-polish-n5. Fix chipmark accessible name glue: scripts/experience_build.py currently renders span.chipmark without a leading space so screen readers hear aegisplaceholder. Put a leading space inside the chipmark text (or equivalent whitespace that survives HTML) so accessible name is company status with a word break; keep visible CSS; do not redesign the desk. Add/adjust a test that fails if glued names return (or assert chipmark starts with space / word boundary). make test green. Open PR. No full restyle. | feat/fleet-desk-polish-n5
