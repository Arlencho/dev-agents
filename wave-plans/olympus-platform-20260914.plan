# Wave plan for olympus-platform — 2026-09-14T20:47:22Z
# Generated by dispatch.sh

1 | go-backend | On branch fix/2848-trip-packages (PR 2857), rebase onto origin/main first. Read the comment on issue 2340 whose first line reads CRITIC TRIP PACKAGES ROUND 1 and the decision comment whose first line reads SELF-RATIFICATION 2026-09-14 22:20. E1: the stay search in apps/api/internal/service/agent_orchestrator.go runs with the arrival date of the top-ranked flight instead of the departure date; buildTripPackages pairs only flights whose arrival date matches that stay search; the card check_in, nights, stored hotel price and the bookable rate all agree; make the critic fixture TestCritic2848_StoredStayPriceMatchesBookableRate green. E2: change the Package rules block in docs/prd/pages/06-conversation-results.md from PROPOSED to ACCEPTED citing that decision comment. F1: add check_in to TripPackage in api.yaml, run make generate and commit the generated files. F2: when the flight lands on the return date, nights 0 and the hotel total agree (no package is built, or the hotel total is zero with the reason). F3: a swapped-in flight takes its own match reason and relevancy score. This is a fix round: run only go test with the race flag on apps/api/internal/service, plus make check-api-spec; nothing else is run. VERIFY: those with real exit codes in the PR body under a Round 2 heading. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Commit and push. | fix/2848-trip-packages
2 | backend-critic | READ-ONLY REVIEW ROUND 2 of PR 2857. Tier B, round 2 of at most 3. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. Re-run only E1 (the bookable rate and the card agree on check-in, nights and price), F1, F2 and F3, plus one regression check that a same-day arrival keeps its check-in. Post ONE comment on issue 2340 whose first line reads CRITIC TRIP PACKAGES ROUND 2 with SAFE-TO-MERGE, BLOCK-FIX or BLOCK-ESCALATE and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | fix/2848-trip-packages
