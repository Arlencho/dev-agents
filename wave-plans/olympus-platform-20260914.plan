# Wave plan for olympus-platform — 2026-09-13T22:21:31Z
# Generated by dispatch.sh

1 | api-designer | On branch feat/ac-brief-and-entry (the open PR), in api.yaml: per the comment on issue 2340 whose first line reads ESCALATION ANSWERED 2026-09-13 23:50, document that POST /api/v1/parse and POST /result-sessions/{id}/refine called with an assistant-scope token never persist the raw text (no conversation row, no transcript turn, no prompt stored), returning the normalised fields only; delete any allowlist entry this touches; run make generate and commit every generated file. VERIFY: make check-api-spec output pasted. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Commit and push. | feat/ac-brief-and-entry
2 | go-backend | On branch feat/ac-brief-and-entry after wave 1. In apps/api: implement the no-persist behaviour for the assistant scope on the parse handler (no conversation row minted, no message recorded, fields returned) and on the refine handler (no prompt turn in the transcript, the marker and the merged fields only), enforced by token scope, with tests that assert no table, log line or error report receives the brief text. In services/iris: close every BLOCK-FIX item of the comment on issue 2340 whose first line reads CRITIC BRIEF AND ENTRY, each with the failing test the critic wrote turned green: a brief conflict with a stated field is needs_clarification, never silently dropped; a flexible-date brief never fires a search with an empty departure date; an empty budget object is not a budget; brief prose never lands on the record through hotel_area or any field; a party total is not that many adults; the learning record carries what section 10.1 names (handover_reached, intent, selection). Then close every finding of the comment whose first line reads SECURITY BRIEF AND ENTRY. VERIFY: go test ./... with the race flag in apps/api and services/iris, make check-api-spec exit 0, real exit codes in the PR body under a Round 2 heading. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Commit and push. | feat/ac-brief-and-entry
3 | backend-critic | READ-ONLY REVIEW ROUND 2 of the PR on branch feat/ac-brief-and-entry. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. Prove the brief text reaches no table, transcript, log or error report on either path under the assistant scope, and that the web scope is byte-identical to main; re-run all nine round 1 fixtures; re-run the accuracy minimum and manifest checks. Post ONE comment on issue 2340 whose first line reads CRITIC BRIEF AND ENTRY ROUND 2 with SAFE-TO-MERGE, BLOCK-FIX or BLOCK-ESCALATE and citations, and a last line TRIAL: producer rounds to SAFE so far <n>. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/ac-brief-and-entry
3 | security-reviewer | SECURITY REVIEW ROUND 2 of the PR on branch feat/ac-brief-and-entry. Read-only; tmp writes only. Record head SHA at start and at report. Confirm every round 1 finding is closed and that the no-persist path cannot be bypassed by a web-scope token presented by the connector. Post ONE comment on issue 2340 whose first line reads SECURITY BRIEF AND ENTRY ROUND 2 with a final SAFE or BLOCK line. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/ac-brief-and-entry
3 | api-critic | READ-ONLY REVIEW of the api.yaml diff on branch feat/ac-brief-and-entry. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. Confirm the no-persist behaviour is declared per scope on both operations, the generated client is committed and matches, make check-api-spec exits 0. Post ONE comment on issue 2340 whose first line reads API CRITIC BRIEF AND ENTRY with SAFE-TO-MERGE, BLOCK-FIX or BLOCK-ESCALATE and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/ac-brief-and-entry
