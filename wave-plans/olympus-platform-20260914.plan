# Wave plan for olympus-platform — 2026-09-14T06:48:46Z
# Generated by dispatch.sh

1 | go-backend | On branch feat/ac-pages-styling (PR 2849), in apps/api/internal/handler/assistant_oauth_pages.go: close the open finding of the comment on issue 2340 whose first line reads FRONTEND CRITIC ASSISTANT PAGES ROUND 2: BLOCK-ESCALATE, option (a): inline the LogoMark SVG from apps/web/components/auth/sign-in-modal-v2.tsx with the Olympus wordmark in the same font and size the web modal uses, above the h1 in the shared shell, on all seven page variants, as 02-auth-unified.md section 5 draws it; inline SVG only, no image request, no CSP change; the mark carries an accessible name Olympus and the SVG is aria-hidden. Add a test that every variant renders the mark and still carries the nonce CSP. No copy changes. VERIFY: go test ./... with the race flag in apps/api, real exit codes in the PR body under a Round 3 heading. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Commit and push. | feat/ac-pages-styling
2 | frontend-critic | READ-ONLY REVIEW ROUND 3 of the pages on PR 2849. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. Render the seven variants at 320, 400 and 1280, light and dark, as in round 2: confirm the mark and wordmark match the web sign-in modal, nothing scrolls sideways, the round 2 passed checks still hold. This is the last round: report only a finding that makes the pages not read as Olympus or breaks a passed check. Post ONE comment on issue 2340 whose first line reads FRONTEND CRITIC ASSISTANT PAGES ROUND 3 with SAFE-TO-MERGE, BLOCK-FIX or BLOCK-ESCALATE and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/ac-pages-styling
