# Wave plan for olympus-platform — 2026-09-14T17:18:08Z
# Generated by dispatch.sh

1 | go-backend | On branch feat/ac-refine-revision-guard (PR 2850), rebase onto origin/main first and resolve conflicts keeping both sides. Fix F3 of the comment on issue 2340 whose first line reads API CRITIC REFINE REVISION GUARD ROUND 2: in apps/api/internal/service/result_sessions.go the conversation write-through (maybeWriteTripStateThrough) runs before the guarded commit (persistRefineResult with UpdateFieldsAtRevision), so a refused commit leaves the refused fields in the linked conversation; move the write-through after a successful guarded commit so a refused commit persists nothing anywhere, exactly as api.yaml states. Add the failing test from that comment (TestAPICritic_CommitTimeConflictPersistsNothing) to the service tests and make it pass. This is a fix round: run go test with the race flag on apps/api/internal/service and apps/api/internal/handler only; nothing else is run. VERIFY: those with real exit codes in the PR body under a Round 3 heading. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Commit and push. | feat/ac-refine-revision-guard
2 | api-critic | READ-ONLY REVIEW ROUND 3 of PR 2850. Tier A. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. Re-run only F3 (a refused commit leaves nothing in the conversation or the record) plus one regression check that window (c) still answers revision_conflict and a successful refine still writes through. Post ONE comment on issue 2340 whose first line reads API CRITIC REFINE REVISION GUARD ROUND 3 with SAFE-TO-MERGE, BLOCK-FIX or BLOCK-ESCALATE and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/ac-refine-revision-guard
