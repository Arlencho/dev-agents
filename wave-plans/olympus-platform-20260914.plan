# Wave plan for olympus-platform — 2026-09-14T19:56:15Z
# Generated by dispatch.sh

1 | test-engineer | Branch test/money-gate-failure-cases from origin/main. Read docs/prd/pages/assistant-channel.md section 4 and sections 3.2, 3.3.4 and 7.1. Write an end-to-end test suite in services/iris (build tag e2e, not run by default) that drives Iris over its MCP interface against a local API with the in-memory stores: case 4 expired offer, get_option on an option whose expires_at has passed answers offer_expired with no price restated; case 6 revoked token, after the binding is disconnected every tool including the read tools answers token_revoked on the very next call; case 7 invented identifier, an option_id, trip_id and search_id never issued, and ones belonging to another traveller, all answer not_found with byte-identical bodies and no near match or newest-record fallback. Add docs/operations/assistant-hosts.md a section Money gate failure cases with the command to run the suite and the evidence each case must show, for the pilot record. VERIFY: go test with the e2e tag on the new suite, real exit code and the per-case output in the PR body. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Open a draft PR with Refs 2800. | test/money-gate-failure-cases
2 | backend-critic | READ-ONLY REVIEW of the PR on branch test/money-gate-failure-cases. Tier A, round 1 of at most 4. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. THE QUESTION THAT MATTERS: would these tests fail if the behaviour broke. Break each behaviour in a tmp copy (return a price on an expired offer, serve a read after revocation, answer a near match for an invented id) and confirm the matching case goes red. Post ONE comment on issue 2340 whose first line reads CRITIC MONEY GATE FAILURE CASES with SAFE-TO-MERGE, BLOCK-FIX or BLOCK-ESCALATE and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | test/money-gate-failure-cases
2 | security-reviewer | SECURITY REVIEW of the PR on branch test/money-gate-failure-cases. Read-only; tmp writes only. Record head SHA at start and at report. Check that case 6 covers every tool and the first call after revocation, and that case 7 proves no existence leak between travellers (status, body, timing within reason). Post ONE comment on issue 2340 whose first line reads SECURITY MONEY GATE FAILURE CASES with a final SAFE or BLOCK line. HOUSE STYLE: no long dash, no AI tool or vendor name. | test/money-gate-failure-cases
