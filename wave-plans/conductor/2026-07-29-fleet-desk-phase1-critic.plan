# Fleet Desk Phase 1 critic — verify schema v2 data contract before merge
# PR: https://github.com/Arlencho/dev-agents/pull/48
# Law: docs/proposals/experience-console-SYNTHESIS.md §5.1–§5.2 Phase 1, §7 Phase 1 line
# Producer: devops on feat/fleet-desk-phase1-data (7cb1bd6) — DO NOT re-implement UI restyle.
1 | test-engineer | CRITIC / VERIFY ONLY for PR #48 feat/fleet-desk-phase1-data (Fleet Desk Phase 1 data contract, schema v2). Checkout the PR branch. Adversarial verification: (1) schema v2 + docs/experience-data.md match shipped JSON; (2) skill git history honest — available:false vs empty history never conflated; git log depth capped; (3) PMI P3 gate exact — P2 outcome bar preserved; P3 only with specialized pack version/history or promoted learning; display_cap P3; Phase 0 "capped at P2" caption gone; mutation: fixture that should stay P2 does not promote; (4) critic pairing by branch — critic_pairs, reviewed_by, critic_rate method branch_pairing with documented fallback; (5) gh enrichment never fails the build when gh missing/errors; redaction holds; fixtures offline; (6) snapshot target if present has no secrets/home paths; (7) make experience / experience-data / desk / make test green; no visual redesign required. Output handoff.md VERDICT APPROVE or REVISE with executable failures only. Comment on PR #48 if gh works. Do not redesign UI. Do not re-implement. Branch feat/fleet-desk-phase1-data. | feat/fleet-desk-phase1-data
