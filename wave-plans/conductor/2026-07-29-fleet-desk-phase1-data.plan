# Fleet Desk Phase 1 — Wave 1 DATA (not UI restyle)
# Law: docs/proposals/experience-console-SYNTHESIS.md §5.1–§5.2 Phase 1 bullets, §7 Phase 1 line
# Phase 0 shipped: #46 data + #47 UI on main. This wave extends the JSON contract only.
# UI surfaces for new fields = later wave (thin bind, no redesign unless required for honesty).
1 | devops | Fleet Desk Phase 1 DATA CONTRACT only (no visual redesign). Read docs/proposals/experience-console-SYNTHESIS.md §5.1 critic_rate Phase 1, §5.2 P3, §7 Phase 1 line, and docs/experience-data.md. Extend scripts/experience_data.py + docs/experience-data.md + tests (fixtures as needed) so site/experience/data/index.json gains Phase 1 enrichment while remaining honest when data is missing: (1) SKILL GIT HISTORY — for each skills/*/SKILL.md (and candidates if cheap), record version history from git log (commit shas, dates, subjects; cap depth e.g. 20); feed PMI so P3 is achievable when version/promotion history exists — document exact P3 gate; Phase 0 caption must update when P3 is real; keep P2 outcome bar. (2) CRITIC PAIRING BY BRANCH — replace Phase 0 name-contains-critic rate with producer↔critic pairing on same branch when both trails exist; expose critic_pairs and role_stats fields; fallback documented when unpaired. (3) GH ENRICHMENT when `gh` authed — optional trail fields (pr_url, pr_state, issue links resolved) for branches that match open/merged PRs; NEVER fail the build if gh missing/errors; redaction still applies; no full issue bodies/secrets. (4) OPTIONAL SNAPSHOT — make target or flag that copies a redacted summary under docs/experience/snapshot/ (or document why deferred); if implemented, keep secrets out and note size. (5) Bump schema_version if required and document migration; make experience / experience-data / desk still work; make test green. Do NOT restyle HTML/CSS (no templates/experience redesign). Thin HTML pass-through of new fields OK only if tests need a smoke, else leave renderer for next wave. Branch feat/fleet-desk-phase1-data. Open PR when done. | feat/fleet-desk-phase1-data
