# Fleet Desk v2 Phase A REVISE — pin critic mutations M5 M7 M8 (tests only)
# PR #57 feat/fleet-desk-v2-phase-a
# Prior critic: REVISE test-only @ ece10fc — functional gates pass; 3 surviving mutations
1 | web-frontend | REVISE ONLY on PR #57 feat/fleet-desk-v2-phase-a after frontend-critic REVISE. Read handoff.md and PR #57 critic comment. Fix blocking M5 M7 M8 by adding tests only — do NOT change production mission/join logic unless a true bug appears (critic found none). M5 HIGHEST: pin unlinked trail with bare issue ref stays company unlinked and repo dash — inject issue_links on an unlinked fixture trail (e.g. 2-fixture-builder-gadget #77); assert path unlinked / repo — / #77; mutant inventing companies[0] must fail. Keep unlinked#N missions. M7: assert prefers-reduced-motion (or reduced-motion) present in templates/experience/site.css or built CSS. M8: assert status pills carry text not empty (e.g. done in pill body). Prove make test green and mentally RED under each mutation. Update handoff.md. Push same branch; leave PR #57 open. No Phase B. No redesign. | feat/fleet-desk-v2-phase-a
