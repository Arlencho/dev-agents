# Fleet Desk Wave 1 REVISE — producer fix critic F1 + F2 only
# PR #46 feat/fleet-desk-data-contract
# Critic: test-engineer VERDICT REVISE (handoff + PR review)
1 | devops | REVISE ONLY on branch feat/fleet-desk-data-contract (PR #46). Critic test-engineer VERDICT REVISE — fix blocking F1 and F2 only. F1: make experience-data must NOT wipe rendered HTML under site/experience/; experience_data.py clean must preserve non-data trees or only clean data/; docs/experience.md must match; add a test that after make experience then make experience-data, HTML index still exists and data/index.json refreshed. F2: "no agent transcript dumps" assertion is vacuous (fixtures log empty); plant fixture handoffs with realistic log paths and assert index.json never embeds agent-log transcript bodies or unredacted secret-like log path dumps per SYNTHESIS; prove mutant would fail. Do not redesign UI. Do not expand scope. Push same branch; comment on PR if gh works. Run full experience tests. | feat/fleet-desk-data-contract
