# Fleet Desk Wave 2 critic — verify UI craft before merge
# PR: https://github.com/Arlencho/dev-agents/pull/47
# Law: docs/proposals/experience-console-SYNTHESIS.md §4 routes/wireframes, §6 UX
# Producer shipped on feat/fleet-desk-ui (Kimi web-frontend) — DO NOT re-implement.
1 | frontend-critic | CRITIC / VERIFY ONLY for PR #47 feat/fleet-desk-ui (Fleet Desk Wave 2 UI craft). Checkout the PR branch. Adversarial verification against SYNTHESIS §4–§6 and the producer handoff: (1) Visual system + routes: dual-scope Global/company chips, Home, Work group-by-wave + flat toggle, Company, Trail detail, Roles+PMI disclosure, Skills, Learn, Conductor, About — hierarchy, density, teaching empty states, status text+color (never color alone), mono SHAs, prefers-color-scheme, a11y (skip link, focus-visible, aria-current, reduced-motion); (2) craft invariants: templates/experience/ CSS external (no inline style= / style blocks in HTML), experience_build.py renders from data/index.json only, make experience / experience-open / desk still work; (3) contract boundary: do NOT require data-pipeline redesign — join/PMI logic must remain Wave 1 law; flag any accidental contract break; (4) tests: make test green; HTML smokes actually prove craft (not vacuous). Output handoff.md with VERDICT APPROVE or REVISE and executable failures only (failing checks, file:line, missing route/affordance). If gh works, comment VERDICT on PR #47. Do not redesign UI. Do not re-implement. Branch feat/fleet-desk-ui. | feat/fleet-desk-ui
