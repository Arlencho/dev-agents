# Conductor: PR 2719 — record the co-founder sign-off and land the two comment corrections — 2026-08-19
#
# DISPATCH: ./scripts/dispatch.sh git@github.com:Arlencho/olympus-platform.git wave-plans/conductor/2026-08-19-2719-signoff-and-corrections.plan --auto --retries 2
#
# The critic verdict was SHIP, gated on three things: two comment corrections and an
# explicit co-founder accept of docs/prd/02-shells.md section 2.7.
#
# THE SIGN-OFF HAS NOW BEEN GIVEN. Arlen approved section 2.7 explicitly on 2026-08-19,
# in session, including the specific consequence that guests lose the help trigger below
# 720px with the four legal links moving inside the mobile menu. That is real approval
# from the co-founder, not an agent stamping its own work. Record it as such, with the
# channel named, and do not overstate it.
# ────────────────────────────────────────────────────────────────────────────

1 | web-frontend | Land three changes on branch fix/2690-mobile-nav, updating PR 2719 in place. This must be a COMMENT AND DOCS ONLY change: no component logic, no CSS, no test assertions, no locators. If your diff filtered of comment and blank lines touches anything but docs/prd/02-shells.md, you have exceeded scope, so stop and report. ONE, record the sign-off. Every block in docs/prd/02-shells.md currently stamped PROPOSED pending co-founder sign-off for issue 2690 becomes approved. Use this exact provenance and do not embellish it: Approved by Arlen, 2026-08-19, given in session and relayed by the orchestrator, covering section 2.7 and the section 1.1 and section 13 reversals, and explicitly covering the consequence that signed-out guests lose the header help trigger below 720px while the four legal links remain one interaction away inside the mobile menu, per the Article 5 contract in issue 1586. There is no first-party GitHub review from him on this PR, so say relayed rather than implying a written review. Grep the file for every PROPOSED marker tied to 2690 and convert them all; there are at least three. Do not touch PROPOSED markers belonging to other issues. TWO, correction from the critic section 1: three in-tree comments claim all FOUR primary destinations were unreachable below 720px on main. It is THREE of four. Plan was always reachable, via the always-visible Plan-a-trip CTA and the logo, both of which dispatch handleNav plan. Find those three comments and correct them to say Explore, Stays and Stories were unreachable and Plan was not. A reader who checks the stronger claim and finds it false will distrust the rest of the reasoning. THREE, correction from the critic section 3: apps/web/tests/e2e/critical/mobile-nav.spec.ts lines 14 to 18 claim the spec guards the invariant rather than the chrome, and that a future redesign keeping the invariant stays green. The critic disproved that by implementing a conformant alternative, letting the centre nav wrap instead of collapsing, which satisfies reachability BETTER at zero interactions and still turns the spec 5 of 5 red. Take the reword option: rewrite that comment to say honestly that the spec guards the SHIPPED chrome, the header dropdown, and that a different conformant design would need the spec updated. Do NOT rewrite the test to be chrome-agnostic in this change; that is a real test change and this task is comment-only. If the PR body repeats the same overstatement, fix it there too. Push to the existing branch. Do not open a new PR. Do not merge. Paste a diff filtered of comment lines to prove nothing executable changed outside the PRD file. | fix/2690-mobile-nav

2 | frontend-critic | READ-ONLY REVIEW of the wave 1 change on PR 2719, branch fix/2690-mobile-nav. You are not a producer: no commits, no push, no gh pr review, no gh pr merge, no labels, no test writing. Permitted writes are a /tmp worktree only. Record the PR head SHA at start and re-check at report. Verify four things and nothing else; a previous seat already reviewed the engineering and found it sound, so do not re-litigate the fix, the desktop byte-identity, the accessibility work or the site-header refusal. ONE, the sign-off stamps: every PROPOSED marker tied to issue 2690 in docs/prd/02-shells.md is now recorded as approved, the provenance names Arlen and the date 2026-08-19, it says the approval was relayed rather than implying a first-party GitHub review, and it explicitly names the guest help-trigger consequence. Flag any PROPOSED marker belonging to a DIFFERENT issue that was converted by mistake, which would be a silent over-reach. TWO, the three-of-four correction: no surviving comment in the tree or the PR body claims all four destinations were unreachable, and the corrected text correctly exempts Plan. THREE, the spec comment at mobile-nav.spec.ts lines 14 to 18 no longer claims chrome-independence and now says honestly that it guards the shipped header dropdown. FOUR, the executable diff outside docs/prd/02-shells.md is empty: no component logic, no CSS, no test assertion and no locator changed. Post ONE comment on issue 2340 whose first line reads CRITIC PR 2719 SIGNOFF, with a verdict of SAFE-TO-MERGE or BLOCK-FIX or BLOCK-ESCALATE and file:line citations. No vendor names, emails, booking ids or gcloud payloads. | fix/2690-mobile-nav
