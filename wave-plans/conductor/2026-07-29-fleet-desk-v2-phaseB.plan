# Fleet Desk v2 Phase B — live Ops Floor (serial waves, one clone)
# Law: docs/proposals/fleet-desk-v2-SYNTHESIS.md Phase B
# Phase A on main: hierarchy + /live/ shell. Wire real events. Never invent live state in index.json.
1 | devops | IMPLEMENT Fleet Desk v2 Phase B DATA PATH only. Read docs/proposals/fleet-desk-v2-SYNTHESIS.md Phase B. (1) Emit append-only JSONL events from scripts/dispatch.sh into logs/fleet-events/DISPATCH_ID.jsonl plus a latest pointer file; events for dispatch_start/end, wave_start/end, seat_dispatch, seat_exit, ratecap, failover, human_wait if known — no agent transcript bodies, filenames only, redaction-safe fields (task_id, agent, branch, provider, status, exit, ts). Opt-out FLEET_EVENTS=0. (2) Add scripts/desk_live.py (or desk-live.sh) stdlib local server: serve site/experience, tail events, write site/experience/data/live.json projection (schema live/1: waiting_on, seats, mode wave|conductor, staleness, last_event_ts); SSE /events optional; file poll support. (3) Makefile targets desk-live / experience-live documenting usage. (4) Tests: unit/fixture for event writer shape and live.json projection with synthetic events; never require live network. (5) Document event schema in docs/experience.md or experience-data.md. Do NOT redesign Almanac craft. Branch feat/fleet-desk-v2-phase-b-data. Open PR or push for wave 2. | feat/fleet-desk-v2-phase-b
2 | web-frontend | IMPLEMENT Fleet Desk v2 Phase B UI on same branch feat/fleet-desk-v2-phase-b after data path exists. Read SYNTHESIS Phase B and Phase A live shell in experience_build.py. Wire /live/ to real live.json when present: ambient LED live/stale/offline, waiting-on strip, pipeline counts, Wave parallel lanes (ghost queued if plan seats known), Conductor spine when mode=conductor, hierarchy context if mission/company known, event tail; degrade honestly if no live.json (keep Phase A teach shell). Tiny optional JS poller for live.json over http or file if allowed — no fake running agents. Home live teaser reflects live.json when present. Tests: smoke with synthetic live.json fixture; make test green. Update docs/experience.md operator path make desk-live. Push PR # for phase-b. No schema v2 join/PMI break. | feat/fleet-desk-v2-phase-b
