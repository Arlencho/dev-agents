# Fleet Desk Phase 1 REVISE — pin P3 gate with fixtures only (PR #48)
# Prior critic: REVISE @ 3f6669d — no production bug; 4 surviving mutants
# Law: do NOT change experience_data.py P3/join logic unless a true bug appears
1 | devops | REVISE ONLY on PR #48 feat/fleet-desk-phase1-data after test-engineer REVISE. Read handoff.md and PR #48 critic comment. Fix ALL blocking B1–B4 by adding fixtures and assertions only — do NOT rework production P3 logic in experience_data.py unless mutation proves a real bug (critic found none). B1 HIGHEST: pin "shared default packs excluded on purpose" — a mutant that treats default packs (e.g. git-ship) as specialized P3 evidence must FAIL tests; add fixture coverage so specialized→packs cannot silently inflate bands. B2: pin "P3 requires P2 outcome bar" — role with pack evidence but n_done/success below P2 must stay below P3; current fixture-builder alone is vacuous. B3: pin revisions>=2 — pack born at v2 with revisions=1 (or never revised) must NOT grant P3. B4: pin git log depth cap — assert history length equals cap when more commits exist, not merely echo constant. Prove: make test green; re-run the four mutants mentally or literally so each goes RED. Update handoff.md. Push same branch; leave PR #48 open. No UI restyle. | feat/fleet-desk-phase1-data
