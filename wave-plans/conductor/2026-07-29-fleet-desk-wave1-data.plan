# Fleet Desk Wave 1 — data contract only (NOT UI craft)
# Law: docs/proposals/experience-console-SYNTHESIS.md §3, §5, §7
# Existing scripts/experience_build.py is scaffold — extract/replace data layer.
# Wave 2 will own great UI. Do not redesign pages for craft this wave.
1 | devops | Fleet Desk Wave 1 DATA CONTRACT only. Read docs/proposals/experience-console-SYNTHESIS.md. Produce stable machine-readable projection for Fleet Desk: (1) emit site/experience/data/index.json (schema documented in docs/experience.md or docs/experience-data.md) containing companies, trails (task_id, agent/role, wave, status, branch, provenance, join_method, project_label, conductor flag, ts, SHAs, redacted handoff section summaries), skills, learnings, role_stats with PMI inputs/bands per freeze (P2 needs n_done>=5 AND success>=70%; Phase 0 cap P2). (2) Join rules: experience-joins.yaml if present, else github_repo/repo slug/name token, else unlinked — never invent companies. (3) Refactor scripts/experience_build.py (or split experience_data.py + thin HTML that only reads JSON) so make experience builds JSON first then HTML. (4) Expand tests/run-experience-tests.sh for joins, wave field, PMI cap, no secret-like tokens in JSON/HTML. (5) Keep make experience / experience-open / desk working. Do NOT spend this wave on great visual redesign (Wave 2 web-frontend). Branch feat/fleet-desk-data-contract. Open PR when done. | feat/fleet-desk-data-contract
