# Fleet Desk Wave 2 REVISE — producer fix for critic batch 1 (PR #47)
# PR: https://github.com/Arlencho/dev-agents/pull/47
# Critic: wave-plans/1/handoffs/1-frontend-critic-feat-fleet-desk-ui.md (VERDICT REVISE @ 0e77687)
# Law: SYNTHESIS §4–§6; Wave 1 join/PMI contract untouched
1 | web-frontend | REVISE ONLY on PR #47 feat/fleet-desk-ui after frontend-critic REVISE. Read handoff.md on the branch and/or the critic PR comment. Fix ALL of the following — blocking first, then non-blocking. Do NOT redesign the UI. Do NOT change join/PMI gates in experience_data.py. Branch feat/fleet-desk-ui. Push and leave PR #47 open. Update handoff.md with what you fixed. BLOCKING B1: In scripts/experience_build.py fix breadcrumb back-links on role/skill/learning detail pages — detail paths are singular (role|skill|learning/<id>/) but indexes are plural. Replace wrong ../index.html crumbs with ../../roles/index.html, ../../skills/index.html, ../../learnings/index.html (mirror company crumb depth pattern). B1b: Add a link-integrity crawl to tests/run-experience-tests.sh (fixture build) that walks every relative href in built HTML and asserts BROKEN: 0 — the critic repro crawler is the reference; a class=crumb grep alone is insufficient. NON-BLOCKING N1: templates/experience/site.css .chip.dim must not convey placeholder status by opacity alone — add a visible text marker on the chip (e.g. label "placeholder" or aria + visible suffix), keep title optional. N2: Tighten assert_absent_html / style checks to match [[:space:]]style= (catch single quotes and loose spacing), not only ' style="'. N3: Slim or extract helpers from home() in experience_build.py so it does not grow further (split sections into small helpers; keep behavior identical). N4 CONTRACT BOUNDARY — do NOT invent company joins or change experience_data.py join law. Optionally clarify empty-state / n=0 copy so operators see that unlinked trails are join-boundary behavior (teach make experience / join rules), not a broken UI. Prove: make test green including new link crawl; fixture crawl BROKEN: 0; real make experience build if possible. Open no new PR — amend branch under #47. | feat/fleet-desk-ui
