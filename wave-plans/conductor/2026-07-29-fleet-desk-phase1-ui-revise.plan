# Fleet Desk Phase 1 UI REVISE — fix critic C1+C2 (PR #49)
# Prior critic: REVISE @ edffe2e — C1 P3 evidence overclaim on sub-P3 roles; C2 unresolved issue links dropped
1 | web-frontend | REVISE ONLY on PR #49 feat/fleet-desk-phase1-ui after frontend-critic REVISE. Read handoff.md and PR #49 critic comment. Fix blocking C1 and C2 in scripts/experience_build.py only (no experience_data.py join/PMI change, no visual redesign). C1: Role PMI UI must not present proven_loop_evidence as if the P3 gate is satisfied when band is not P3. When evidence exists but role is below P2 outcome bar (fixture-runner is P1), label honestly that evidence is present but the gate is not met / still short of P2. Only band P3 gets the positive proven-loop-passed framing. C2: Links row must not drop unresolved issue_links when some refs resolve; show resolved entries and remaining raw issue_links not already covered (union both). Land the critic failing assertions into tests/run-experience-tests.sh with the fixes so they stay green. Prove make test green. Update handoff.md. Push same branch; leave PR #49 open. | feat/fleet-desk-phase1-ui
