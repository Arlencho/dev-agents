# Fleet Desk Phase 1 — Wave 2 thin UI bind (NO visual redesign)
# Law: SYNTHESIS Phase 1 fields already in schema v2 on main (#48). Surface them honestly.
# Do NOT restyle tokens/layout/colors — operator already noted Phase 0 look is temporary.
1 | web-frontend | Fleet Desk Phase 1 UI BIND only on main after #48. Branch feat/fleet-desk-phase1-ui. Read docs/experience-data.md (schema v2) and docs/proposals/experience-console-SYNTHESIS.md §5.1–§5.2. Consume data/index.json only — do NOT change experience_data.py join/PMI gates. Thin-bind Phase 1 fields into existing pages with minimal craft (reuse current CSS classes; add only tiny utility styles if required for honesty, not a redesign): (1) Roles/role detail — show P3 when band is P3; proven_loop_evidence in PMI details; retire any remaining "capped at P2" UI copy that contradicts display_cap P3; (2) Skills/skill detail — git_history / revisions / first-last commit when available, honest empty when history_available false; (3) Trail detail — pr_url/pr_state/issue_links_resolved when present; reviewed_by/reviews pairing links when present; (4) Home or Conductor/About — critic_pairs count + critic_rate_method/basis summary if data has them; gh_enrichment status line if useful. Keep make experience / desk working; tests: extend HTML smokes for new fields; make test green. Update docs/experience.md one short Phase 1 operator note. Open PR when done. No full restyle. | feat/fleet-desk-phase1-ui
