# Wave plan for veto — 2026-09-26T20:02:43Z
# Generated by dispatch.sh

1 | test-engineer | New branch test/maestro-device-suite from main. Add a repeatable device test suite under e2e/maestro/ for the Android app, run later by the lead on an emulator with the Solana Mobile fake wallet (com.solana.mobilewalletadapter.fakewallet) as the wallet. Flows (Maestro YAML, appId com.veto.app, selectors by visible text as the app renders it today; read the app screens to get exact labels): 01-onboarding (first-run "Next: you set one rule", "Next: a refusal is saved", "Next: you decide", then the Connect wallet button, note the step strip also contains the text CONNECT WALLET so tap the button by id or index, not plain text), 02-connect (fake wallet AUTHORIZE), 03-add-agent-paste (Paste your agent's address, Name your agent, Review the rule), 04-payment-rule (Rules, Write a rule, set payee and agent, lower limits with the typed amount inputs, press and hold approve as a long press of at least 3 seconds, fake wallet AUTHORIZE then SEND TRANSACTION TO CLUSTER), 05-trade-rule (Trade rule tab, agent, defaults, long press approve, fake wallet), 06-hold (Open Hold, Set up a vault, amount, 1 day, guardian second Seeker address, explicit safe address, long press, fake wallet), 07-decisions (pull to refresh, assert a refused row text). Parameterise addresses with env variables (AGENT, PAYEE, GUARDIAN, SAFE). Add e2e/maestro/README.md: emulator setup, installing the fake wallet, funding the fake wallet's devnet address, running each flow with maestro --device emulator-5554, and how to drive agent requests from the laptop with sdk/examples (trade-once, hacked-agent, pay-once) with keys/devnet-addresses.env loaded. Validate the YAML syntax offline only. No em dash anywhere. No AI vendor name in code, commits or PR text. | test/maestro-device-suite
1 | devops | New branch ci/anchor-cache-on-prs from main. The program CI job still fails on pull request branches with "anchor install failed 3 attempts; the binary never passed verification" (runs on PRs 337 and 343) although PR 335 added a cache and retry. Find out from the workflow and the action logs whether the cache is restored on PR branches (cache keys, scope: caches written on main are readable from PR branches, caches written on PR branches are not shared), whether the verification step runs against the cached binary and fails for a reason other than download (for example the attestation lookup itself failing or rate limited), and fix the root cause so a verified cached binary from main is reused on PRs and the provenance check is not repeated against the network when the binary's checksum matches a pinned value; keep verification mandatory (checksum pinned in the repo at minimum). Update the offline tests of scripts/ci-toolchain.sh and run make test-scripts until green. No em dash anywhere. No AI vendor name in code, commits or PR text. | ci/anchor-cache-on-prs
