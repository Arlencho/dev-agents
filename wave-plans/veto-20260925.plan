# Wave plan for veto — 2026-09-25T21:49:12Z
# Generated by dispatch.sh

1 | mobile | New branch fix/main-app-cancellation from main. The app CI job on main fails six tests added by PR 315: "a rule cancellation disappears after later navigation", "... after later connect", "... after later sign", "... after later disconnect", "... after later partial sign", "... after later account selection" (not ok 188 to 193). Reproduce locally in app/ with the node test runner on the file that holds them, find why they fail on current main (PR 315 was green on its own branch, so a change merged before it, likely the trade rule screens from PR 294 or the hold button label change from PR 314, changed a selector, label or shared hook such as app/lib/useWalletActionError.ts), and fix the root cause in the code or the stale test selector, whichever is actually wrong; do not delete or skip tests. Then run the whole app test command CI uses and the app typecheck until green. Open the PR as ready for review, not draft. Never run adb or touch any phone. No em dash anywhere. No AI vendor name in code, commits or PR text. | fix/main-app-cancellation
