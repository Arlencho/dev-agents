# Tile follow-ups rebase: PR 2836 conflicts with main after the connect guide landed. Issue 2827. 2026-09-13
#
# DISPATCH: ./scripts/dispatch.sh git@github.com:Arlencho/olympus-platform.git wave-plans/assistant-channel/2026-09-13-tile-followups-rebase.plan --detach --retries 1
# ────────────────────────────────────────────────────────────────────────────

1 | web-frontend | On branch fix/ac-tile-followups (PR 2836, issue 2827), in apps/web: rebase the branch onto origin/main and resolve the conflicts with the connect guide that landed in the Connected assistants tile (apps/web/components/concierge); keep both behaviours, the guide and the three follow-ups (disconnect error mapping, 44 px retry button, connected toast on a new device); force-push the rebased branch. Then confirm every follow-up still matches docs/prd/pages/assistant-channel.md section 7.1 after the rebase and that no string outside section 9 was introduced. VERIFY: web unit tests for the touched files, web lint and web typecheck through the Makefile targets, real commands and exit codes in the PR body under a Rebase heading. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. | fix/ac-tile-followups

2 | frontend-critic | READ-ONLY REVIEW of PR 2836 on branch fix/ac-tile-followups after the rebase. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. Check that the connect guide and the three follow-ups coexist on the tile with no regression of either, that every string on the tile is in section 9 of the assistant channel PRD, that the disconnect error mapping matches section 7.1, and that the retry target is 44 px at 400 wide. Write failing tests where the PR falls short and cite them. Post ONE comment on issue 2340 whose first line reads CRITIC TILE FOLLOW-UPS with SAFE-TO-MERGE or BLOCK-FIX and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | fix/ac-tile-followups
