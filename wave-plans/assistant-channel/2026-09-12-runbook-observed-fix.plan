# Fix wave for docs/settlement-check-observed after critic BLOCK-FIX. 2026-09-12
#
# DISPATCH: ./scripts/dispatch.sh git@github.com:Arlencho/olympus-platform.git wave-plans/assistant-channel/2026-09-12-runbook-observed-fix.plan --auto --retries 2
#
# DOCS ONLY. Critic comment: issue 2340, comment id 5645028856. Three blocks.
# Block 1 and 2 are fixed here. Block 3 (log evidence) needs a live gcloud
# session and is handled by the orchestrator on PR 2812.
# ────────────────────────────────────────────────────────────────────────────

1 | devops | Fix branch docs/settlement-check-observed (PR 2812) per the critic comment on issue 2340 with id 5645028856. READ that comment first, then docs/operations/deployment.md section Establishing what the RUNNING production build does. THE FACT THAT DRIVES THIS: the CTA sub-line Confirm booking with the words no balance was retired from BOTH modal variants by commit ea4776b9 (PR 2778), which shipped to production web on 2026-09-04, so on 2026-09-11 the sub-line did not exist on either build and the observation recorded at the line beginning Observed 2026-09-11 is false. Verify that yourself with git log and the comment in apps/web/components/checkout/payment-confirm-modal.tsx near line 816 before editing. WHAT TO CHANGE. One, delete the sub-line observation from the Observed 2026-09-11 line and keep only what was actually observed: the modal-open POST to /api/v1/bookings carrying settlement card and answered 201, which is the network row of the step-4 table. Two, the step-4 table row marked READ THIS ONE FIRST still names the CTA sub-line as the primary discriminator; that row is stale on main since PR 2778 because both variants now render Confirm booking with no sub-line. Promote the modal-open network POST row to be the primary discriminator (move the READ THIS ONE FIRST marker to it) and rewrite the CTA sub-line row to state that since PR 2778 the CTA reads Confirm booking on both variants and no longer discriminates. Three, the paragraph beginning Why the CTA sub-line and not the card field, and any other sentence in the section that relies on the sub-line as the discriminator (the critic cites lines 150, 156 and 160), must be corrected to the network POST, with the same reasoning: the POST at open is a pure function of the settlement flag baked into the bundle and fires before any card leg mounts, so a phase-1 failure cannot mask it. Keep the existing warning that the card field is NOT a discriminator. Four, do not change the dated record near line 118 in substance; the orchestrator is attaching log evidence for it separately on PR 2812. Do not touch anything outside the section and do not change the step-6 remedy text. VERIFY: read the whole section once more end to end and confirm no sentence still says the sub-line discriminates; make check-prd-index if in scope; git diff against main must show only docs/operations/deployment.md. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Push to the same branch so PR 2812 updates. Do not merge. | docs/settlement-check-observed

2 | backend-critic | READ-ONLY RE-REVIEW of branch docs/settlement-check-observed (PR 2812) after the fix wave. No commits, no push, no review, no labels, no edits; tmp writes only. Record head SHA at start and at report. Your prior comment on issue 2340 has id 5645028856 and raised three blocks; grade each. BLOCK 1: confirm the false sub-line observation is gone and nothing in the section now claims the sub-line was seen or that it discriminates; confirm this against commit ea4776b9 and payment-confirm-modal.tsx near line 816. BLOCK 2: confirm the primary discriminator is now the modal-open POST row, the stale rows the comment cited (150, 156, 160 at the old numbering) are corrected, and the reasoning paragraph is consistent with it. BLOCK 3: look at the comments on PR 2812 for one whose first line reads LOG EVIDENCE 2026-09-11 and check every fact in the dated record near line 118 against those pasted lines. If no such comment exists yet, do not fail the wave on it and do not attempt gcloud; report BLOCK 3 as PENDING LOG PASTE and grade the other two. Also re-confirm nothing outside the section changed, step-6 remedy byte-identical to main, no long dash, no vendor name, no trailer. Post ONE comment on issue 2340 whose first line reads CRITIC SETTLEMENT CHECK OBSERVED ROUND 2, with SAFE-TO-MERGE, SAFE-TO-MERGE PENDING LOG PASTE, or BLOCK-FIX, and line citations. | docs/settlement-check-observed
