# Handover landing fix round: one finding from CRITIC HANDOVER LANDING on issue 2340. Issue 2842. 2026-09-13
#
# DISPATCH: ./scripts/dispatch.sh git@github.com:Arlencho/olympus-platform.git wave-plans/assistant-channel/2026-09-13-handover-landing-fix1.plan --detach --retries 1
# ────────────────────────────────────────────────────────────────────────────

1 | web-frontend | On branch feat/ac-handover-landing (PR 2844), in apps/web, fix B1 of the comment on issue 2340 whose first line starts with CRITIC HANDOVER LANDING: after the budget skip in lib/chat-step-machine/project-snapshot.ts the thread message ids collide, the confirm ask is appended under an already incremented id and the first in-page message after landing duplicates it; make ids unique after the skip and add the unit test the critic wrote, plus keep every existing test green. VERIFY: web unit tests for the touched files, web lint and web typecheck through the Makefile targets, real commands and exit codes in the PR body under a Round 2 heading. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Commit and push. | feat/ac-handover-landing

2 | frontend-critic | READ-ONLY REVIEW ROUND 2 of PR 2844 on branch feat/ac-handover-landing. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. Re-run the round 1 red test for B1 and confirm it is green; confirm the real record fixture still lands on the picker with the chosen offer selected and no budget ask; confirm ids in the thread are unique after landing and after one typed message. Post ONE comment on issue 2340 whose first line reads CRITIC HANDOVER LANDING ROUND 2 with SAFE-TO-MERGE or BLOCK-FIX and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/ac-handover-landing
