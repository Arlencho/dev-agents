# Brief and entry round 3: three Iris findings from CRITIC BRIEF AND ENTRY ROUND 2 and two api.yaml sentences from API CRITIC BRIEF AND ENTRY ROUND 1. The no-persist rule and the web scope already hold. PR 2851. 2026-09-14
#
# DISPATCH: ./scripts/dispatch.sh git@github.com:Arlencho/olympus-platform.git wave-plans/assistant-channel/2026-09-14-brief-and-entry-fix2.plan --detach --retries 1
# ────────────────────────────────────────────────────────────────────────────

1 | api-designer | On branch feat/ac-brief-and-entry (PR 2851), in api.yaml: apply F1 and F2 of the comment on issue 2340 whose first line reads API CRITIC BRIEF AND ENTRY ROUND 1, following its fix lines: on an assistant-scope token the refine prompt is accepted for body validation but not applied and not ranked on, and the caller sends normalised fields as parsed_field_deltas; the parse description gains the sentence that on the assistant scope the call merges against no prior trip-state and no account defaults. Run make generate and commit the generated files. VERIFY: make check-api-spec exit 0 only. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Commit and push. | feat/ac-brief-and-entry

2 | go-backend | On branch feat/ac-brief-and-entry after wave 1, in services/iris: fix F1, F2 and F3 of the comment on issue 2340 whose first line reads CRITIC BRIEF AND ENTRY ROUND 2: every conflict field gives the host a question it can answer (a clarification the host can reply to for return_date, cabin_class, travellers and stay_required); a conflict raised on refine is answered as a refine clarification and the refine is not lost; a hotel_stars of 0 is not a class band. This is a fix round: run only the Iris test packages that cover brief.go and search.go plus the failing tests from the critic comment; no full-suite run, no mutation run. VERIFY: go test with the race flag on those packages, real exit codes in the PR body under a Round 3 heading. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Commit and push. | feat/ac-brief-and-entry

3 | backend-critic | READ-ONLY REVIEW ROUND 3 of PR 2851. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. Re-run only the fixtures for F1, F2 and F3 plus one regression check that the brief still reaches no table on the assistant scope. Post ONE comment on issue 2340 whose first line reads CRITIC BRIEF AND ENTRY ROUND 3 with SAFE-TO-MERGE, BLOCK-FIX or BLOCK-ESCALATE and citations, and a last line TRIAL: producer rounds to SAFE so far <n>. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/ac-brief-and-entry

3 | api-critic | READ-ONLY REVIEW ROUND 2 of the api.yaml diff on PR 2851. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. Confirm F1 and F2 are closed, the generated client matches, make check-api-spec exits 0. Post ONE comment on issue 2340 whose first line reads API CRITIC BRIEF AND ENTRY ROUND 2 with SAFE-TO-MERGE, BLOCK-FIX or BLOCK-ESCALATE and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/ac-brief-and-entry
