# Brief and entry: rebase PR 2851 onto main after the refine guard merged, then merge on the round 4 decision (issue 2340, ESCALATION ANSWERED 2026-09-14 20:15). The quota test clock is already fixed on main. 2026-09-14
#
# DISPATCH: ./scripts/dispatch.sh git@github.com:Arlencho/olympus-platform.git wave-plans/assistant-channel/2026-09-14-brief-and-entry-rebase.plan --detach --retries 1
# FIX-ROUND: 2026-09-14-brief-and-entry-fix3.plan
# TIER: A
# ────────────────────────────────────────────────────────────────────────────

1 | go-backend | On branch feat/ac-brief-and-entry (PR 2851): rebase onto origin/main. Main gained the refine revision guard (PR 2850) in apps/api/internal/service/result_sessions.go, apps/api/internal/handler/result_sessions.go, services/iris/internal/tools/search.go, api.yaml and the generated client since the branch was cut; both PRs change the refine path, so resolve every conflict keeping both behaviours: the assistant scope never persists the raw brief or prompt, and a refine carries expected_revision and a refused commit persists nothing. Run make generate after resolving api.yaml and commit the generated files. Force-push. This is a rebase round: run only go test with the race flag on apps/api/internal/service, apps/api/internal/handler and services/iris/internal/tools, plus make check-api-spec; nothing else is run. VERIFY: those commands with real exit codes in the PR body under a Rebase heading, and name each conflicted file with how it was resolved. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. | feat/ac-brief-and-entry

2 | backend-critic | READ-ONLY REVIEW of the rebase of PR 2851. Tier A. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. Check only the conflict resolutions: the no-persist tests and the refine revision tests both pass on this head, and a refused refine on the assistant scope still persists nothing. Post ONE comment on issue 2340 whose first line reads CRITIC BRIEF AND ENTRY REBASE with SAFE-TO-MERGE, BLOCK-FIX or BLOCK-ESCALATE and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/ac-brief-and-entry
