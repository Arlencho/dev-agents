# Round two for fix/ac-pilot-search-results (PR 2838) after backend-critic BLOCK-FIX. 2026-09-13
#
# DISPATCH: ./scripts/dispatch.sh git@github.com:Arlencho/olympus-platform.git wave-plans/assistant-channel/2026-09-13-pilot-search-results-fix.plan --auto --retries 1
# Root cause confirmed by the producer: the API emits the terminal done event before it
# commits parsed_fields and the snapshot, so a read straight after done sees the old record.
# The producer added a read-back loop; the critic showed it settles on any writer.
# ────────────────────────────────────────────────────────────────────────────

1 | go-backend | Fix PR 2838 on branch fix/ac-pilot-search-results per the comment on issue 2340 whose first line reads CRITIC PILOT SEARCH RESULTS. READ IT IN FULL FIRST and add the critic RED test unchanged. ONE (block 1, the real fix belongs in the API): in apps/api, the terminal done event of a search or refine stream must be emitted only after parsed_fields and the snapshot are committed, so any reader that sees done sees the committed record; find the emit in service/agent_stream.go and the commit in the runner and reorder, with a test that reads the record inside the done handler and finds the snapshot. TWO (block 1, defence in Iris): keep the read-back but settle only on proof that this call landed: on refine gate on parsed_fields.assistant_revision equal to the value Iris wrote, on search gate on a snapshot being present, never on a bare revision bump from any writer; the critic test must pass. THREE (block 2): remove the date_flexibility_days row from docs/prd/pages/assistant-channel.md section 3.3.1 and the refusal row from services/iris/README.md, both in this PR (the PRD edit is a behaviour correction under the standing delegation, note it in the PR body for the log on issue 2340). FOUR (non-blocking cite): map json.UnmarshalTypeError on id or method to wrong_type rather than not_an_object. VERIFY: go test ./... with the race flag in services/iris and apps/api, make check-prd-index, real exit codes in the PR body. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Push to the same branch, keep the PR a draft. | fix/ac-pilot-search-results

2 | backend-critic | READ-ONLY RE-REVIEW of PR 2838 on branch fix/ac-pilot-search-results after the fix wave. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. Grade block 1 and block 2 CLEARED or STILL OPEN: run your race test; then race the other way against the API change (a reader inside the done handler must see the snapshot; a website PATCH between done and the read must not be mistaken for the refine landing); confirm the PRD and README rows are gone and the manifest, the args and the docs agree. Re-run the production-shaped fixture from round one. Post ONE comment on issue 2340 whose first line reads CRITIC PILOT SEARCH RESULTS ROUND 2 with SAFE-TO-MERGE or BLOCK-FIX. HOUSE STYLE: no long dash, no AI tool or vendor name. | fix/ac-pilot-search-results
