# Connected assistants tile follow-ups. Issue 2827. 2026-09-13
#
# DISPATCH: ./scripts/dispatch.sh git@github.com:Arlencho/olympus-platform.git wave-plans/assistant-channel/2026-09-13-tile-followups.plan --auto --retries 1
# From the round-two critic on PR 2826: disconnect error mapping, the 44 px retry
# button, and the connected toast firing on a device that never saw the binding.
# ────────────────────────────────────────────────────────────────────────────

1 | web-frontend | Implement issue 2827 on branch fix/ac-tile-followups, apps/web/components/concierge only. READ FIRST the issue, docs/prd/01-conventions.md rows near 253 to 256 (the four failure classes and their ratified copy), docs/prd/pages/09-bookings-detail.md near line 418 (the four-way switch pattern), docs/prd/pages/assistant-channel.md section 7.1 (when the S8 toast fires), and the components named in the issue. ONE: the disconnect dialog maps each failure class to its ratified copy (4xx generic, 5xx, offline or thrown fetch, timeout) with the switch pattern the bookings page uses; add a test per class. TWO: the retry control gets the min-h-11 md:min-h-0 lift the same file already applies to its neighbour; fix the same gap on the Telegram retry in concierge-section.tsx since it is the same component family. THREE: the connected toast fires only when the traveller lands back after consent, never on a device that has simply not seen the binding before: gate it on consented_at being within the last few minutes (the cheapest option in the issue), keep the per-device seen set as the second condition, and rewrite the test that currently pins the false positive so it asserts the fresh-device case shows no toast. VERIFY: npm run lint, typecheck and the unit tests for apps/web with real exit codes; the voice lint on changed files; paste exit codes in the PR body. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Open a draft PR with Closes 2827. | fix/ac-tile-followups

2 | frontend-critic | READ-ONLY REVIEW of the PR on branch fix/ac-tile-followups. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. Check: each of the four failure classes renders its ratified string and nothing else (drive the dialog with a mocked 500, a thrown fetch, a timeout and a 409); both retry controls measure at least 44 px tall at 375 wide; a fresh device with an old binding shows no toast and a return from consent within the window does; no new string outside the catalog. Run the gates and paste exit codes. Post ONE comment on issue 2340 whose first line reads CRITIC TILE FOLLOWUPS with SAFE-TO-MERGE or BLOCK-FIX and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | fix/ac-tile-followups
