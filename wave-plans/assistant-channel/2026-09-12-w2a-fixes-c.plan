# W2-A round four: one block (B4), one fix (F8), two nits, S11 status. PR 2829. 2026-09-12
#
# DISPATCH: ./scripts/dispatch.sh git@github.com:Arlencho/olympus-platform.git wave-plans/assistant-channel/2026-09-12-w2a-fixes-c.plan --auto --retries 1
# Round three: critic BLOCK-FIX on B4 only, security SAFE. Everything else cleared.
# ────────────────────────────────────────────────────────────────────────────

1 | devops | Close round three on PR 2829, branch feat/ac-w2a-iris-scaffold, per the comment on issue 2340 whose first line reads CRITIC W2A IRIS SCAFFOLD ROUND 3. READ IT IN FULL FIRST. ONE (B4): add the ASSISTANT_DENIED_CLIENTS row to docs/operations/env-vars-api.md under ASSISTANT_RESOURCE_URI exactly as the critic describes (plain env var, default unset, comma-separated pilot-host client ids, a listed host gets 403 on every assistant path and its refresh grants are refused, IRIS_DENIED_HOSTS is the same list at the front door), and correct the three sentences the same commit made false that the critic names; run python3 scripts/check-env-vars.py and paste exit 0. TWO (F8, N9): the edge now refuses to boot in production without IRIS_PUBLIC_URL, so the first deploy can no longer succeed before the bootstrap. Fix it in .github/workflows/deploy-iris.yml by passing the full set at deploy time (IRIS_PUBLIC_URL, ASSISTANT_RESOURCE_URI, OLYMPUS_API_URL, OLYMPUS_WEB_URL) from repository variables, noting in docs/operations/env-vars-iris.md that the Cloud Run URL is deterministic from the project number and region so IRIS_PUBLIC_URL can be set before the first deploy, and rewrite the first-deploy and bootstrap sentences so they describe the order that now works. THREE (N8): the kill switch cuts every message, not only tool calls, and the alert is once per host per hour; fix the four places the critic lists. FOUR: in docs/prd/pages/assistant-channel.md section 9, row S11 moves from PROPOSED to ACCEPTED, co-founder sign-off 2026-09-12 (recorded on issue 2340). VERIFY: scripts/check-env-vars.py exit 0, make check-prd-index exit 0, make check-ci-wiring exit 0, actionlint or the workflow lint the repo uses on deploy-iris.yml if available; paste real exit codes. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Push to the same branch. | feat/ac-w2a-iris-scaffold

2 | backend-critic | READ-ONLY CHECK of PR 2829 on branch feat/ac-w2a-iris-scaffold after round four. No commits, no push, no review, no labels, no edits; tmp writes only. Record head SHA at start and at report. Grade B4, F8, N8 and N9 from your round-three comment CLEARED or STILL OPEN with proof: run scripts/check-env-vars.py and paste the exit; read deploy-iris.yml and the runbook and state whether a first deploy on a fresh project can now boot the edge before any manual bootstrap; grep the four kill-switch sentences; confirm S11 reads ACCEPTED with the date. Confirm the Go tree is unchanged since c4dcd334 (git diff on services and apps must be empty) so your round-three proofs still hold. Post ONE comment on issue 2340 whose first line reads CRITIC W2A IRIS SCAFFOLD ROUND 4 with SAFE-TO-MERGE or BLOCK-FIX. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/ac-w2a-iris-scaffold
