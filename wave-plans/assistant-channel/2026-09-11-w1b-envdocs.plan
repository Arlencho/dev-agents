# Assistant Channel W1-B env docs: the one finding the fix wave skipped. PR 2804.
#
# DISPATCH: ./scripts/dispatch.sh git@github.com:Arlencho/olympus-platform.git wave-plans/assistant-channel/2026-09-11-w1b-envdocs.plan --auto --retries 2
#
# Same branch as PR 2804. Two files, documentation only. The Env Var
# Completeness CI job is red on the branch and this closes it.
# ────────────────────────────────────────────────────────────────────────────

1 | devops | One narrow task on branch feat/ac-w1b-scope, PR 2804: document the two env vars the branch added to apps/api/internal/config/config.go and nothing else. The Env Var Completeness CI job, which runs scripts/check-env-vars.py bidirectionally between config.go and docs/operations/env-vars-api.md, is RED on this branch because ASSISTANT_SERVICE_TOKEN and CORS_ALLOWED_ORIGINS exist in config.go and not in the doc. READ FIRST: scripts/check-env-vars.py to see exactly what it requires; docs/operations/env-vars-api.md around line 96 where HERMES_SERVICE_TOKEN is documented, whose row format you must match exactly; .env.example around line 323 where HERMES_SERVICE_TOKEN appears; and config.go around lines 242 and 895 and 1655 for what each new var does, so the doc says something true. Add both vars to both files. For ASSISTANT_SERVICE_TOKEN say it is the assistant channel service secret, distinct from the Hermes one on purpose because a leaked service token is user impersonation, and that an operator creates the Secret Manager entry. For CORS_ALLOWED_ORIGINS say it is a comma separated list, that the default is the two localhost origins and is unchanged, and that a wildcard is refused at load. Touch NOTHING under apps/api/internal; the Go side is done and verified. VERIFY by running python3 scripts/check-env-vars.py and pasting its real exit code captured directly rather than read after a pipe; it must be 0. Confirm git diff names exactly two files. Push to the SAME branch; do not open a new PR; do not merge. Comment on PR 2804 with the exit code. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. | feat/ac-w1b-scope

2 | backend-critic | READ-ONLY VERIFICATION on branch feat/ac-w1b-scope, PR 2804, of one narrow change: two env vars documented. No commits, no push, no review, no labels, no edits. Record the PR head SHA at start and re-check at report; if it moved report BLOCKED with both SHAs. FIRST, diff against 72f96411 and confirm exactly two files changed, .env.example and docs/operations/env-vars-api.md, and nothing under apps/api. Anything else is scope creep on a branch you already verified. SECOND, run python3 scripts/check-env-vars.py yourself on the branch and confirm exit 0; this is the gate that was red. THIRD, read both new doc rows and confirm each says something true about the variable by checking it against config.go: the token is a distinct secret from the Hermes one, the origins list defaults to two localhost entries and refuses a wildcard. A row that documents the var but misdescribes it is a BLOCK. FOURTH, confirm the PR checks are now green apart from anything unrelated to this branch, and name any that are not. FIFTH, the W0 citation dependency: check whether docs/prd/pages/assistant-channel.md is on main; if yes verify the citations, if no record PENDING PR 2802 and do not block. SIXTH, no long dash, no vendor name. Post ONE comment on issue 2340 whose first line reads CRITIC ASSISTANT CHANNEL W1B ENVDOCS VERIFIED, with SAFE-TO-MERGE or BLOCK-FIX and file and line citations. No emails, booking ids or gcloud payloads. | feat/ac-w1b-scope
