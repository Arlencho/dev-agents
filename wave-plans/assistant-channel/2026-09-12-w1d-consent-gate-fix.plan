# Fix wave for fix/ac-consent-gate (PR 2828) after backend-critic BLOCK-FIX. 2026-09-12
#
# DISPATCH: ./scripts/dispatch.sh git@github.com:Arlencho/olympus-platform.git wave-plans/assistant-channel/2026-09-12-w1d-consent-gate-fix.plan --auto --retries 1
# ────────────────────────────────────────────────────────────────────────────

1 | go-backend | Fix PR 2828 on branch fix/ac-consent-gate per the critic comment on issue 2340 whose first line reads CRITIC CONSENT GATE BLOCK-FIX, finding B1. READ IT IN FULL FIRST, including the RED test it pasted. The root router sets Content-Security-Policy default-src none on every response (apps/api/internal/handler/routes.go near lines 228 to 238, value in middleware/security_headers.go near line 67), so the inline onchange handler that enables the Allow button never runs in a real browser and the button stays disabled forever. Fix it the way security_headers.go near lines 31 to 34 already prescribes: a route-scoped policy for the assistant HTML pages only, never at the root, that permits exactly one inline script by a per-response nonce (script-src with the nonce, everything else still none, frame-ancestors none), and move the enabling logic into that single nonced script; the server-side refusal of a consent POST without the consent field stays exactly as it is, so the gate holds with scripting off. Add the critic RED test to the suite, driven through the root middleware chain rather than the handler directly, asserting the response carries a script-src nonce that matches the script tag and that the root policy is unchanged on a non-HTML route. VERIFY: go test ./... with the race flag in apps/api, real exit codes captured directly and pasted into the PR body. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Push to the same branch, keep the PR a draft. | fix/ac-consent-gate

2 | devops | On branch fix/ac-consent-gate fix finding B2 of the same critic comment: in docs/prd/pages/assistant-channel.md the section 9 Where column for rows S4 and S5 still reads section 7.1; point both rows at the new consent-screen subsection of section 7 so the catalog and the subsection agree, and touch nothing else. VERIFY: make check-prd-index, exit code pasted. HOUSE STYLE: no long dash, no AI tool or vendor name, no Co-Authored-By trailer. Commit and push. | fix/ac-consent-gate

3 | backend-critic | READ-ONLY RE-REVIEW of PR 2828 on branch fix/ac-consent-gate after the fix waves. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. Grade B1 and B2 from your prior comment CLEARED or STILL OPEN with proof: fetch the consent page through the real router in a test and show the policy header on it, the nonce on the script tag, and the unchanged root policy on a JSON route; confirm the POST without the consent field is still refused; confirm the section 9 Where cells now name the consent subsection. Run go test ./... with the race flag and paste the exit code. Post ONE comment on issue 2340 whose first line reads CRITIC CONSENT GATE ROUND 2 with SAFE-TO-MERGE or BLOCK-FIX. HOUSE STYLE: no long dash, no AI tool or vendor name. | fix/ac-consent-gate
