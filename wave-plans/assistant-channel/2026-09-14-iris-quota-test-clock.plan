# Iris quota test time bomb: the memory store reads the wall clock while the meter is pinned to 2026-09-12, so since 2026-09-13 the cap never binds in tests and Iris CI is red on every PR. Escalation R4-1 on PR 2851, answered 2026-09-14 20:15. Test-only; production uses the Redis store. 2026-09-14
#
# DISPATCH: ./scripts/dispatch.sh git@github.com:Arlencho/olympus-platform.git wave-plans/assistant-channel/2026-09-14-iris-quota-test-clock.plan --detach --retries 1
# TIER: B
# ────────────────────────────────────────────────────────────────────────────

1 | go-backend | Branch fix/iris-quota-test-clock from origin/main. Read finding R4-1 of the comment on issue 2340 whose first line reads CRITIC BRIEF AND ENTRY ROUND 4. In services/iris/internal/quota: the tests pin the Meter clock but NewMemoryStore leaves MemoryStore.Now nil, so the store compares the wall clock against an expiry taken from the pinned day. Make the store take its clock from the same source as the meter in tests (set MemoryStore.Now to the pinned clock in every test that pins the meter, or have the meter pass its clock to the store), so no quota test depends on the calendar date; add one test that fails if the store and meter clocks diverge. Do not change production behaviour. Run only go test with the race flag on services/iris/internal/quota; nothing else is run. VERIFY: that command with its real exit code, run once with the system date as is, in the PR body. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Open a PR titled fix(iris): quota tests share one clock, Refs 2851. | fix/iris-quota-test-clock

2 | backend-critic | READ-ONLY REVIEW of the PR on branch fix/iris-quota-test-clock. Tier B, round 1 of at most 3. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. Confirm no quota test reads the wall clock (run the package with a faked date well in the future and one in the past), production code paths are unchanged, and the cap binds in the tests again. Post ONE comment on issue 2340 whose first line reads CRITIC IRIS QUOTA TEST CLOCK with SAFE-TO-MERGE, BLOCK-FIX or BLOCK-ESCALATE and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | fix/iris-quota-test-clock
