# Refine revision guard round 2: window (c) reported as an outage, and two spec gaps. 2026-09-13
#
# DISPATCH: ./scripts/dispatch.sh git@github.com:Arlencho/olympus-platform.git wave-plans/assistant-channel/2026-09-13-refine-revision-guard-fix1.plan --detach --retries 1
# ────────────────────────────────────────────────────────────────────────────

1 | api-designer | On branch feat/ac-refine-revision-guard (the open PR), in api.yaml: fix F1 and F2 of the comment on issue 2340 whose first line reads API CRITIC REFINE REVISION GUARD: the field is documented as required for the assistant scope and optional for the web scope, matching the runtime; a commit-time revision mismatch has a declared wire outcome (the 409 with the current record, or a named terminal event on the stream, whichever the runtime emits after wave 2), never the infrastructure error frame. Run make generate and commit every generated file. VERIFY: make check-api-spec exit 0. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Commit and push. | feat/ac-refine-revision-guard

2 | go-backend | On branch feat/ac-refine-revision-guard after wave 1: fix F1 of the comment on issue 2340 whose first line reads CRITIC REFINE REVISION GUARD: a website write during the search (window c) is a revision conflict, answered as the declared outcome with the current record, never as refine_failed or search service unavailable, never at error level, never paging; make the three failing tests the critic wrote green, on the API handler, the Iris client and the Iris tool. VERIFY: go test ./... with the race flag in apps/api and services/iris, real exit codes in the PR body under a Round 2 heading. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Commit and push. | feat/ac-refine-revision-guard

3 | backend-critic | READ-ONLY REVIEW ROUND 2 of the PR on branch feat/ac-refine-revision-guard. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. Re-run the three windows on this head; confirm window (c) is a conflict outcome with the current record and no error-level log; confirm the web scope is unchanged. Post ONE comment on issue 2340 whose first line reads CRITIC REFINE REVISION GUARD ROUND 2 with SAFE-TO-MERGE, BLOCK-FIX or BLOCK-ESCALATE and citations, and a last line TRIAL: producer rounds to SAFE so far <n>. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/ac-refine-revision-guard

3 | api-critic | READ-ONLY REVIEW ROUND 2 of the api.yaml diff on branch feat/ac-refine-revision-guard. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. Confirm F1 and F2 are closed and the runtime matches the spec. Post ONE comment on issue 2340 whose first line reads API CRITIC REFINE REVISION GUARD ROUND 2 with SAFE-TO-MERGE, BLOCK-FIX or BLOCK-ESCALATE and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/ac-refine-revision-guard
