# Assistant channel: the API-rendered pages (sign-in, consent, done, error) look like Olympus. Owner request 2026-09-13 (the flow looks awful but it works). Ratified on issue 2340 under the standing delegation, behaviour and look only, no new copy. 2026-09-13
#
# DISPATCH: ./scripts/dispatch.sh git@github.com:Arlencho/olympus-platform.git wave-plans/assistant-channel/2026-09-13-assistant-pages-styling.plan --detach --retries 1
# Dispatch after the routing PR (feat/routing-grok-producers) has merged and the fleet checkout is pulled.
# ────────────────────────────────────────────────────────────────────────────

1 | devops | Branch feat/ac-pages-styling from origin/main. Amend docs/prd/pages/assistant-channel.md: add section 7.5, API-rendered pages, stating that every page the assistant channel serves from the API (the sign-in page, the consent screen of section 7.3, the connected and error pages) uses the Olympus visual language of 01-conventions and the web app (the same font stack, ink and background palette, button styles, spacing and 400-wide layout as the web sign-in surfaces), served by the API with an inline stylesheet under the route-scoped CSP nonce the consent page already carries, with no new strings (S1 to S20 unchanged) and no change to the OAuth flow; the page title is Connect <host> to Olympus as today. Date it 2026-09-13, cite issue 2340 (comment SELF-RATIFICATION 2026-09-13 assistant pages). VERIFY: make check-prd-index exit 0. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Commit and push. | feat/ac-pages-styling

2 | go-backend | On branch feat/ac-pages-styling after wave 1, in apps/api/internal/handler/assistant_oauth.go and any template it uses: implement section 7.5. One shared layout and one inline stylesheet for every page this handler renders, mirroring the web tokens in apps/web/app/globals.css (the ra block: font stack, ink, ink-inv, background, radius, the primary and secondary button rules): a centred card at most 28rem wide, the title as the only h1, the consent checkbox and its S4 line as one label with a 44 px target, the S5 sentence as body text, Connect as the primary button (disabled until the box is checked, as today) and Cancel as the secondary, the same treatment on the sign-in, connected and error pages, dark and light both readable, no horizontal scroll at 400 wide, visible keyboard focus. The stylesheet is emitted with the same route-scoped CSP nonce the page script uses; the CSP header is not loosened and style-src gains only the nonce. No string changes anywhere. Tests: every rendered page carries the stylesheet with the request nonce and the CSP header names that nonce for style-src; the ratified strings are byte-identical to before; the consent form still posts the CSRF token and the Allow path is unchanged (existing tests stay green). VERIFY: go test ./... with the race flag in apps/api, real exit codes in the PR body, plus one screenshot of the consent page at 400 and at 1280 rendered from a local run. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Open a draft PR titled Assistant channel: the API-rendered pages look like Olympus, Refs 2800 and 2340, PRD line ticked. | feat/ac-pages-styling

3 | backend-critic | READ-ONLY REVIEW of the PR on branch feat/ac-pages-styling. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. THE QUESTION THAT MATTERS: did styling touch security. Check the CSP header on every page: style-src carries the request nonce only, no unsafe-inline, script-src unchanged; the nonce is per request and never reused; the CSRF token, the consent POST, the Allow gate and the sign-in redirect are byte-identical in behaviour to main; no string changed. Write failing tests where the PR falls short. Post ONE comment on issue 2340 whose first line reads CRITIC ASSISTANT PAGES with SAFE-TO-MERGE, BLOCK-FIX or BLOCK-ESCALATE and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/ac-pages-styling

3 | frontend-critic | READ-ONLY REVIEW of the look of the pages on branch feat/ac-pages-styling. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. Render each page from a local run at 400 and 1280, light and dark: compare against the web sign-in surface for font, palette, button styles and spacing; check contrast, a 44 px checkbox target, visible focus on every control, no horizontal scroll, the disabled Connect state readable, and that the page reads as Olympus and not as a browser default. Post ONE comment on issue 2340 whose first line reads FRONTEND CRITIC ASSISTANT PAGES with SAFE-TO-MERGE, BLOCK-FIX or BLOCK-ESCALATE and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/ac-pages-styling
