# Fix wave for docs/live-booking-docs-sweep (PR 2813) after plan-critic BLOCK-FIX. 2026-09-12
#
# DISPATCH: ./scripts/dispatch.sh git@github.com:Arlencho/olympus-platform.git wave-plans/assistant-channel/2026-09-12-live-docs-sweep-fix.plan --auto --retries 2
# ────────────────────────────────────────────────────────────────────────────

1 | devops | Fix branch docs/live-booking-docs-sweep (PR 2813) per the plan-critic comment on issue 2340 whose first line reads CRITIC LIVE DOCS SWEEP. Three edits, nothing else. ONE, docs/prd/pages/atlas-chat.md, the balance-variant row near line 3442: the new sentence cites PR 2663 alone; the card journey landed in two PRs on 2026-08-14, PR 2663 (commit 34f380bb, the checkout libs post settlement card) and PR 2664 (commit 3b10e08b, chat-v2 wiring and the CARD row in this page), so cite both. In the same cell delete the leftover sentence that says those two intents have no ratified card confirm SURFACE, since it is contradicted by the CARD rows below and by the sentence you are correcting. TWO, docs/operations/duffel-balance-funding.md near line 52: the replacement sentence mentions KYC and a funding date that no file in the repo or the LOG EVIDENCE comment on PR 2812 supports. Rewrite it to state only what is supported: the Balance held funds on 2026-09-11, shown by the Duffel order POST answering 201 in the LOG EVIDENCE paste on PR 2812 for booking 282d1d31-8915-48e9-806c-c2bf8326f16d, and the insufficient_balance failure mode documented near lines 13 to 14 of the same file did not fire. No amounts, no KYC, no calendar claims beyond the booking date. VERIFY: make check-prd-index; git diff against main shows exactly the same four files as before; paste real exit codes. HOUSE STYLE: no long dash, no AI tool or vendor name, no Co-Authored-By trailer. Push to the same branch. Do not merge. | docs/live-booking-docs-sweep

2 | plan-critic | READ-ONLY RE-REVIEW of branch docs/live-booking-docs-sweep (PR 2813) after the fix wave. No commits, no push, no edits, no labels; tmp writes only. Record head SHA at start and at report. Your prior comment on issue 2340 starts CRITIC LIVE DOCS SWEEP and listed three fixes; grade each: (1) atlas-chat.md near line 3442 cites both PR 2663 and PR 2664 and the leftover no ratified card confirm SURFACE sentence is gone; (2) duffel-balance-funding.md near line 52 claims only what the LOG EVIDENCE comment on PR 2812 and lines 13 to 14 of the file support, with no KYC, no amounts and no funding date; (3) every other changed sentence still traces to a file and line on main, a merged PR, the LOG EVIDENCE paste, or the /api/v1/health response. Re-check the DPA status cell still reads Not started and the DPA sentence records only the 2026-09-12 co-founder decision; exactly four files changed; no long dash, no vendor name, no trailer. Post ONE comment on issue 2340 whose first line reads CRITIC LIVE DOCS SWEEP ROUND 2, with SAFE-TO-MERGE or BLOCK-FIX and line citations. | docs/live-booking-docs-sweep
