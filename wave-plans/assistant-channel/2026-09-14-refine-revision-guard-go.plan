# Refine revision guard round 2, code and critics: the spec half landed at 7955cae2 on PR 2850; the code seat never started because the earlier dispatch stalled and was stopped at 05:23. 2026-09-14
#
# DISPATCH: ./scripts/dispatch.sh git@github.com:Arlencho/olympus-platform.git wave-plans/assistant-channel/2026-09-14-refine-revision-guard-go.plan --detach --retries 1
# ────────────────────────────────────────────────────────────────────────────

1 | go-backend | On branch feat/ac-refine-revision-guard (PR 2850, spec already amended at 7955cae2): fix F1 of the comment on issue 2340 whose first line reads CRITIC REFINE REVISION GUARD: a website write during the search (window c) is a revision conflict, answered as the outcome api.yaml now declares with the current record, never as refine_failed or search service unavailable, never at error level, never paging; make the three failing tests the critic wrote green, on the API handler, the Iris client and the Iris tool. VERIFY: go test ./... with the race flag in apps/api and services/iris, make check-api-spec exit 0, real exit codes in the PR body under a Round 2 heading. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Commit and push. | feat/ac-refine-revision-guard

2 | backend-critic | READ-ONLY REVIEW ROUND 2 of PR 2850. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. Re-run the three windows on this head; confirm window (c) is the declared conflict outcome with the current record and no error-level log; confirm the web scope is unchanged. Post ONE comment on issue 2340 whose first line reads CRITIC REFINE REVISION GUARD ROUND 2 with SAFE-TO-MERGE, BLOCK-FIX or BLOCK-ESCALATE and citations, and a last line TRIAL: producer rounds to SAFE so far <n>. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/ac-refine-revision-guard

2 | api-critic | READ-ONLY REVIEW ROUND 2 of the api.yaml diff on PR 2850. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. Confirm F1 and F2 are closed and the runtime matches the spec. Post ONE comment on issue 2340 whose first line reads API CRITIC REFINE REVISION GUARD ROUND 2 with SAFE-TO-MERGE, BLOCK-FIX or BLOCK-ESCALATE and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/ac-refine-revision-guard
