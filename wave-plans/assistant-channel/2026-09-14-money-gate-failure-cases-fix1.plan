# Money gate failure cases round 2: every tool refuses a revoked token before doing work (decision on issue 2340 at 22:20), plus the critic and security findings on the suite. PR 2858. Tier A. 2026-09-14
#
# DISPATCH: ./scripts/dispatch.sh git@github.com:Arlencho/olympus-platform.git wave-plans/assistant-channel/2026-09-14-money-gate-failure-cases-fix1.plan --detach --retries 1
# FIX-ROUND: 2026-09-14-money-gate-failure-cases.plan
# TIER: A
# ────────────────────────────────────────────────────────────────────────────

1 | go-backend | On branch test/money-gate-failure-cases (PR 2858), rebase onto origin/main first. Read the comments on issue 2340 whose first lines read CRITIC MONEY GATE FAILURE CASES ROUND 1 and SECURITY MONEY GATE FAILURE CASES, and the decision comment SELF-RATIFICATION 2026-09-14 22:20 item 3. Product fix in services/iris: before any tool does work (place resolution included), Iris confirms the binding is live with one authenticated API call, so a revoked token answers token_revoked on the very next call of every tool; a refused token never touches the named traveller quota. Suite fixes: F1 the checkout record assertion must be able to fail (give the fake API a checkout route and writer, or remove the assertion and say why); F2 case 7 presents a real option under the wrong search; F3 and the security MEDIUM, test the 403 to not_found mapping so the existence-oracle defence has a failing case; LOW, case 6 asserts the first call per tool; LOW, case 7 covers status, the write path and list isolation. This is a fix round: run only go test with the race flag on services/iris/internal/tools and the e2e tag on the new suite; nothing else is run. VERIFY: those with real exit codes and per-case output in the PR body under a Round 2 heading. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Commit and push. | test/money-gate-failure-cases

2 | backend-critic | READ-ONLY REVIEW ROUND 2 of PR 2858. Tier A. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. Re-run only the round 1 mutations M8 and M9 and F1, plus the revoked-token ambiguous search repro; each must now go red when the behaviour breaks and green on this head. Post ONE comment on issue 2340 whose first line reads CRITIC MONEY GATE FAILURE CASES ROUND 2 with SAFE-TO-MERGE, BLOCK-FIX or BLOCK-ESCALATE and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | test/money-gate-failure-cases

2 | security-reviewer | SECURITY REVIEW ROUND 2 of PR 2858. Read-only; tmp writes only. Record head SHA at start and at report. Re-run the three MEDIUM findings: revoked token on an ambiguous search, quota burned by a refused token, existence oracle. Post ONE comment on issue 2340 whose first line reads SECURITY MONEY GATE FAILURE CASES ROUND 2 with a final SAFE or BLOCK line. HOUSE STYLE: no long dash, no AI tool or vendor name. | test/money-gate-failure-cases
