# Connected assistants tile: the How to connect guide for host B gains the Always allow step, S21 and S22, co-founder sign-off on issue 2340 at 21:55. Tier B. 2026-09-15
#
# DISPATCH: ./scripts/dispatch.sh git@github.com:Arlencho/olympus-platform.git wave-plans/assistant-channel/2026-09-15-connect-guide-always-allow.plan --detach --retries 1
# TIER: B
# ────────────────────────────────────────────────────────────────────────────

1 | web-frontend | Branch feat/connect-guide-always-allow from origin/main. Read the comment on issue 2340 whose first line reads CO-FOUNDER SIGN-OFF 2026-09-15 21:55; it carries the exact text of S21 and S22. ONE, docs/prd/pages/assistant-channel.md: add rows S21 and S22 to section 9 with that exact text, marked ACCEPTED citing that comment, update the section 9 header range to S1 to S22, and add step 5 with the note to the host B How to connect description in section 7.1. TWO, apps/web/components/concierge/assistant-connect-guide.tsx: render S21 as step 5 of the host B steps after S15, in the same list style, and S22 directly under it as a smaller helper note; host A is unchanged. Use the strings exactly, no other copy. Tests in the existing test file for that component: S21 renders as the fifth host B step, S22 renders under it, the host A row is unchanged. VERIFY: the web unit tests for that component, web lint and web typecheck, with real exit codes in the PR body. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name in commits or PR text, no Co-Authored-By trailer. Open a draft PR with Refs 2800 and 2340, PRD line ticked. | feat/connect-guide-always-allow

2 | frontend-critic | READ-ONLY REVIEW of the PR on branch feat/connect-guide-always-allow. Tier B, round 1 of at most 3. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. Check the strings are byte-identical to the sign-off comment, step 5 reads naturally after S15 at 400 wide and on desktop, the note is visibly secondary but readable, keyboard and screen reader order follow the steps, and the PRD rows match the code. Post ONE comment on issue 2340 whose first line reads FRONTEND CRITIC CONNECT GUIDE ALWAYS ALLOW with SAFE-TO-MERGE, BLOCK-FIX or BLOCK-ESCALATE and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/connect-guide-always-allow
