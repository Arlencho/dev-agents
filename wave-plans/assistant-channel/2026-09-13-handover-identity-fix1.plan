# Handover identity fix round: two findings from CRITIC HANDOVER IDENTITY on issue 2340. 2026-09-13
#
# DISPATCH: ./scripts/dispatch.sh git@github.com:Arlencho/olympus-platform.git wave-plans/assistant-channel/2026-09-13-handover-identity-fix1.plan --detach --retries 1
# ────────────────────────────────────────────────────────────────────────────

1 | go-backend | On branch feat/ac-handover-identity (PR 2845), in apps/api, fix BLOCK 1 and BLOCK 2 of the comment on issue 2340 whose first line starts with CRITIC HANDOVER IDENTITY: ONE, the integration tag does not compile on the branch (a constructor call in the integration test file lacks the new tenth argument; append the empty origin argument the critic names); TWO, clone of a record whose parsed_fields is not a JSON object is a 500 on Postgres; reject a non-object parsed_fields at Create with the existing ErrResultSessionInvalidParsedFields (400, the same answer Refine gives) and add the memory fixture test the critic wrote plus a guard test on the clone query path. VERIFY: go test ./... with the race flag in apps/api and go vet with the integration tag, real exit codes in the PR body under a Round 2 heading. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Commit and push. | feat/ac-handover-identity

2 | backend-critic | READ-ONLY REVIEW ROUND 2 of PR 2845 on branch feat/ac-handover-identity. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. Confirm the integration tag compiles, the non-object parsed_fields create is 400 and clone of any stored record is never 500, and re-run the round 1 access matrix probes on this SHA. Post ONE comment on issue 2340 whose first line reads CRITIC HANDOVER IDENTITY ROUND 2 with SAFE-TO-MERGE or BLOCK-FIX and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/ac-handover-identity
