# Connector pages styling round 2: one route outside the CSP group, one alert contrast rule. 2026-09-13
#
# DISPATCH: ./scripts/dispatch.sh git@github.com:Arlencho/olympus-platform.git wave-plans/assistant-channel/2026-09-13-assistant-pages-styling-fix1.plan --detach --retries 1
# ────────────────────────────────────────────────────────────────────────────

1 | go-backend | On branch feat/ac-pages-styling (the open PR), in apps/api: fix B1 of the comment on issue 2340 whose first line reads CRITIC ASSISTANT PAGES (the Google return route renders an expired page outside the assistant HTML CSP group, so its style nonce is empty; mount it inside the group or redirect into it, plus the test), and the blocking item of the comment whose first line reads FRONTEND CRITIC ASSISTANT PAGES (the alert text colour fails contrast; use the rule the critic gives, ink colour with the alert border, and add the measured-contrast test). No copy changes. VERIFY: go test ./... with the race flag in apps/api, real exit codes in the PR body under a Round 2 heading. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Commit and push. | feat/ac-pages-styling

2 | backend-critic | READ-ONLY REVIEW ROUND 2 of the PR on branch feat/ac-pages-styling. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. Confirm every HTML page the assistant flow can render, including the Google return path on failure, carries the nonce CSP and a matching style nonce; nothing else moved. Post ONE comment on issue 2340 whose first line reads CRITIC ASSISTANT PAGES ROUND 2 with SAFE-TO-MERGE, BLOCK-FIX or BLOCK-ESCALATE and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/ac-pages-styling

2 | frontend-critic | READ-ONLY REVIEW ROUND 2 of the pages on branch feat/ac-pages-styling. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. Re-measure the alert contrast at 400 light and dark, re-run the round 1 passed checks on this head. Post ONE comment on issue 2340 whose first line reads FRONTEND CRITIC ASSISTANT PAGES ROUND 2 with SAFE-TO-MERGE, BLOCK-FIX or BLOCK-ESCALATE and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/ac-pages-styling
