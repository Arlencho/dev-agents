# Round two for feat/ac-tile-connect-guide (PR 2839) after frontend-critic BLOCK-FIX. 2026-09-13
#
# DISPATCH: ./scripts/dispatch.sh git@github.com:Arlencho/olympus-platform.git wave-plans/assistant-channel/2026-09-13-tile-connect-guide-fix.plan --auto --retries 1
# Critic comment on issue 2340, first line CRITIC TILE CONNECT GUIDE: block 1 (no refetch
# on return, so the tile cannot show Connected without a reload), block 2 (the screen
# labels in the steps have no recorded source), fix 3 (orphaned period), fix 4 (34 px field).
# ────────────────────────────────────────────────────────────────────────────

1 | devops | On branch feat/ac-tile-connect-guide fix block 2 and the pilot precondition note, docs only. THE SOURCE FOR THE LABELS, observed by the operator on 2026-09-13 between 10:47 and 12:00 local on the host B desktop app while connecting Olympus for real, recorded here for you as fact: the path is Settings, then Connectors, then Add custom connector; the dialog has a Name field and a URL field; a section titled Authentication with three options where Sign in now was detected and selected; a section titled OAuth client with three options: Use Claude published identity (Recommended), Register automatically, and Use your own OAuth client, the last one taking a client ID and an optional secret; an Add button; then the connector page shows the URL, the sentence You are not connected to Olympus yet, and a Connect button; pressing Connect opens the browser on an interstitial titled Finish connecting a connector? with a Continue connecting button, which then lands on our sign-in page; after consent the browser shows Connected and returns to the desktop app, where the connector page lists Tool permissions with Read-only tools (6) and Write/delete tools (1), each defaulting to Needs approval. Record exactly this in docs/operations/assistant-hosts.md, host B section, as a dated walk record (observed 2026-09-13 by the operator, not from the published pages), and make the S12 note in docs/prd/pages/assistant-channel.md cite that record instead of claiming the pages carry it. Also update assistant-hosts.md near line 24: the running API and Iris now carry the Iris MCP URL as ASSISTANT_RESOURCE_URI (PR 2837 deployed 2026-09-13 mid-day), so the precondition is met. VERIFY: make check-prd-index exit 0. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name in the doc text beyond the host name the contract already uses, no Co-Authored-By trailer. Commit and push. | feat/ac-tile-connect-guide

2 | web-frontend | On branch feat/ac-tile-connect-guide fix block 1, fix 3 and fix 4 of the critic comment on issue 2340 whose first line reads CRITIC TILE CONNECT GUIDE. BLOCK 1: the bindings envelope is read once on mount, so a tester who follows the steps returns to a tab that still says Not connected; refetch on visibilitychange and window focus (debounced, never more than once per few seconds), so the row flips to Connected and the S8 toast fires when the traveller comes back; add the critic test return-refetch.test.tsx unchanged. FIX 3: step 3 renders an orphaned period on its own line after the block-level value field; either drop the leading punctuation from the trailing text or render the field inline, and add the assertion that no text node inside any guide step starts with a period. FIX 4: the copyable value field is 34 px tall on mobile; give it the same min-h-11 md:min-h-0 lift the Copy button carries, 44 px at 375 wide. VERIFY: npm run lint, typecheck and unit tests for apps/web with real exit codes, voice lint on changed files. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Push to the same branch, keep the PR a draft. | feat/ac-tile-connect-guide

3 | frontend-critic | READ-ONLY RE-REVIEW of PR 2839 on branch feat/ac-tile-connect-guide after the fix waves. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. Grade block 1, block 2, fix 3 and fix 4 CLEARED or STILL OPEN: run your return-refetch test; simulate the return (visibilitychange to visible) with a fixture whose bindings now include a fresh host binding and confirm the row reads Connected and the S8 toast fires once; confirm the labels in the steps now cite the dated walk record in assistant-hosts.md and match it word for word; probe the DOM for a text node starting with a period; measure the value field at 375 wide. Then the cold read again as a first-time tester. Post ONE comment on issue 2340 whose first line reads CRITIC TILE CONNECT GUIDE ROUND 2 with SAFE-TO-MERGE or BLOCK-FIX. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/ac-tile-connect-guide
