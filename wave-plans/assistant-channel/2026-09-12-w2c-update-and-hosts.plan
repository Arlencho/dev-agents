# Assistant Channel W2-C: update_trip, the enforcement matrix, and connecting both hosts. Issue 2800. 2026-09-12
#
# DISPATCH: ./scripts/dispatch.sh git@github.com:Arlencho/olympus-platform.git wave-plans/assistant-channel/2026-09-12-w2c-update-and-hosts.plan --auto --retries 1
#
# FIRE AFTER W2-B IS MERGED. Contract sections 3.3.7, 5, 6, 7.2.
# ────────────────────────────────────────────────────────────────────────────

1 | go-backend | In services/iris implement update_trip per docs/prd/pages/assistant-channel.md section 3.3.7 and section 5: the only Wave 2 tool that writes, and only the travellers own trip record (title, dates, traveller counts, option selection, a short note), under the explicit revision from the W1-A column; a stale revision returns stale_revision with the current state and the writer must reconcile; the note is trip-scoped and never becomes a standing preference (issue 2800 fact 5). Then the enforcement matrix the contract requires in section 5 and issue 2800 demands as proof: a test that enumerates every route the connector can reach with an assistant-scoped token and asserts that none of them mints a pay-link, creates a checkout, charges, authorises, captures, refunds or stores a payment credential, by exercising the scope enforcement of the API (apps/api/internal/middleware/auth.go) rather than by reading a list; the two Wave 3 tool names must answer not_available. VERIFY: go test ./... in services/iris with the race flag, and the matrix test output pasted in full into the PR body. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Open a draft PR with Refs 2800. Do not merge. | feat/ac-w2c-update-and-hosts

2 | devops | On branch feat/ac-w2c-update-and-hosts write the operator documentation for connecting the two pilot hosts, docs/operations/assistant-hosts.md: for each host, the exact steps a tester follows to add Iris (custom connector for host B, the developer connector path for host A), what the consent page shows, how to disconnect, and how an operator turns a host off with the kill switch from W2-A and confirms it took effect in the logs; plus the runbook lines for watching a pilot session per binding. Take every host-side fact from the published documentation cited in the contract section 6 table, and cite URL and date again; nothing from memory. Keep it to what a tester and an operator need. Add the page to the docs index if one exists and run make check-prd-index. HOUSE STYLE: no long dash, no AI tool or vendor name in the doc, commit or PR text, no Co-Authored-By trailer. Push to the same branch. | feat/ac-w2c-update-and-hosts

3 | backend-critic | READ-ONLY REVIEW of the draft PR on branch feat/ac-w2c-update-and-hosts. No commits, no push, no review, no labels, no edits; tmp writes only. Record head SHA at start and at report. THE QUESTIONS THAT MATTER: does a concurrent edit with a stale revision lose data or overwrite (write the race as a test and paste it); does the note ever reach any preference or profile table (grep the write path end to end); does the enforcement matrix really exercise the scope middleware rather than assert a list, and does it cover every route the connector can reach; do the Wave 3 names answer not_available. Then read docs/operations/assistant-hosts.md against the contract section 6 sources and flag any host-side claim without a citation. Post ONE comment on issue 2340 whose first line reads CRITIC W2C UPDATE AND HOSTS with SAFE-TO-MERGE or BLOCK-FIX and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/ac-w2c-update-and-hosts

4 | security-reviewer | SECURITY REVIEW of the draft PR on branch feat/ac-w2c-update-and-hosts, the first write tool on the assistant channel and the enforcement matrix. Read-only; tmp writes only. Record head SHA at start and at report. Threat model: a host writing to a trip that belongs to another traveller; a note used to smuggle a traveller name or instruction into the website chat; a replayed update with an old revision; a host reaching a money route through any path the matrix did not enumerate. For each, defeat with file and line or write the exploit. Post ONE comment on issue 2340 whose first line reads SECURITY W2C UPDATE AND HOSTS with a severity per finding and a final SAFE or BLOCK line. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/ac-w2c-update-and-hosts
