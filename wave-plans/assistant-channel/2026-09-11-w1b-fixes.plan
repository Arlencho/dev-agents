# Assistant Channel W1-B fixes: concierge liveness, env docs, CORS guard. PR 2804.
#
# DISPATCH: ./scripts/dispatch.sh git@github.com:Arlencho/olympus-platform.git wave-plans/assistant-channel/2026-09-11-w1b-fixes.plan --auto --retries 2
#
# Same branch as PR 2804. The W0 citation item is a founder dependency being
# resolved in parallel (PR 2802 sign-off); it is NOT in scope here.
# ────────────────────────────────────────────────────────────────────────────

1 | go-backend | Fix the findings raised against PR 2804 on branch feat/ac-w1b-scope. READ FIRST the review comment on issue 2340 whose first line reads CRITIC ASSISTANT CHANNEL W1B SCOPE and treat its file and line citations as the specification. Three fixes and nothing else; do not restructure and do not touch the allowlist, which the review verified as correct. ONE, the BLOCK, a security defect proven on the running router. The concierge liveness branch at middleware/auth.go around line 201 still calls HasActiveBinding with only a userID, and service/concierge.go around lines 625 to 634 returns true for any active binding on any channel, with no channel predicate at line 631. Now that the assistant channel value shares the table, an active assistant binding keeps a REVOKED Telegram user token alive for its full TTL. The reviewer drove it: revoke telegram with an assistant binding active, the concierge token still creates a result session, 201; the control with no assistant binding correctly gets 401. Fix: give the concierge branch the same channel-keyed check the assistant branch already uses, HasActiveBindingForChannel with the telegram channel constant, so each channel liveness keys only to its own binding. Then add the MIRROR IMAGE row to TestHermesScopeEnforcement_AssistantRevocationDoesNotCoupleToConcierge: revoke telegram while an assistant binding is active, assert the concierge token is refused on the next request, and keep the existing row that revokes assistant and asserts telegram survives. Both directions must be pinned. Prove the new row FAILS on the current branch head before your fix and passes after, in a throwaway worktree, with real exit codes captured directly rather than read after a pipe; this must be a behavioural red, not a compile error, because the reviewer noted every earlier fail-first was compile-only. TWO, documentation: add ASSISTANT_SERVICE_TOKEN and CORS_ALLOWED_ORIGINS to .env.example next to HERMES_SERVICE_TOKEN around line 323, and to docs/operations/env-vars-api.md next to its sibling around line 96, following the existing row format exactly. THREE, the advisory, cheap so fold it in: getEnvOriginList at config.go around line 1655 does no validation and feeds a credentialed CORS handler, so a wildcard produces a silently broken config. Reject a wildcard entry at load with a clear error, add a test, and leave the default and every valid origin untouched. DO NOT touch the seven citations of docs/prd/pages/assistant-channel.md: that page is PR 2802, awaiting co-founder sign-off in parallel, and the citations become valid when it merges. Say that in the PR comment. Run go build, go vet, the api unit suite with race, golangci-lint and make check-api-spec; paste real exit codes. Commit before you break anything; never git stash; never git checkout to revert. Push to the SAME branch so PR 2804 updates in place; do not open a new PR; do not merge. Comment on PR 2804 listing what changed per finding. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name in commit or PR text, no Co-Authored-By trailer. | feat/ac-w1b-scope

2 | backend-critic | READ-ONLY VERIFICATION of the fixes on branch feat/ac-w1b-scope, PR 2804. You raised the findings; this wave checks they are closed and nothing else moved. No commits, no push, no gh pr review, no gh pr merge, no labels, no edits. Permitted writes are a slash tmp worktree only. Record the PR head SHA at start and re-check at report; if it moved report BLOCKED with both SHAs. FIRST, diff the new head against d7a4eee5, the head you reviewed, and confirm only the three findings moved: the liveness fix and its mirror test, the two env docs, the wildcard guard. The allowlist, the enforcement matrix rows and the route wiring must be byte-identical to what you passed. SECOND, the BLOCK, grade strictly and by execution: drive the real router; revoke telegram while an assistant binding is active and confirm the concierge token is now refused on the next request; then the reverse; then confirm a user with only a telegram binding is unaffected by the change. All three must hold. THIRD, confirm the new test row fails behaviourally on d7a4eee5 rather than failing to compile, by running it there yourself. FOURTH, confirm both env vars are documented in both files in the existing format. FIFTH, drive getEnvOriginList with a wildcard and confirm load refuses it with a clear error, and confirm the default and a valid explicit list still load. SIXTH, the W0 citation item: check whether docs/prd/pages/assistant-channel.md is on main now. If it is, verify the seven citations resolve to the allowlist it specifies. If it is not, record that as PENDING the PR 2802 sign-off being handled in parallel by the founders, and do not block on it, since the producer was instructed not to touch it. SEVENTH, no long dash, no vendor name, no trailer. Post ONE comment on issue 2340 whose first line reads CRITIC ASSISTANT CHANNEL W1B FIXES VERIFIED, with a verdict of SAFE-TO-MERGE or BLOCK-FIX or BLOCK-ESCALATE and per-finding file and line citations. SAFE-TO-MERGE may carry the W0 citation item as a stated dependency. No vendor names, emails, booking ids or gcloud payloads. | feat/ac-w1b-scope
