# W2-B fix wave after backend-critic BLOCK-FIX on PR 2832. 2026-09-12
#
# DISPATCH: ./scripts/dispatch.sh git@github.com:Arlencho/olympus-platform.git wave-plans/assistant-channel/2026-09-12-w2b-fixes.plan --auto --retries 1
# Critic comment on issue 2340, first line CRITIC W2B READ TOOLS: B1 to B4 with RED
# tests in the diff, C1 to C3 without, and the CI job Iris Build + Test is red.
# ────────────────────────────────────────────────────────────────────────────

1 | api-designer | On branch feat/ac-w2b-read-tools (PR 2832) fix finding C1 of the critic comment on issue 2340 whose first line reads CRITIC W2B READ TOOLS: apps/api/internal/handler/airport_resolve.go serialises a candidates property that AirportResolveResponse in api.yaml does not declare. Add the property to api.yaml with the exact shape the handler emits (read the handler and its model), run make generate and commit the generated client, run make check-api-spec, and paste real exit codes. Touch nothing else. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Push to the same branch. | feat/ac-w2b-read-tools

2 | go-backend | Fix PR 2832 on branch feat/ac-w2b-read-tools per the critic comment on issue 2340 whose first line reads CRITIC W2B READ TOOLS. READ IT IN FULL FIRST, including the four RED tests at the bottom, and add those tests to the suite unchanged apart from package placement. ONE (B1, in apps/api): an assistant-scoped token must never read the record of another traveller. The result-session read surface serves any signed-in holder of the UUID by the share-link directive; that directive stays for web sessions, but on the assistant scope the read handlers for result sessions (get, list, and the refine path) must enforce owner equality and answer 404 for a record the caller does not own, so Iris can map it to not_found with no other information; put the gate in the middleware or handler layer where the scope is already known, with a test for each of the three surfaces. TWO (B2, iris): clarify must never take a field named in answered as a resolved code; a question_id must be signed or otherwise verified as one Iris issued, and the answer must go through place resolution like any other input; a crafted id or a free-text answer never fires a paid search. THREE (B3, iris): refine on a web-made record must keep departure_airport_code and arrival_airport_code and never overwrite them with the free-text from and to fields; read the model in apps/api/internal/model/search.go and the web writer in apps/web/lib/preflight-airports.ts to get the shape right. FOUR (B4, iris): list_trips must accept the API cursor shape (padded URL-safe base64 of o:<offset>); fix the id shape check and make the fake return a real cursor so the test cannot bless the wrong thing. FIVE (C2): move the ambiguous-place and multi-airport calls out of the airport_resolve handler into the agent service; decode, call service, encode. SIX (C3): every error result carries web_url; fix the upstream_unavailable fallback. SEVEN: the CI job Iris Build + Test is red on the PR head; open the run log, fix the cause, and paste the green run. VERIFY: go test ./... with the race flag in services/iris and in apps/api, the four critic tests green, real exit codes pasted into the PR body. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Push to the same branch, keep the PR a draft. | feat/ac-w2b-read-tools

3 | backend-critic | READ-ONLY RE-REVIEW of PR 2832 on branch feat/ac-w2b-read-tools after the fix waves. No commits, no push, no review, no labels, no edits; tmp writes only. Record head SHA at start and at report. Grade B1 to B4 and C1 to C3 from your round-one comment CLEARED or STILL OPEN with proof: run your four RED tests; then run the harness you used in round one against the new tree: the UUID of another traveller on get_trip, get_option and refine (must be not_found with no other information), three crafted question ids on clarify (no paid search, resolves must run), one assistant refine on a web-made record (airport codes intact), list_trips page two with the real cursor; and re-ask the section 3.1 field scan and the no-ranking, no-pricing check. Confirm the CI job Iris Build + Test is green on the head. Post ONE comment on issue 2340 whose first line reads CRITIC W2B READ TOOLS ROUND 2 with SAFE-TO-MERGE or BLOCK-FIX and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/ac-w2b-read-tools

4 | api-critic | READ-ONLY REVIEW of the api.yaml change on branch feat/ac-w2b-read-tools (PR 2832). No commits, no push, no edits; tmp writes only. Record head SHA at start and at report. Confirm the new candidates property on AirportResolveResponse matches byte for byte what the handler serialises (write a contract test that decodes a real handler response against the generated type), that make generate output is committed and clean, that make check-api-spec is green, and that no other response shape on the assistant read surface drifted from the spec (compare the result-session envelope Iris consumes against api.yaml). Post ONE comment on issue 2340 whose first line reads API CRITIC W2B with SAFE-TO-MERGE or BLOCK-FIX and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/ac-w2b-read-tools
