# W2-C round three: the PATCH contract in api.yaml. PR 2833. 2026-09-13
#
# DISPATCH: ./scripts/dispatch.sh git@github.com:Arlencho/olympus-platform.git wave-plans/assistant-channel/2026-09-13-w2c-spec.plan --auto --retries 0
# Critic round two: items 1, 2, 4, 6 CLEARED, 3 and 5 hold, CI green; one new item 7:
# the runtime PATCH contract moved and api.yaml did not. API Designer scope.
# ────────────────────────────────────────────────────────────────────────────

1 | api-designer | On branch feat/ac-w2c-update-and-hosts (PR 2833) land item 7 of the critic comment on issue 2340 whose first line reads CRITIC W2C UPDATE AND HOSTS ROUND 2. READ that item first, then the runtime: apps/api/internal/handler/result_sessions.go near lines 117, 290 and 295, and apps/api/internal/service/result_sessions.go near line 285. Three schema edits in api.yaml: ResultSession gains the revision property (integer, present on every read); PatchResultSessionRequest gains expected_revision (integer, optional for web callers, required on the assistant scope, say so in the description); PATCH /result-sessions/{id} gains a 409 response whose data carries the current ResultSession, and its 400 description names the missing expected_revision case on the assistant scope. Then make generate and commit the generated client, make check-api-spec, and paste real exit codes. Touch nothing else. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Push to the same branch. | feat/ac-w2c-update-and-hosts

2 | api-critic | READ-ONLY REVIEW of the api.yaml change on branch feat/ac-w2c-update-and-hosts (PR 2833). No commits, no push, no edits; tmp writes only. Record head SHA at start and at report. Write a contract test that decodes a real handler response for GET, PATCH 200, PATCH 409 and PATCH 400 on the assistant scope against the generated types and the spec; confirm revision is on every read, expected_revision is declared with the stated optionality, the 409 carries the current record, make generate output is committed and clean, and make check-api-spec is green. Post ONE comment on issue 2340 whose first line reads API CRITIC W2C with SAFE-TO-MERGE or BLOCK-FIX and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/ac-w2c-update-and-hosts
