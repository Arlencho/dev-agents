# Handover identity round 3: the web half after issue 2842 landed, plus the remaining critic findings. 2026-09-13
#
# DISPATCH: ./scripts/dispatch.sh git@github.com:Arlencho/olympus-platform.git wave-plans/assistant-channel/2026-09-13-handover-identity-fix2.plan --detach --retries 1
# Findings: CRITIC HANDOVER IDENTITY ROUND 2 (PATCH bypasses the Create guard), SECURITY HANDOVER IDENTITY (MEDIUM: the copy carries assistant_note verbatim; LOW: clone not idempotent), API CRITIC HANDOVER IDENTITY (generated client breaks three existing web files), FRONTEND CRITIC HANDOVER IDENTITY (B1 typecheck and build red from wave 3, B2 web half absent, B3 stamp now cleaned and recorded on issue 2340).
# ────────────────────────────────────────────────────────────────────────────

1 | go-backend | On branch feat/ac-handover-identity (PR 2845), in apps/api, after rebasing the branch on origin/main (the 2842 fix is on main now): ONE, the non-object parsed_fields guard from round 2 also applies to PATCH, so no stored record can 500 on clone (the round 2 comment on issue 2340, first line CRITIC HANDOVER IDENTITY ROUND 2, has the repro). TWO, the copy strips assistant_note as well as assistant_owner and assistant_revision (SECURITY HANDOVER IDENTITY, MEDIUM); the PRD row in 06 section 13.6 and assistant-channel 7.4 that says same assistant note is corrected to say the note stays with the source, and the devops-owned PRD line may be edited by this seat for that one sentence only, say so in the commit. THREE, clone idempotency under double submit (SECURITY LOW): reuse the create route idempotency if one exists, otherwise leave it and record it as a follow-up in the PR body. Tests for ONE and TWO. VERIFY: go test ./... with the race flag in apps/api and go vet with the integration tag, real exit codes in the PR body under a Round 3 heading. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Commit and push. | feat/ac-handover-identity

2 | web-frontend | On branch feat/ac-handover-identity after wave 1, in apps/web. First fix the red typecheck and build: the generated client from wave 3 broke three existing files that build ResultSession values (the API CRITIC HANDOVER IDENTITY and FRONTEND CRITIC HANDOVER IDENTITY B1 comments on issue 2340 name them); update those usages for the new origin field, never loosen the schema. Then implement the web half exactly as the original wave 5 task states (PRD assistant-channel 7.4 and 06 section 4 on app/r/[id]): the record fetch answering 401 keeps the page shell mounted with the chat-loading placeholder, opens the in-page sign-in modal through requestAuth with the existing line Sign in to continue where you left off, and re-fetches in place after the modal resolves; 200 hydrates through the landing that issue 2842 shipped (results, chosen offer selected); 403 not_owner calls cloneResultSession from the generated client, replaces the URL with the new id through the router without a full navigation, and hydrates the copy through the same landing; the S20 line This trip is now saved in your account. renders once after a copy (ACCEPTED, section 9). Smooth transition: no full reload, no interstitial, no flash of the interview or of the 404 page at any step; the modal closes into the trip. Tests: guest on an assistant record sees the modal over the mounted shell and never the interview; owner after sign-in lands on the chosen flight without a reload; non-owner after sign-in lands on the copy at the new id with the same selection and sees S20 once; a web-origin record is untouched. E2E: triage the specs covering /r/[id] in the same PR per docs/operations/e2e-flake-diagnosis.md. VERIFY: web unit tests for the touched files, web lint, web typecheck and web build through the Makefile targets, real commands and exit codes in the PR body; the PR title stays as is because the web half now ships in it. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Commit and push. | feat/ac-handover-identity

3 | backend-critic | READ-ONLY REVIEW ROUND 3 of PR 2845. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. Confirm the PATCH guard closes the round 2 block, the copy carries none of assistant_owner, assistant_revision or assistant_note, and re-run the access matrix probes on this SHA. Post ONE comment on issue 2340 whose first line reads CRITIC HANDOVER IDENTITY ROUND 3 with SAFE-TO-MERGE or BLOCK-FIX and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/ac-handover-identity

3 | frontend-critic | READ-ONLY REVIEW ROUND 2 of the web half of PR 2845. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. THE QUESTION THAT MATTERS: is the transition smooth. Walk the three cases as a traveller and look for any full reload, interstitial, flash of the interview or of the 404 page, lost selection, or wrong URL after the copy; confirm typecheck and build are green on this head; confirm S20 renders once and only after a copy; confirm a web-origin record behaves exactly as before. Write failing tests where the PR falls short. Post ONE comment on issue 2340 whose first line reads FRONTEND CRITIC HANDOVER IDENTITY ROUND 2 with SAFE-TO-MERGE or BLOCK-FIX and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/ac-handover-identity

3 | api-critic | READ-ONLY REVIEW ROUND 2 of the api.yaml diff and generated client on PR 2845. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. Confirm the generated client compiles against every existing usage, the contract still matches PRD 06 section 13.3.3 and 13.6 (including the corrected note sentence), and make check-api-spec exits 0. Post ONE comment on issue 2340 whose first line reads API CRITIC HANDOVER IDENTITY ROUND 2 with SAFE-TO-MERGE or BLOCK-FIX and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/ac-handover-identity

3 | security-reviewer | SECURITY REVIEW ROUND 2 of PR 2845. Read-only; tmp writes only. Record head SHA at start and at report. Confirm the MEDIUM from round 1 is closed (no source free text on the copy), re-check the guest and non-owner answers on GET, PATCH and clone on this head, and check the web half stores nothing about the source after a copy. Post ONE comment on issue 2340 whose first line reads SECURITY HANDOVER IDENTITY ROUND 2 with a final SAFE or BLOCK line. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/ac-handover-identity
