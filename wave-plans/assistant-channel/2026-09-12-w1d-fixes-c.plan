# W1-D fix wave, part C: the non-gating leftovers from round two, then merge. 2026-09-12
#
# DISPATCH: ./scripts/dispatch.sh git@github.com:Arlencho/olympus-platform.git wave-plans/assistant-channel/2026-09-12-w1d-fixes-c.plan --auto --retries 1
#
# Round two on issue 2340: backend-critic SAFE-TO-MERGE, security SAFE. Leftovers:
# critic N1 (18 long dashes in added lines), N2 (four swallowed template errors),
# B7 (comment must cite the tracking issue, now 2825), security N1 (Google
# return-context association is per-instance memory; everything else moved to
# Postgres, this should too).
# ────────────────────────────────────────────────────────────────────────────

1 | db-architect | On branch feat/ac-w1d-authserver (draft PR 2815) add one nullable column to assistant_authorize_requests for the Google sign-in return context, keyed by the CSRF state nonce, in a new timestamped migration, plus the two sqlc queries the service needs: set the association by state for a pending request, and pop it by state (read and clear in one statement). READ FIRST the security round-two comment on issue 2340 (first line SECURITY W1D AUTHSERVER ROUND 2), finding N1, and apps/api/internal/handler/google_oauth.go near lines 74, 151 and 183 to see the map you are replacing. Run sqlc generation and the migration filename gate; paste real exit codes. Do not edit the handler; wave 2 does. HOUSE STYLE: no long dash, no AI tool or vendor name, no Co-Authored-By trailer. Commit and push to the same branch. | feat/ac-w1d-authserver

2 | go-backend | On branch feat/ac-w1d-authserver (draft PR 2815), four small items, one commit each. ONE, wire apps/api/internal/handler/google_oauth.go to the new column and queries from wave 1 so the return-context association survives across instances; delete the in-process map; keep the existing tests green and add one that proves a pop on a different service instance (a second handler over the same store) finds the association. TWO, replace the four _ = template.Execute calls in apps/api/internal/handler/assistant_oauth.go near lines 764, 771, 784 and 797 with one shared helper that logs the error with slog at error level and the template name, nothing else changes. THREE, the comment in apps/api/internal/service/assistant_auth.go near lines 713 to 720 about the pre-existing two-hosts limitation must cite issue 2825, which now tracks the migration. FOUR, remove every long dash (U+2014 and U+2013) from lines this branch adds: run git diff origin/main...HEAD -U0, grep the plus lines for those code points, fix each, and paste the final count, which must be zero, captured directly. VERIFY: go build ./... and go test ./... with the race flag in apps/api, real exit codes pasted into the PR body; update the PR body to mention the association column and that the PRD page is now on main (so the merge-order note is moot). Mark the PR ready for review at the end. HOUSE STYLE: no long dash, no AI tool or vendor name, no Co-Authored-By trailer. | feat/ac-w1d-authserver

3 | backend-critic | READ-ONLY CHECK of PR 2815 on branch feat/ac-w1d-authserver after part C. No commits, no push, no edits; tmp writes only. Record head SHA at start and at report. Confirm only these: the Google return-context association is in Postgres and the in-process map is gone, with the cross-instance test present and green; the four template executions log their error; the two-hosts comment cites issue 2825; zero long dashes in added lines (run the grep yourself and paste the count); go test ./... with the race flag green with the exit code pasted; nothing from rounds one and two regressed (re-run the four RED tests you shipped). Post ONE comment on issue 2340 whose first line reads CRITIC W1D AUTHSERVER ROUND 3 with SAFE-TO-MERGE or BLOCK-FIX. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/ac-w1d-authserver
