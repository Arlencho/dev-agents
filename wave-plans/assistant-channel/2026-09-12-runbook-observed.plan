# Record the first execution of the settlement check in deployment.md. 2026-09-12
#
# DISPATCH: ./scripts/dispatch.sh git@github.com:Arlencho/olympus-platform.git wave-plans/assistant-channel/2026-09-12-runbook-observed.plan --auto --retries 2
#
# DOCS ONLY. The runbook says its own procedure was never run. It was run on
# 2026-09-11 against a real booking. This makes the doc true again.
# ────────────────────────────────────────────────────────────────────────────

1 | devops | Update docs/operations/deployment.md, section Establishing what the RUNNING production build does, to record that the procedure was executed against production on 2026-09-11 and what it observed, and change nothing else. READ FIRST the whole section, including the notice that begins Steps 2 to 4 are PENDING FIRST EXECUTION, which the runbook itself says the first operator should replace with the date and observed position and then delete. THE OBSERVATION, taken from Cloud Run logs for olympus-api and from the operator walking the funnel, which you record as facts and do not embellish. Operator: Arlen, signed in as the Olympus identity, flight-only cart. At modal-open, 11:26:31 UTC, the API received POST /api/v1/bookings and answered 201, having first revalidated the offer with Duffel and then minted a manual-capture Stripe PaymentIntent, booking id 282d1d31-8915-48e9-806c-c2bf8326f16d, flow flights, currency EUR. That is the card variant, exactly as the step-4 table predicts: a booking row and an uncaptured intent exist before the CTA is pressed, and the balance variant posts nothing at open. The operator then confirmed: card authorised 11:28:26, Duffel order POST answered 201 at 11:28:34, Stripe capture completed 11:28:35, booking_confirmed with settlement card and amount 16245 cents. Order before capture, per issue 691. So the RUNNING production web build on 2026-09-11 posted settlement card, matching the environment value of 1 for the card flag. WHAT TO WRITE. One, replace the PENDING FIRST EXECUTION notice with a dated record in the same style as the existing dated bundle-grep line: date, operator role, the observed position, the booking id as the artifact, and the three log lines that establish it. Two, in the step-4 table or directly under it, mark the primary discriminator as observed rather than derived, since the CTA sub-line and the POST at open were both seen. Three, add one dated line noting that two webhooks were rejected during the same confirm, Duffel signature mismatch and Stripe API version mismatch, with pointers to issues 2317 and 2805, because an operator running this check later will see the same 400s in the log and must not mistake them for a settlement problem. Do not describe the defects further; the issues do. DO NOT touch anything outside that section. Do not change the step-6 remedy text. Do not add copy that is not a recorded fact. VERIFY: make check-prd-index if the doc is in scope of it, and paste real exit codes captured directly rather than read after a pipe; git diff must show only docs/operations/deployment.md. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name in the doc, commit or PR text, no Co-Authored-By trailer. Open a PR referencing issue 2646 with Refs. Do not merge. | docs/settlement-check-observed

2 | backend-critic | READ-ONLY REVIEW of the wave 1 change on branch docs/settlement-check-observed. No commits, no push, no review, no labels, no edits. Permitted writes are a slash tmp worktree only. Record the PR head SHA at start and re-check at report; if it moved report BLOCKED with both SHAs. THE QUESTION THAT MATTERS: does the doc now record what actually happened, and nothing that did not. You have gcloud access on this machine as the Olympus identity. FIRST, verify every factual claim in the new text against the Cloud Run logs for olympus-api on 2026-09-11 between 11:26:00Z and 11:29:00Z: the 201 at modal-open, the PaymentIntent mint before the CTA, the booking id, the authorise, the Duffel 201, the capture, the booking_confirmed event with settlement card and amount 16245. A recorded fact you cannot find in the log is a BLOCK, because this section exists to be trusted over documentation. SECOND, confirm the PENDING notice is gone and replaced by a dated record in the style of the existing dated line. THIRD, confirm the two webhook rejections are noted with issue pointers and not elaborated. FOURTH, confirm nothing outside the section changed and the step-6 remedy is byte-identical to main. FIFTH, no long dash, no vendor name, no trailer. Post ONE comment on issue 2340 whose first line reads CRITIC SETTLEMENT CHECK OBSERVED, with SAFE-TO-MERGE or BLOCK-FIX and line citations. No emails or gcloud payloads; booking ids are permitted here because the doc records one. | docs/settlement-check-observed
