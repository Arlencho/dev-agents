# W1-C fix wave after frontend-critic BLOCK-FIX on PR 2826. 2026-09-12
#
# DISPATCH: ./scripts/dispatch.sh git@github.com:Arlencho/olympus-platform.git wave-plans/assistant-channel/2026-09-12-w1c-fixes.plan --auto --retries 1
#
# Critic comment on issue 2340, first line CRITIC ASSISTANT CHANNEL W1C TILE.
# Co-founder ruling 2026-09-12 on F4 (Arlen): the tile has NO Connect button;
# both pilot hosts start the connection on their side and hand the browser to
# us. New string S10 approved as written below. Direction on F1 (orchestrator,
# consistent with the signed contract): drop the /assistant-connect web route;
# the consent flow stays on the API-rendered pages from W1-D. F2 is therefore
# an API fix and runs as its own wave (2026-09-12-w1d-consent-gate.plan).
# ────────────────────────────────────────────────────────────────────────────

1 | devops | Amend docs/prd/pages/assistant-channel.md on branch feat/ac-w1c-tile (PR 2826) to record the co-founder ruling of 2026-09-12 on the tile, nothing else. READ FIRST the critic comment on issue 2340 whose first line reads CRITIC ASSISTANT CHANNEL W1C TILE, finding F4, then sections 6, 7.1 and 9 of the page. CHANGES: in section 7.1 remove the Connect button and the Continuing loading label from the button list, and state, dated 2026-09-12 with the co-founder named as Arlen, that connecting starts inside the host per the section 6 rows and that the tile therefore has no Connect control; specify the not-connected state as the S1 heading, the S2 sub-line and the new S10 sentence; specify that the S8 toast fires when the traveller lands back on the site after consent, not from a tile action. In section 9 add row S10 with the exact text: Start from inside your assistant: add Olympus as a connector there, and it will bring you back here to approve. Mark S10 ACCEPTED, co-founder sign-off 2026-09-12. Do not add a section 7.0 and do not describe any web route for the consent flow; the flow stays on the API-rendered pages. VERIFY: make check-prd-index, exit code pasted. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Commit and push to the same branch. | feat/ac-w1c-tile

2 | web-frontend | Fix PR 2826 on branch feat/ac-w1c-tile per the critic comment on issue 2340 whose first line reads CRITIC ASSISTANT CHANNEL W1C TILE, after wave 1 amended the PRD. READ the comment in full and the amended docs/prd/pages/assistant-channel.md sections 7.1 and 9 first. F1: delete the /assistant-connect route and everything only it used (its copy catalog, view, tests); the consent flow lives on the API-rendered pages and is out of this PR; remove the two code comments that claimed a PRD addition. F3 becomes moot with F1; confirm nothing left reads host or step from the query string. F4: implement the not-connected state exactly as section 7.1 now specifies (S1, S2, S10, no Connect control), the connected row with S3 and the per-host Disconnect dialog (S6, S7) and toast (S9) as already built, and the S8 toast on the return from consent as section 7.1 now places it. F5 to F8 as the critic wrote them: no string outside section 9 (remove or replace the one it found), the one-time-code autofill attribute is not needed once the code screen is gone but check nothing else dropped it, fix the false provenance citation, register the new full-page surface with the shell only if any full-page surface remains (with the route gone, confirm none does). F9 and F10 as nits. VERIFY: npm run lint, typecheck and the unit tests for apps/web with real exit codes; the voice lint on changed files; the E2E spec for the settings tile if one exists; paste exit codes into the PR body and update the PR body to describe the ruling. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Push to the same branch, keep the PR a draft. | feat/ac-w1c-tile

3 | frontend-critic | READ-ONLY RE-REVIEW of PR 2826 on branch feat/ac-w1c-tile after the fix waves. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. Your prior comment on issue 2340 starts CRITIC ASSISTANT CHANNEL W1C TILE; grade F1 to F10 as CLEARED or STILL OPEN with file and line. Then verify against the amended section 7.1 and section 9: every string the tile renders is in section 9 or cited from a ratified page; the not-connected state shows S1, S2 and S10 and no Connect control; the S8 toast fires on the return from consent; the Disconnect dialog is the ratified shape; the PRD amendment is in this PR and names the co-founder and the date. Run the gates you ran in round one and paste exit codes. Post ONE comment on issue 2340 whose first line reads CRITIC ASSISTANT CHANNEL W1C TILE ROUND 2 with SAFE-TO-MERGE or BLOCK-FIX. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/ac-w1c-tile
