# W1-D follow-up: the API-rendered consent page gets the real consent gate. 2026-09-12
#
# DISPATCH: ./scripts/dispatch.sh git@github.com:Arlencho/olympus-platform.git wave-plans/assistant-channel/2026-09-12-w1d-consent-gate.plan --auto --retries 1
#
# From finding F2 of the W1-C critic (issue 2340): the consent screen renders
# S4 and S5 as prose and the allow action is always enabled. The consent flow
# stays on the API-rendered pages (orchestrator direction), so the gate is here.
# ────────────────────────────────────────────────────────────────────────────

1 | devops | Amend docs/prd/pages/assistant-channel.md on branch fix/ac-consent-gate, nothing else, to resolve finding N4 of the round-two tile critic on issue 2340 (first line CRITIC ASSISTANT CHANNEL W1C TILE ROUND 2): section 7.1 still lists S4 (the consent line) and S5 (the money boundary line) as tile elements, but by the co-founder ruling of 2026-09-12 the tile has no consent moment. Move S4 and S5 to a short new subsection of section 7 for the consent screen, stating that the consent screen is the API-rendered page that receives the handoff from the host (W1-D), that it carries the connect-dialog consent-first gate from docs/prd/pages/24-account-concierge.md (unchecked checkbox labelled S4, S5 below it, the primary action disabled until checked), and that section 9 rows S4 and S5 keep their text unchanged. Mark the subsection as a placement correction ratified under the standing delegation on 2026-09-12 with a pointer to issue 2340. VERIFY: make check-prd-index, exit code pasted. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Commit and push. | fix/ac-consent-gate

2 | go-backend | On branch fix/ac-consent-gate (created by wave 1), in apps/api/internal/handler/assistant_oauth.go and its templates, make the consent page carry the real consent gate the contract requires. READ FIRST docs/prd/pages/assistant-channel.md section 7.1 (it reuses the connect dialog consent-first gate) and docs/prd/pages/24-account-concierge.md near lines 106 to 111 and 210 (unchecked by default, the S4 line is the checkbox label, the primary action is disabled and carries aria-disabled until checked, the checkbox is the client-side gate that no token can be minted without informed consent). Implement exactly that on the API-rendered consent page: a real input type checkbox with S4 as its label, S5 below it, the allow button disabled until checked, and, server side, the consent POST refuses (400 with the fixed description) when the consent field is absent, so the gate holds without JavaScript. Keep S4 and S5 verbatim from section 9. VERIFY: go test ./... with the race flag in apps/api, plus a handler test that a POST without the consent field is refused and one with it proceeds; voice lint on the changed file; paste real exit codes. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Open a PR with Refs 2799 and 2340 that includes the wave 1 PRD amendment. Do not merge. | fix/ac-consent-gate

3 | backend-critic | READ-ONLY REVIEW of the PR on branch fix/ac-consent-gate. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. Confirm the PRD amendment in section 7 matches what the page renders, then confirm: the checkbox is unchecked by default, S4 is its label verbatim, the allow button is disabled and aria-disabled until checked, a POST without the consent field is refused server side with the fixed description and the pending request is not consumed by that refusal unless the browser binding is also invalid, and the browser-binding and CSRF checks from W1-D are untouched. Run go test ./... with the race flag and paste the exit code. Post ONE comment on issue 2340 whose first line reads CRITIC CONSENT GATE with SAFE-TO-MERGE or BLOCK-FIX. HOUSE STYLE: no long dash, no AI tool or vendor name. | fix/ac-consent-gate
