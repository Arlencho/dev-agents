# deploy-iris.yml must carry the Redis secret and the VPC connector the code now requires. 2026-09-13
#
# DISPATCH: ./scripts/dispatch.sh git@github.com:Arlencho/olympus-platform.git wave-plans/assistant-channel/2026-09-13-iris-deploy-redis.plan --auto --retries 1
#
# Observed 2026-09-13 09:41 and 09:49 UTC: two Iris deploys of the pilot fix failed
# to boot with config: REDIS_URL must be set when ENVIRONMENT=production. The env
# doc row says deploy-iris.yml sets it with --update-secrets; the workflow does not,
# and it passes no VPC connector, so even with the secret the private Redis is
# unreachable. Fixed by hand on the service (revision 00006): secret olympus-redis-url
# as REDIS_URL, connector olympus-connector, egress private-ranges-only. The
# workflow must carry the same so a fresh deploy or a fresh project boots.
# ────────────────────────────────────────────────────────────────────────────

1 | devops | Branch fix/iris-deploy-redis. In .github/workflows/deploy-iris.yml add to the gcloud run deploy step exactly what deploy-api.yml already passes for the same needs: --update-secrets REDIS_URL=olympus-redis-url:latest, --vpc-connector olympus-connector, --vpc-egress private-ranges-only, with a comment citing the two failed runs (34749946399 and 34750281551) and the hand-applied revision; keep the existing update-env-vars merge semantics. Make docs/operations/env-vars-iris.md row REDIS_URL true to the workflow, and add the connector requirement to the deployment doc section for Iris. Add a pre-deploy assertion in the workflow that the secret exists in the project, failing with a one-line message that names the secret, so the failure is a workflow error and not a container that refuses to boot. VERIFY: actionlint or the workflow lint the repo uses, make check-ci-wiring, python3 scripts/check-env-vars.py if it covers iris, real exit codes pasted. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Open a PR with Refs 2800. Do not merge. | fix/iris-deploy-redis

2 | plan-critic | READ-ONLY REVIEW of branch fix/iris-deploy-redis. No commits, no push, no edits; tmp writes only. Record head SHA at start and at report. Confirm the three flags match deploy-api.yml byte for byte, the pre-deploy assertion fails loudly when the secret is absent (simulate with a wrong name in a dry run), the doc row and the workflow agree, and nothing else in the deploy step changed. Post ONE comment on the PR whose first line reads CRITIC IRIS DEPLOY REDIS with SAFE-TO-MERGE or BLOCK-FIX. HOUSE STYLE: no long dash, no AI tool or vendor name. | fix/iris-deploy-redis
