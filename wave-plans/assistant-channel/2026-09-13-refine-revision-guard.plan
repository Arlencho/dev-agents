# Refine revision guard, issue 2843, and the first trial of the cross-vendor producer seats with first-party critics. 2026-09-13
#
# DISPATCH: ./scripts/dispatch.sh git@github.com:Arlencho/olympus-platform.git wave-plans/assistant-channel/2026-09-13-refine-revision-guard.plan --detach --retries 1
# Dispatch only after the routing PR (feat/routing-grok-producers) has merged and the fleet checkout is pulled; the producer seats must resolve to grok.
# ────────────────────────────────────────────────────────────────────────────

1 | api-designer | Branch feat/ac-refine-revision-guard from origin/main. Read issue 2843 first; it names the gap, the files and the fix. In api.yaml add an optional expected_revision integer to the refine request of POST /result-sessions/{id}/refine, with a 409 response carrying the current record in the same shape as the PATCH predicate from PR 2832; extend docs/prd/pages/assistant-channel.md section 3.4 so revision_conflict covers section 3.3.3 (refine) as well as 3.3.7, dated 2026-09-13 citing issue 2843 and the critic comment on issue 2340 that found it. Run make generate and commit every generated file. VERIFY: make check-api-spec output pasted (it fails until wave 2 adds the handler; say so), make check-prd-index exit 0. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Commit and push. | feat/ac-refine-revision-guard

2 | go-backend | On branch feat/ac-refine-revision-guard after wave 1. In apps/api: the refine handler and service honour expected_revision the way PATCH does (updateAtRevision), answering 409 with the current record on mismatch and refusing an assistant-scope refine that omits it, exactly as PATCH already refuses; web-scope refine without the field keeps its current behaviour. In services/iris: runRefine sends sess.Revision from the locked re-read as expected_revision and maps the 409 to revision_conflict with the current state, the same way update_trip does; keep the settle-on-proof logic exactly. Tests: the critic repro from issue 2843 (a website departure_date edit between the read and the refine POST) now yields revision_conflict and the record keeps the website date, in both windows; assistant refine without expected_revision is refused; web refine without it is unchanged. VERIFY: go test ./... with the race flag in apps/api and in services/iris, make check-api-spec exit 0, real exit codes in the PR body. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Open a draft PR with Closes 2843 and Refs 2800 and 2340, PRD line ticked. | feat/ac-refine-revision-guard

3 | backend-critic | READ-ONLY REVIEW of the PR on branch feat/ac-refine-revision-guard. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. Re-run the issue 2843 repro in both windows on this head; race a website PATCH against an assistant refine; confirm a web-scope refine without the field is byte-identical to main; confirm the assistant scope cannot omit the predicate. Write failing tests where the PR falls short. Post ONE comment on issue 2340 whose first line reads CRITIC REFINE REVISION GUARD with SAFE-TO-MERGE, BLOCK-FIX or BLOCK-ESCALATE and citations, and a last line TRIAL: producer rounds to SAFE so far <n>. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/ac-refine-revision-guard

3 | api-critic | READ-ONLY REVIEW of the api.yaml diff on branch feat/ac-refine-revision-guard. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. Check the request and 409 schemas against the PATCH predicate from PR 2832 and against PRD section 3.4 as amended; the generated client is committed and matches; make check-api-spec exits 0. Post ONE comment on issue 2340 whose first line reads API CRITIC REFINE REVISION GUARD with SAFE-TO-MERGE, BLOCK-FIX or BLOCK-ESCALATE and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/ac-refine-revision-guard
