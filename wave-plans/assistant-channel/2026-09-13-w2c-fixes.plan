# W2-C fix wave after backend-critic BLOCK-FIX on PR 2833 (security SAFE). 2026-09-13
#
# DISPATCH: ./scripts/dispatch.sh git@github.com:Arlencho/olympus-platform.git wave-plans/assistant-channel/2026-09-13-w2c-fixes.plan --auto --retries 1
# Critic comment on issue 2340, first line CRITIC W2C UPDATE AND HOSTS: items 1, 2
# block, 4 and 6 fix in PR, 3 and 5 clean. CI job Iris Build + Test is red.
# ────────────────────────────────────────────────────────────────────────────

1 | db-architect | On branch feat/ac-w2c-update-and-hosts (PR 2833) give the result-session write a revision predicate so a stale writer cannot overwrite a newer one, per item 1 of the critic comment on issue 2340 whose first line reads CRITIC W2C UPDATE AND HOSTS. READ FIRST that item, apps/api/db/queries/result_sessions.sql near lines 48 to 61, the revision column added by migration 20260911080505 (W1-A), and apps/api/db/CLAUDE.md. Deliver: the update queries that write parsed_fields and the selection take an expected revision and bump it in the same statement (WHERE id = $1 AND deleted_at IS NULL AND revision = $expected, RETURNING the new row), a query that reads the current revision, and no behaviour change for callers that do not pass a revision yet (a second query without the predicate stays for the website until it adopts the column; name both clearly). Run sqlc generation; paste real exit codes. Do not edit the service; wave 2 does. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Commit and push. | feat/ac-w2c-update-and-hosts

2 | go-backend | Fix PR 2833 on branch feat/ac-w2c-update-and-hosts per the critic comment on issue 2340 whose first line reads CRITIC W2C UPDATE AND HOSTS, after wave 1. READ IT IN FULL FIRST and add the three critic tests to the suites unchanged apart from package placement. ONE (item 1): update_trip must use the real revision column through the API: the assistant PATCH carries the expected revision, the API service uses the wave 1 predicate query on the assistant path and answers 409 with the current state on a mismatch, Iris maps that to stale_revision with the current record; a write that lands from the website between read and write must therefore raise a conflict, never be overwritten; drop the assistant_trip_revision blob field. TWO (item 2): a linked record must not have its selection written while the tool reports not_available; either refuse the whole PATCH on a linked record before any field is written, or report exactly what landed; the tool result must never say nothing landed when something did. THREE (item 4): the enforcement matrix fixture must wire every service the router registers so the matrix walks all 88 registered routes, not 68; make the critic test that asserts full coverage green. FOUR: the CI job Iris Build + Test is red on the PR head; open the run log, fix the cause, paste the green run. VERIFY: go test ./... with the race flag in services/iris and apps/api, the three critic tests green, real exit codes pasted in the PR body. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Push to the same branch, keep the PR a draft. | feat/ac-w2c-update-and-hosts

3 | devops | On branch feat/ac-w2c-update-and-hosts fix item 6 of the same critic comment in docs/operations/assistant-hosts.md: line 47 must not claim both hosts run as a public client beyond what each cited page says (state it per host with the citation that carries it, or drop the claim); line 62 must not claim the host prompts before update_trip, because the tool declares destructiveHint false and the cited checklist binds prompting to destructiveHint true (state what the annotations actually cause, and note that a prompt would require destructiveHint true, which is a contract decision, not a doc claim); line 74 must carry the same UI disclaimer as line 18. Nothing else. VERIFY: make check-prd-index if in scope; paste the exit code. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Commit and push. | feat/ac-w2c-update-and-hosts

4 | backend-critic | READ-ONLY RE-REVIEW of PR 2833 on branch feat/ac-w2c-update-and-hosts after the fix waves. No commits, no push, no review, no labels, no edits; tmp writes only. Record head SHA at start and at report. Grade items 1, 2, 4 and 6 from your round-one comment CLEARED or STILL OPEN with proof: run your three tests; write the race again against the real revision column (a website write between the assistant read and write must produce 409 then stale_revision with the current state); confirm a linked record PATCH writes nothing when the tool reports not_available; confirm the matrix walks every registered route and paste the count; check the three doc lines against their citations. Re-confirm items 3 and 5 still hold and the CI job Iris Build + Test is green. Post ONE comment on issue 2340 whose first line reads CRITIC W2C UPDATE AND HOSTS ROUND 2 with SAFE-TO-MERGE or BLOCK-FIX and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/ac-w2c-update-and-hosts
