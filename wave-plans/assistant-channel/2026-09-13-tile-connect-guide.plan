# Connected assistants tile: the per-host connection guide. Co-founder sign-off 2026-09-13. 2026-09-13
#
# DISPATCH: ./scripts/dispatch.sh git@github.com:Arlencho/olympus-platform.git wave-plans/assistant-channel/2026-09-13-tile-connect-guide.plan --auto --retries 1
# Strings approved on issue 2340 (comment CO-FOUNDER SIGN-OFF 2026-09-13, connection guide).
# ────────────────────────────────────────────────────────────────────────────

1 | devops | Branch feat/ac-tile-connect-guide. Amend docs/prd/pages/assistant-channel.md: section 7.1 gains the per-host How to connect disclosure on the tile (closed by default, one per host row, numbered steps, the MCP server address and the client id rendered as copyable values with the ratified Copy control), the line under the steps, and the hint on the connected row; section 9 gains the strings exactly as recorded on issue 2340 in the comment whose first line reads CO-FOUNDER SIGN-OFF 2026-09-13, Connected assistants tile: connection guide, as S12 onward, marked ACCEPTED with that date; host A shows the standard Coming soon treatment from docs/prd/01-conventions.md section 15 with the text Coming soon for ChatGPT until its path is walked. State where the two values come from: the MCP address is the public Iris URL plus /mcp and the client id is the slot id the API registers for that host, both served to the web app by the API (name the endpoint if one exists, else specify a small read-only field on the existing bindings envelope for wave 2 to add). VERIFY: make check-prd-index exit 0. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Commit and push. | feat/ac-tile-connect-guide

2 | go-backend | On branch feat/ac-tile-connect-guide after wave 1: expose to the web app, on the existing concierge bindings envelope or the endpoint the PRD amendment names, the two values per host slot the guide renders: the MCP server address (from configuration, never hard-coded) and the client id of the slot; read-only, no secret ever. Add api.yaml entries for the new fields and run make generate; add a handler test. VERIFY: go test ./... with the race flag in apps/api, make check-api-spec, real exit codes. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Push to the same branch. | feat/ac-tile-connect-guide

3 | web-frontend | On branch feat/ac-tile-connect-guide after waves 1 and 2, in apps/web/components/concierge: implement section 7.1 as amended. Each host row gets a How to connect disclosure, closed by default, with the numbered steps verbatim from section 9, the two values in copyable fields using the ratified Copy control and its Copied toast, the line under the steps, and the hint on the connected row; the host A row shows the standard Coming soon treatment. Nothing else on the tile changes. Keyboard reachable, 44 px targets on mobile, works at 375 wide. Tests for: disclosure closed by default and opens, both values render from the API fields and the copy control writes them, the coming-soon row for host A, the connected row hint. VERIFY: npm run lint, typecheck and unit tests for apps/web, the voice lint on changed files, real exit codes in the PR body. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Open a draft PR with Refs 2800 and 2340. | feat/ac-tile-connect-guide

4 | frontend-critic | READ-ONLY REVIEW of the PR on branch feat/ac-tile-connect-guide. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. THE TEST THAT MATTERS: read the tile cold as a first-time tester who has never seen Claude connectors, and say whether you could complete the connection from the tile alone, step by step, without asking anyone; any step that needs outside knowledge is a BLOCK. Then: every rendered string is in section 9 or cited from a ratified page; the two values come from the API and match what the connector actually needs; copy controls work and announce; 44 px targets at 375 wide; disclosure closed by default; host A shows the standard coming-soon treatment; nothing else on the tile regressed against the round-two tile review. Post ONE comment on issue 2340 whose first line reads CRITIC TILE CONNECT GUIDE with SAFE-TO-MERGE or BLOCK-FIX and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/ac-tile-connect-guide
