# Docs that the first live flight booking (2026-09-11) made false. 2026-09-12
#
# DISPATCH: ./scripts/dispatch.sh git@github.com:Arlencho/olympus-platform.git wave-plans/assistant-channel/2026-09-12-live-docs-sweep.plan --auto --retries 2
#
# DOCS ONLY. Four files. Every new sentence must trace to a file, a PR, or the
# log lines pasted on PR 2812. DPA cell stays Not started; the DPA sentence records the 2026-09-12 co-founder decision.
# ────────────────────────────────────────────────────────────────────────────

1 | devops | Correct four docs that the first live production flight booking on 2026-09-11 made false. Change nothing else in them. THE FACTS, with sources you must verify before writing: (a) production flights run on the live Duffel token since 2026-09-11; GET /api/v1/health reports flights_mode production and stays_mode sandbox, both honest token readouts (apps/api/internal/provider/duffel.go near line 222 and duffel_stays.go near line 326). (b) The first live booking was 282d1d31-8915-48e9-806c-c2bf8326f16d, EUR 162.45, flight-only cart, card settlement; the log lines are pasted as a comment on PR 2812 whose first line reads LOG EVIDENCE 2026-09-11; the money model observed there is: traveller pays Olympus by Stripe card with a manual-capture PaymentIntent minted at modal open, Duffel order is created, then the card is captured (order before capture, issue 691), and Duffel is settled from the Olympus Balance, never from the traveller card. (c) The flight-only and hotel-only card surface shipped in PR 2663 on 2026-08-14 (apps/web/lib/chat-flight-checkout.ts near line 496 and chat-stay-checkout.ts near line 344 post settlement card). (d) Duffel Stays remains on the test token by design pending issue 2039. FILE ONE, docs/compliance/SUBPROCESSORS.md, the Duffel row: replace planned, not yet active with active since 2026-09-11 for flights, and replace the sentence Will be activated when #301 lands with a sentence stating flights are live and Stays is pending issue 2039. DPA: keep the status cell at Not started, and replace the sentence DPA required before going live with a dated one recording the co-founder decision of 2026-09-12: the Duffel DPA is not yet in place, live validation of the flights path proceeds first, and the DPA follows as an open compliance item. Add nothing else about the DPA. FILE TWO, docs/COST_INVENTORY.md, the Duffel capabilities block near line 340: rewrite the Flights and Payment bullets to the money model in fact (b); keep the Hotels bullet as is since Stays is still pending; keep the streaming bullet. FILE THREE, docs/prd/pages/atlas-chat.md, the balance-variant row of the onConfirm table near line 3442: the sentence that says the flight-only / hotel-only CARD journey is UNBUILT and needs its own spec and sign-off is contradicted by the CARD row a few lines below (the row citing issue 2302) and by production; replace it with one sentence stating that the card journey for both intents shipped in PR 2663 and is specified in the CARD rows below, dated 2026-09-12 in the same amended-on style the row already uses. Do not touch any other cell. FILE FOUR, docs/operations/duffel-balance-funding.md near line 52: the sentence about the CEO pre-funding the London demo window is stale and names a role the company does not use; replace it with one dated sentence that the Balance was funded on 2026-09-11 after Duffel cleared KYC and that the first live booking drew from it. Do not state amounts. VERIFY: make check-prd-index; git diff must show exactly those four files; paste real exit codes. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name in the doc, commit or PR text, no Co-Authored-By trailer. Open a PR with Refs 2646 and 2106 and a one-line note that the atlas-chat change is a behaviour correction ratified under the standing delegation logged on issue 2340. Do not merge. | docs/live-booking-docs-sweep

2 | plan-critic | READ-ONLY REVIEW of branch docs/live-booking-docs-sweep. No commits, no push, no edits, no labels; tmp writes only. Record head SHA at start and at report. THE QUESTION THAT MATTERS: does every new sentence trace to something real. For each changed sentence, name the source that makes it true: a file and line on main, a merged PR number, the LOG EVIDENCE 2026-09-11 comment on PR 2812, or the /api/v1/health response you fetch yourself. A sentence with no source is a BLOCK. Also check: the DPA status cell in the SUBPROCESSORS Duffel row still reads Not started and the replacement DPA sentence records only the 2026-09-12 co-founder decision (not in place, live validation first, DPA follows) with no promise of a date; no amounts appear in the balance funding doc; the atlas-chat CARD row citing issue 2302 is untouched; exactly four files changed; no long dash, no vendor name, no trailer. Post ONE comment on issue 2340 whose first line reads CRITIC LIVE DOCS SWEEP, with SAFE-TO-MERGE or BLOCK-FIX and line citations. | docs/live-booking-docs-sweep
