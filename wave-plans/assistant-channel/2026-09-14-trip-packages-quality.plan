# Trip packages: check-in follows arrival, and packages do not all share one flight. Issue 2848. Money gate criterion 3 needs a shortlist a tester would book. 2026-09-14
#
# DISPATCH: ./scripts/dispatch.sh git@github.com:Arlencho/olympus-platform.git wave-plans/assistant-channel/2026-09-14-trip-packages-quality.plan --detach --retries 1
# AFTER: wave-plans/assistant-channel/2026-09-14-refine-revision-guard-fix2.plan
# TIER: B
# ────────────────────────────────────────────────────────────────────────────

1 | go-backend | Branch fix/2848-trip-packages from origin/main. Read issue 2848 first; it names the record, the observed packages and the lines. In apps/api/internal/service/agent.go: ONE, the stay check-in for a package follows the arrival date of the flight it is paired with, not the departure date, and nights are recomputed; a flight that lands on a later calendar day moves check-in to that day. TWO, package composition: the top packages do not all share one flight when the ranked flights hold other options of comparable price; at minimum the top packages span the best two distinct flights, and a nonstop within a stated price margin of the top flight is represented. Write the PRD sentence for both rules into docs/prd/pages/06-conversation-results.md next to the package rules, marked PROPOSED, never ACCEPTED. Tests: the 2026-10-05 overnight arrival case from the issue produces check-in 2026-10-06; five packages over a ranked list containing a nonstop and a one-stop are not all one flight; a same-day arrival keeps check-in on the departure date. VERIFY: go test with the race flag on apps/api/internal/service only, real exit code in the PR body; state in the PR body whether the web trip flow shows the same packages. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Open a draft PR with Closes 2848 and Refs 2800. | fix/2848-trip-packages

2 | backend-critic | READ-ONLY REVIEW of the PR on branch fix/2848-trip-packages. Tier B, round 1 of at most 3. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. Check the arrival rule across time zones and a flight arriving after midnight local time, the nights count, the variety rule on a list where every option is one-stop, and that ranking order is otherwise unchanged. Post ONE comment on issue 2340 whose first line reads CRITIC TRIP PACKAGES with SAFE-TO-MERGE, BLOCK-FIX or BLOCK-ESCALATE and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | fix/2848-trip-packages
