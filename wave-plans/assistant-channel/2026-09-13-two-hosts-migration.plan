# Two assistant hosts per traveller: widen the channel_bindings unique index. Issue 2825. 2026-09-13
#
# DISPATCH: ./scripts/dispatch.sh git@github.com:Arlencho/olympus-platform.git wave-plans/assistant-channel/2026-09-13-two-hosts-migration.plan --auto --retries 1
# Needed before the Connected assistants tile shows two rows (decision D4).
# ────────────────────────────────────────────────────────────────────────────

1 | db-architect | Implement issue 2825 on branch feat/ac-two-hosts, apps/api/db only. READ FIRST the issue, apps/api/db/migrations/000033_channel_bindings_and_source.sql near lines 71 to 83 (the two partial unique indexes), apps/api/db/CLAUDE.md, the W1-D binding identity (external_id is now per traveller per host on the assistant channel, see apps/api/internal/service/assistant_auth.go), and the store queries in apps/api/db/queries that touch channel_bindings. Deliver one timestamped migration that keeps one active binding per (channel, user_id) for every channel except assistant, and for the assistant channel allows one active binding per (channel, user_id, external_id), with the index expressions written so Telegram behaviour is byte-for-byte unchanged; adjust the sqlc queries that implement the replace flow so that on the assistant channel the replacement revokes only the binding with the same external_id, never the other host; regenerate sqlc; add the migration down. VERIFY: sqlc generation clean, the migration filename gate, and the Postgres integration test target if it exists locally; paste real exit codes. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Open a draft PR with Closes 2825 and Refs 2800. | feat/ac-two-hosts

2 | go-backend | On branch feat/ac-two-hosts after wave 1, wire the service side: CreateBindingReplacingActive and its memory mirror, RevokeForUser and the concierge disconnect path must use the per-host identity on the assistant channel so that a traveller can hold ChatGPT and Claude at once, connecting host B does not revoke host A, and disconnecting one leaves the other; the critic probe from W1-D round two (TestProbe_OneTravellerTwoHosts on issue 2340) becomes a permanent green test, and a Telegram test proves that channel still replaces. Update the comment near apps/api/internal/service/assistant_auth.go line 713 that cites issue 2825 to say it is resolved. VERIFY: go test ./... with the race flag in apps/api, real exit codes in the PR body. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Push to the same branch. | feat/ac-two-hosts

3 | database-critic | READ-ONLY REVIEW of the migration and queries on branch feat/ac-two-hosts. No commits, no push, no edits; tmp writes only. Record head SHA at start and at report. Apply the migration up and down against a scratch Postgres (testcontainers or a local instance), then prove with real rows: two active assistant bindings for one user with different external_id succeed; a third with a duplicated external_id is refused by the index; a second active Telegram binding for one user is still refused; the replace flow on the assistant channel revokes only the same external_id; the down migration restores the original index exactly. Post ONE comment on issue 2340 whose first line reads DATABASE CRITIC TWO HOSTS with SAFE-TO-MERGE or BLOCK-FIX and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/ac-two-hosts

4 | backend-critic | READ-ONLY REVIEW of the service changes on branch feat/ac-two-hosts. No commits, no push, no edits; tmp writes only. Record head SHA at start and at report. Run the two-hosts probe and the Telegram replace test; then attack it: connect host A, connect host B, refresh both, disconnect A, refresh B (must work), refresh A (must be refused), and check the middleware liveness still keys on binding_id. Post ONE comment on issue 2340 whose first line reads CRITIC TWO HOSTS with SAFE-TO-MERGE or BLOCK-FIX. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/ac-two-hosts
