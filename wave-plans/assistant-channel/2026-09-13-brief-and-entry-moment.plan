# Assistant channel: the brief is honoured (issue 2847) and the host calls Olympus only at the booking moment (co-founder sign-off 2026-09-13 22:30, issue 2340). 2026-09-13
#
# DISPATCH: ./scripts/dispatch.sh git@github.com:Arlencho/olympus-platform.git wave-plans/assistant-channel/2026-09-13-brief-and-entry-moment.plan --detach --retries 1
# Dispatch after the routing PR has merged and the fleet checkout is pulled, so the producer seats resolve to grok.
# ────────────────────────────────────────────────────────────────────────────

1 | devops | Branch feat/ac-brief-and-entry from origin/main. Amend docs/prd/pages/assistant-channel.md per the comment on issue 2340 whose first line reads CO-FOUNDER SIGN-OFF 2026-09-13 22:30: section 1 gains one sentence that the host plans in its own voice and Olympus is called only when the traveller asks to book or accepts the host offer to book through Olympus; add section 7.6, When the host calls Olympus, stating the two triggers, that every tool description carries the rule in the manifest, that the search tool is never a substitute for the host asking its own questions, and the manifest wording from that comment quoted verbatim for search and the one-line rule for the other tools. Date it, name the co-founder, cite the comment. VERIFY: make check-prd-index exit 0. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Commit and push. | feat/ac-brief-and-entry

2 | go-backend | On branch feat/ac-brief-and-entry after wave 1, in services/iris: ONE, issue 2847, read it first: search accepts brief by passing it to the API parse path the web uses for the homepage prompt (POST /api/v1/parse) and merging the parsed fields under the precedence rule (an explicit structured field always wins, the brief only fills absent fields), keeping the section 3.1 scan (a brief carrying a forbidden field is refused with invalid_input and nothing is stored); refine accepts brief by passing it as the prompt of POST /result-sessions/{id}/refine under the same precedence rule, and a brief that is not a narrowing is intent_change with the hint as today; remove the brief_not_supported refusal. TWO, the manifest: every tool description carries the wording from PRD section 7.6 verbatim (search gets the full paragraph, the six others the one-line rule); annotations unchanged. Tests: the exact brief from the 2026-09-13 journey (nonstop flights, mid-range central hotels) narrows a trip search and the structured fields it does not name are untouched; a brief that states a date the host also stated keeps the host value; a brief with a date of birth is refused and the record is unchanged; a brief adding a stay to a flights-only search is intent_change; the manifest carries the section 7.6 text for every tool. VERIFY: go test ./... with the race flag in services/iris, real exit codes in the PR body; paste the served manifest description of search. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Open a draft PR with Closes 2847 and Refs 2800 and 2340, PRD line ticked. | feat/ac-brief-and-entry

3 | backend-critic | READ-ONLY REVIEW of the PR on branch feat/ac-brief-and-entry. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. THE QUESTION THAT MATTERS: can a brief ever override what the host stated, and can a brief smuggle a forbidden field or a supplier id into the record. Probe: brief and structured field disagree on dates, origin, party; brief carries a date of birth, a passport number, a Duffel offer id; brief over 1000 characters; brief on refine that widens instead of narrows. Then the manifest: every tool carries the section 7.6 text, no tool description contradicts it. Post ONE comment on issue 2340 whose first line reads CRITIC BRIEF AND ENTRY with SAFE-TO-MERGE, BLOCK-FIX or BLOCK-ESCALATE and citations, and a last line TRIAL: producer rounds to SAFE so far <n>. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/ac-brief-and-entry

3 | security-reviewer | SECURITY REVIEW of the PR on branch feat/ac-brief-and-entry. Read-only; tmp writes only. Record head SHA at start and at report. The change lets free text from a third-party host reach our parse and refine paths. Check: the section 3.1 scan runs before anything is stored or sent to a supplier; the parse call is bounded (length, rate, quota per binding); a brief cannot name the record of another traveller; log lines never carry the brief text; the refusal reveals nothing beyond the code. Post ONE comment on issue 2340 whose first line reads SECURITY BRIEF AND ENTRY with a final SAFE or BLOCK line. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/ac-brief-and-entry
