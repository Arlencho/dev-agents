# Calliope C1 v2 - two live unmarshal shapes. Issue #2169, epic #2168.
# v1 was REVISE: it required RED on brief shapes already fixed by #2107 (2026-08-05).
# Scope below is probe-verified against main d812e04b. Briefs are GREEN regression
# guards, not RED gates. Tests live in apps/api/internal/model/.
# QA test-first: wave 1 must be RED on the two residual shapes before wave 2 runs.
1 | test-engineer | ADD failing tests for issue #2169 in apps/api/internal/model/ on branch feat/calliope-c1-tolerant-unmarshal. Two RED cases only. RED case one: TripIntent with unsupported_preferences set to the string "null" currently fails with cannot unmarshal string into Go struct field .tripIntentJSON.unsupported_preferences of type model.UnsupportedPreferences; cover "null", empty string and JSON null. RED case two: TripIntent with fields.locked_airline_iata set to an array such as SK and A3 currently fails with cannot unmarshal array into Go struct field ParsedFields.tripIntentJSON.fields.locked_airline_iata of type string; the test must assert the resulting LockedAirlineIATAs SET equals the input carriers, that a lone string dual-writes the scalar plus a set of one, and that more than five carriers is capped per docs/prd/pages/atlas-chat.md section 6.0 - asserting only that err is nil is insufficient and will be rejected by wave 3. Fixtures may be synthetic provided each reproduces one of those two exact error strings; do not require capturing production logs. ALSO add GREEN regression guards asserting hotel_brief, flight_brief and itinerary_brief set to the string "null" continue to parse, alongside the existing TestTripIntent_UnmarshalTolerantBriefs; these must NOT be forced RED because #2107 already fixed them. Add one guard that a malformed OBJECT with wrong field types still errors. Write no production code. Read issue #2169 first. | feat/calliope-c1-tolerant-unmarshal
2 | go-backend | IMPLEMENT the two residual unmarshal fixes for issue #2169 on branch feat/calliope-c1-tolerant-unmarshal so the wave 1 RED cases turn GREEN and every wave 1 guard stays GREEN. Give UnsupportedPreferences a tolerant UnmarshalJSON mirroring the existing AgentBrief contract in apps/api/internal/model/trip_intent.go: accept the string "null", the empty string and JSON null as an empty value, while a malformed OBJECT with wrong field types still errors. Make the scalar key locked_airline_iata accept an array and route it into LockedAirlineIATAs as a capped set of at most five per docs/prd/pages/atlas-chat.md section 6.0, with a lone string dual-writing the scalar plus a set of one; do NOT join the array or take the first element into the scalar, because that yields a passing parse and a silently broken airline filter downstream of EffectiveAirlineSet. Do NOT re-touch AgentBrief tolerance beyond adding a single log line per tolerated coercion. Do not modify the wave 1 tests. Open a PR and link issue #2169. Read issue #2169 first. | feat/calliope-c1-tolerant-unmarshal
3 | backend-critic | CRITIC ONLY on the PR for branch feat/calliope-c1-tolerant-unmarshal issue #2169. Confirm the airline contract test asserts the resulting SET and the cap rather than merely that err is nil, that the array is not coerced into the scalar, that AgentBrief tolerance from #2107 is unchanged apart from added logging, that a malformed object still errors, and that the brief regression guards were not forced RED. Executable findings only. Output VERDICT APPROVE or REVISE in handoff. Comment on the PR. Do not re-implement. | feat/calliope-c1-tolerant-unmarshal
