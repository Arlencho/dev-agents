# Wave plan for dev-agents — 2026-09-25T21:43:11Z
# Generated by dispatch.sh

1 | devops | Branch fix/dispatch-success-and-caps (existing PR 126): rebase on main. Blocking review finding: in scripts/vendor-auth-check.sh run_check (around lines 463 to 471) an active <vendor>.credit-until returns 1 with status out-of-credit and the main loop (around 505 to 512) sets FAIL=1, so scripts/dispatch.sh (around 1010 to 1014) aborts the whole dispatch before resolve_provider runs, which makes the credit failover unreachable on the default path. Make out-of-credit non-fatal in the preflight: still report the status in the JSON and the human output, but do not set FAIL, since the dispatcher already skips that vendor via credit-until. Clear the credit-until file when a fresh probe for that vendor succeeds, so a top-up is picked up without a manual delete, and update the README line that tells operators to delete it. Flip the case "preflight reports exhausted credit" in run-delivery-caps-tests.sh to expect exit 0 with the out-of-credit status still present, and add a case that a successful probe clears the file. Also guard provider_cooling against a non-numeric credit-until value with a numeric regex check. Run run-delivery-caps-tests.sh and run-failover-tests.sh until green. Push to the same branch. No em dash anywhere. No AI vendor name in code, commits or PR text. | fix/dispatch-success-and-caps
