# Wave plan for dev-agents — 2026-09-12T16:47:56Z
# Generated by dispatch.sh

1 | devops | Fix branch feat/floor-queue (PR 68) per the comment on the PR whose first line reads FRONTEND CRITIC FLOOR QUEUE: BLOCK-FIX. READ IT IN FULL FIRST, including the repro steps. Three items, one commit each, page sources only (templates/experience/floor.js, templates/experience/site.css, the Floor renderer in scripts/experience_build.py; site/ is build output). ONE, the block: the Landed today note is a variable-length data-driven string placed in the nowrap cardhead slot, so any live dispatch pushes the page past 400px; move it under the head as a muted paragraph exactly like the Up next note already does, on both the server-rendered and the client paths, and re-run the critic repro to assert document scrollWidth equals clientWidth at 400 wide with at least one dispatch live today; paste the two numbers. TWO, advisory one: the still live count in the Landed today head must wear the watermark like every other live claim: when the projection is stale or offline, render it qualified (for example, still live at last event) or omit it, using the state renderAll already holds. THREE, advisory two: the elapsed ticker must stop when the projection is stale or offline or the fetch fails, freezing at the last known value the way the existing lanes do. VERIFY: make experience, the repro at 400x900 with numbers pasted, the stale and offline fixtures showing the head and the ticker frozen, bash -n and python3 -m py_compile on touched files, real exit codes. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Push to the same branch. | feat/floor-queue
2 | frontend-critic | READ-ONLY RE-REVIEW of the Floor page on branch feat/floor-queue (PR 68) after the fix wave. No commits, no push, no edits; tmp writes and the gitignored site build only. Record head SHA at start and at report. Re-run your own repro from round one exactly: make experience, a projection with three queued plans, two live seats in two dispatches with activity lines, three landings today including one failed, at viewport 400x900; assert scrollWidth equals clientWidth and paste both numbers. Then age the projection to stale and to offline and confirm the still live count is qualified or gone and the elapsed ticker is frozen, with the measured values pasted. Re-confirm everything that passed in round one still passes. Post ONE comment on the PR whose first line reads FRONTEND CRITIC FLOOR QUEUE ROUND 2 with SAFE-TO-MERGE or BLOCK-FIX and line citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/floor-queue
