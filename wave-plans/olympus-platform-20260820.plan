# Wave plan for olympus-platform — 2026-08-19T22:47:40Z
# Generated by dispatch.sh

1 | devops | Two changes on branch ci/e2e-aptget-hang, updating PR 2721 in place. ITEM ONE, the blocker, one line. scripts/ci/install-playwright-deps.sh has a reap_apt function around lines 55 to 60 that kills processes matching apt-get. During a real mirror outage the process actually stuck on the network is NOT apt-get, it is the apt fetch helper at /usr/lib/apt/methods/http, which has no apt-get in its cmdline and therefore survives the reap. The reviewer measured it surviving more than 122 seconds after the reap. That matters because the survivor still holds the step inherited stdout, and the runner reads step stdout to EOF, so the step does not complete when the script exits. The chain is: mirror black-holes, timeout kills node, reap kills apt-get but not the helper, script exits 0 at about 149 seconds, step stays open on the pipe, the 3 minute step cap fires at 180 seconds, the step FAILS, and the E2E step that follows is skipped because it runs under the default success condition. Slack between 149 and 180 seconds is 31 seconds against a measured survivor of more than 122 seconds. Fix it by also reaping the helper, for example adding alongside the existing pkill a line that kills processes matching /usr/lib/apt/methods with SIGKILL, absorbing a non-match with the usual guard so a clean run is unaffected. If you prefer the belt-and-braces route instead, redirect the apt attempt so descendants never inherit the step pipe; either is acceptable, but say which you chose and why. Then re-state the worst case honestly in the header comment so a reader is not left believing the slack is 31 seconds. ITEM TWO, a comment correction, no behaviour change. The comment at .github/workflows/ci.yml around lines 1042 to 1045 justifies the 4 minute cap with the arithmetic 2.5 plus 4 plus 5 equals about 11.5, but it omits the 3 minute cap the same commit adds 34 lines below. The true sum of caps is 2.5 plus 4 plus 3 plus 5 which is 14.5 and exceeds the job ceiling of 12. The reviewer was fair about why this rarely bites, because the download step is fatal so a full 4 minute download kills the job at about 6.5 minutes and never reaches the rest, and the realistic bad run is about 11 minutes. Do NOT raise timeout-minutes 12; the surrounding comment forbids it twice and the reviewer confirmed no timeout was raised in this diff. Just correct the arithmetic in the comment so it stops asserting a fit it does not have, and state the realistic worst case alongside it. VERIFY BY MEASUREMENT, not by reading: reproduce a stuck helper, run the script, and show that after your change no /usr/lib/apt/methods process survives and the script releases stdout promptly. Paste the before and after process listings and the elapsed times. Do NOT change the fatal browser-download step, the non-fatal design, the path filter, or the measured font-package finding, all of which the reviewer verified as correct. Push to the existing branch. Do not open a new PR. Do not merge. | ci/e2e-aptget-hang
2 | devops | READ-ONLY VERIFY of the wave 1 change on PR 2721, branch ci/e2e-aptget-hang. No commits, no push, no gh pr review, no gh pr merge, no labels. Permitted writes are a /tmp worktree only. Record the PR head SHA at start and re-check at report. ONE QUESTION ONLY, and answer it by measurement rather than by reading the diff: with a black-holed apt mirror, does any process matching /usr/lib/apt/methods survive the reaper, and does the script release the step stdout promptly enough that the step completes well inside its 3 minute cap. Reproduce the stuck-helper scenario, show the process listing before and after the reap, and show elapsed time from script start to stdout EOF. State the resulting slack in seconds against the 180 second cap. If the helper still survives, or if stdout is still held past the cap, that is BLOCK-FIX and the original finding stands. SECOND, much smaller: confirm the ci.yml comment arithmetic now matches the caps actually present, that timeout-minutes 12 was not raised anywhere in the diff, and that the fatal browser-download step, the non-fatal apt design and the path filter are unchanged from the version already reviewed. Post ONE comment on issue 2340 whose first line reads CRITIC PR 2721 REAP, with a verdict of SAFE-TO-MERGE or BLOCK-FIX or BLOCK-ESCALATE and file:line citations plus your measured numbers. No vendor names, emails, booking ids or gcloud payloads. | ci/e2e-aptget-hang
