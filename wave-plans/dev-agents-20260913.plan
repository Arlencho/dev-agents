# Wave plan for dev-agents — 2026-09-13T20:26:07Z
# Generated by dispatch.sh

1 | web-frontend | On branch feat/orchestrator-loop (PR 82), Python in scripts/queue_loop.py: fix the three findings of the comment on PR 82 whose first line reads CRITIC ORCHESTRATOR LOOP ROUND 4: BLOCK-FIX, reproducing each first. ONE, a SAFE comment that names no 40-character commit is not bound to any head and therefore never counts for landing; the runner writes a stop saying the critic did not record the head. TWO, a check suite whose status is still IN_PROGRESS is not green whatever its completed jobs say; only a completed suite with a success conclusion on the head counts. THREE, after marking a draft ready the runner re-reads the checks and the merge state on the head before landing, never the pre-ready rollup. Add a test per finding, run the independent fixture scripts of the critic from rounds 2, 3 and 4 before and after and paste all three in the PR body under a Round 5 heading. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Commit and push. | feat/orchestrator-loop
2 | plan-critic | READ-ONLY REVIEW ROUND 5 of PR 82 on branch feat/orchestrator-loop. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. Re-run every fixture from rounds 2, 3 and 4 and confirm all are closed. Then answer one more question explicitly in your comment: is the landing surface now exhausted, meaning every input the landing decision reads (critic comments, head SHA, check suites, merge state, draft flag, branch protection answer) has a fixture that proves the wrong value cannot land; list any input without one. Post ONE comment on PR 82 whose first line reads CRITIC ORCHESTRATOR LOOP ROUND 5 with SAFE-TO-MERGE, BLOCK-FIX or BLOCK-ESCALATE and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/orchestrator-loop
