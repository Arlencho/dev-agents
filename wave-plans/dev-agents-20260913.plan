# Wave plan for dev-agents — 2026-09-13T20:35:01Z
# Generated by dispatch.sh

1 | web-frontend | Branch feat/floor-needs-you-superseded from origin/main. Read issue 86 first. In scripts/desk_live.py and the page renderer: ONE, a failed_dispatch item is dropped from needs_you when a later dispatch exists today for the same plan file, or for a plan whose header carries FIX-ROUND naming it or whose filename is the same stem with a fix suffix, or when a dispatch on the same branch ended landed or success later, or when the branch has merged (gh, optional, unverified when skipped); the dropped item moves to the details fold as superseded by <plan> so nothing is hidden, and the strip failed and aborted counts keep counting them (they happened) while needs you counts only what is still open. TWO, the row text no longer repeats the repo: the repo chip is rendered once by the row, the item text starts at the plan purpose. THREE, the NOW card of a critic seat shows its own task line, not the wave 1 line (seat_progress task_line lookup keyed by seat, not by wave). Tests: the eleven rows from the 2026-09-13 streams (copy the real live.json into a fixture) reduce to zero open items and eleven superseded entries in the fold; a failed dispatch with no later round stays; a row renders the repo once; a critic seat card shows its own line. VERIFY: the desk and page test suites with real exit codes in the PR body; rebuild with make experience and paste the needs_you count from a --once run against the real streams. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Open a draft PR titled Floor: superseded dispatches leave NEEDS YOU, Closes 86. | feat/floor-needs-you-superseded
2 | plan-critic | READ-ONLY REVIEW of the PR on branch feat/floor-needs-you-superseded. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. THE QUESTION THAT MATTERS: can the superseded rule hide a failure that still needs a person. Try: a fix round that itself failed (must stay open, the newest failure shows); a later dispatch of the same plan that is still running (the old failure is superseded, the running seat shows in NOW); a merged branch whose last dispatch failed (superseded by the merge, with the merge named); gh absent (the merge rule is skipped and says so, the plan and branch rules still apply). Confirm the strip counts still include superseded failures and needs you does not. Post ONE comment on the PR whose first line reads CRITIC FLOOR NEEDS YOU with SAFE-TO-MERGE, BLOCK-FIX or BLOCK-ESCALATE and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/floor-needs-you-superseded
