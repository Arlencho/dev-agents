# Wave plan for dev-agents — 2026-09-13T19:41:56Z
# Generated by dispatch.sh

1 | web-frontend | On branch feat/orchestrator-loop (PR 82), Python in scripts/queue_loop.py and scripts/desk_live.py: fix the three findings of the comment on PR 82 whose first line reads CRITIC ORCHESTRATOR LOOP ROUND 3: BLOCK-FIX, reproducing each with its fixture first. ONE, a critic comment whose first line carries no verdict is not a verdict, whatever a later body line says; the landing rule reads first lines only. TWO, an assigned critic seat is matched to a comment by its exact stem, never by a shared word with the plan filename; an unmatched seat is silent and blocks landing. THREE, a SAFE is bound to the head SHA it names (the comment records head at report); a comment recorded on an earlier head does not count after the head moves, so a push after SAFE returns the PR to waiting for critics. Add a test per finding and keep every existing test green; run the independent fixture script of the critic before and after and paste both in the PR body under a Round 4 heading. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Commit and push. | feat/orchestrator-loop
2 | plan-critic | READ-ONLY REVIEW ROUND 4 of PR 82 on branch feat/orchestrator-loop. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. Re-run every fixture from rounds 2 and 3 and confirm all are closed; try two more ways to make the runner land wrongly. Post ONE comment on PR 82 whose first line reads CRITIC ORCHESTRATOR LOOP ROUND 4 with SAFE-TO-MERGE, BLOCK-FIX or BLOCK-ESCALATE and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/orchestrator-loop
