# Wave plan for dev-agents — 2026-09-13T21:15:03Z
# Generated by dispatch.sh

1 | web-frontend | On branch feat/floor-needs-you-superseded (PR 89), Python in scripts/desk_live.py: fix the four findings of the comment on PR 89 whose first line reads CRITIC FLOOR NEEDS YOU BLOCK-FIX, following its fix line: ONE, a later fix-round plan supersedes only when its stem or branch names the row, never by a token subset of the header (keep one-letter track tokens); TWO, the merge rule requires merged_at later than the row ended_at or started_at, same repo; THREE, the same-plan rule filters by repo; FOUR, a later dispatch of the same plan with only a dispatch_start and no seat does not fold the failure until a seat exists (or the row stays with a note that a re-dispatch is starting). Add the four critic fixtures (H, B2 and the two others named in the comment) as tests and keep every existing test green. VERIFY: the desk test suite with real exit codes in the PR body under a Round 2 heading. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Commit and push. | feat/floor-needs-you-superseded
2 | plan-critic | READ-ONLY REVIEW ROUND 2 of PR 89 on branch feat/floor-needs-you-superseded. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. Re-run the four round 1 fixtures and confirm each is closed; try a fix-round plan for a different track with a near-identical title, and a merge that predates the failed row. Post ONE comment on PR 89 whose first line reads CRITIC FLOOR NEEDS YOU ROUND 2 with SAFE-TO-MERGE, BLOCK-FIX or BLOCK-ESCALATE and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/floor-needs-you-superseded
