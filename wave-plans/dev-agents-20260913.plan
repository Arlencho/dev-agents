# Wave plan for dev-agents — 2026-09-13T13:34:10Z
# Generated by dispatch.sh

1 | web-frontend | Branch feat/floor-v3b from origin/feat/floor-v3a (the v3-A data branch, PR 77; its live.json schema is documented in docs/experience-data.md, read it first, then docs/proposals/floor-v3-purpose.md section 4, which is the requirement). Rebuild the Ops Floor page, templates/experience/floor.js and templates/experience/site.css with the renderer in scripts/experience_build.py, in exactly this order top to bottom: 4.1 status strip (running, up next, landed, failed, needs you, last event; each figure links to its section; needs you red when non-zero; under stale or offline the strip says so first in words); 4.2 NEEDS YOU (one row per item newest first, one action each, empty state Nothing needs you); 4.3 NOW grouped by repo (keep the seat cards from PR 74 as they are); 4.4 UP NEXT (queue by position, repo first, issue, purpose, plan file dim, a blocked plan shows its reason in place); 4.5 INITIATIVES (one row per open milestone from the v3-A data, waves landed of planned, open issues, last landed PR, exit sentence; when gh is unavailable the row shows what streams alone prove and says so); 4.6 LANDED and FAILED today (failed first when non-empty); 4.7 one details control at the bottom, closed by default, holding replay, scrubber, event tail, stream paths, schema line and trail. Plain words, no ids in headers, every number a link to its source. Keep every honesty rule in section 6: queued never renders running, stale and offline degrade every element, replay never claims live, no prompt or path or secret reaches the page. One screen at 1280, one scroll at 400, no horizontal scroll, visible keyboard focus. Tests in the existing page test file: a fixture with two repos live and one critic block renders the strip counts, the NEEDS YOU row with its action, both repo groups, the queue with a blocked reason, an initiatives row, and the details closed; the stale fixture puts the stale sentence before any number; the replay fixture shows the watermark and an empty NEEDS YOU. VERIFY: make experience builds clean and the page tests pass, real commands and exit codes in the PR body; include one screenshot at 1280 and one at 400 from the fixture. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Open a draft PR against feat/floor-v3a titled Floor v3-B: the page, Refs 72. | feat/floor-v3b
2 | frontend-critic | READ-ONLY REVIEW of the PR on branch feat/floor-v3b. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. THE ACCEPTANCE TEST, from docs/proposals/floor-v3-purpose.md section 7: a reader who has never seen the page answers all five questions of section 1 from the page alone, at 1280 and at 400, with two repos live and one BLOCK outstanding. Build that fixture yourself in tmp, render the page, and answer the five questions in your comment with the exact element that answered each; any question you cannot answer from the page is a BLOCK. Then attack the honesty rules: a queued plan that renders as running, a stale stream that shows a live count, a replay that hides its watermark, an id in a header, a path or prompt in the DOM, horizontal scroll at 400, a focus ring missing on any link. Post ONE comment on the PR whose first line reads CRITIC FLOOR V3B with SAFE-TO-MERGE or BLOCK-FIX and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/floor-v3b
