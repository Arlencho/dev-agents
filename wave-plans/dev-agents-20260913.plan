# Wave plan for dev-agents — 2026-09-13T21:14:13Z
# Generated by dispatch.sh

1 | web-frontend | On branch feat/orchestrator-loop (PR 82), Python in scripts/queue_loop.py: ONE, landing is behind a switch, QUEUE_LOOP_LAND, default off; when off the runner never calls gh pr ready, gh pr merge or land.sh, and a PR whose critics are all SAFE with green checks on the head becomes a stop row of type ready_to_merge with the action merge, so the Floor NEEDS YOU shows it; the docs state that landing is off until the landing gate is proven. TWO, fix the two findings of the comment on PR 82 whose first line reads CRITIC ORCHESTRATOR LOOP ROUND 5: BLOCK-FIX so the switched-on path is correct when it is enabled later: after gh pr ready the runner re-runs the named rollup on the head before landing, and an empty headRefOid never lands and never binds a SAFE. Tests: with the switch off, a fully green PR produces the stop row and no gh call that writes; with the switch on, the two round 5 fixtures pass; every earlier fixture from rounds 2 to 4 stays green. Run the independent fixture scripts of the critic from rounds 2 to 5 before and after and paste all in the PR body under a Round 6 heading. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Commit and push. | feat/orchestrator-loop
2 | plan-critic | READ-ONLY REVIEW ROUND 6 of PR 82 on branch feat/orchestrator-loop. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. THE QUESTION THAT MATTERS: with QUEUE_LOOP_LAND unset, can the runner ever write to GitHub. Grep and trace every gh call and every land.sh call; run the fully green fixture and assert no write; confirm the stop row appears. Then confirm the memory guard, AFTER, the single fix round and the stops file fixtures from rounds 2 to 5 all hold. Post ONE comment on PR 82 whose first line reads CRITIC ORCHESTRATOR LOOP ROUND 6 with SAFE-TO-MERGE, BLOCK-FIX or BLOCK-ESCALATE and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/orchestrator-loop
