# Wave plan for dev-agents — 2026-09-13T12:56:17Z
# Generated by dispatch.sh

1 | devops | On branch feat/floor-v3a (PR 77), fix the four findings in the comment on PR 77 whose first line reads CRITIC FLOOR V3A BLOCK-FIX; read that comment first, it cites lines in scripts/desk_live.py and docs/experience-data.md and gives the fixtures. ONE, quiet_seat: never emit it when the stream own age is at or past offline_after_s, and a seat with no close-out is not running once its stream is offline; project it as unknown the same way a dispatcher close-out does. TWO, verdict parsing: the first line uses the same start-of-token rule as body lines; a verdict quoted mid-sentence never counts; a heading word BLOCK or SAFE never steals BLOCK-FIX or SAFE-TO-MERGE. THREE, missing_variable when the gh lookup is skipped: do not add the item; the needs_you_meta check row is the record; summary.needs_you counts verified items only. FOUR, fallback initiatives rows carry epic_title and exit_lookup exactly as docs/experience-data.md documents them. Add a test per finding using the critic fixtures (offline stream with no close-out, the quoted-verdict first line, gh absent, fallback initiatives) and keep every existing test green. VERIFY: the test runner for scripts/desk_live.py passes with real exit codes in the PR body; run the desk once with --once against a fixture directory and paste the needs_you output for the offline case. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Commit, push, update the PR body with a Round 2 section. | feat/floor-v3a
2 | plan-critic | READ-ONLY REVIEW ROUND 2 of PR 77 on branch feat/floor-v3a. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. Re-run the four fixtures from the round 1 comment (offline stream with no close-out, quoted verdict on the first line, gh absent and gh error, fallback initiatives) and confirm each lie is gone; then try to make needs_you lie a fifth way (a seat that closes out after going offline, a first line with two verdict tokens, a variable present but empty). Post ONE comment on PR 77 whose first line reads CRITIC FLOOR V3A ROUND 2 with SAFE-TO-MERGE or BLOCK-FIX and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/floor-v3a
