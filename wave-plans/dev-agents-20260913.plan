# Wave plan for dev-agents — 2026-09-13T14:42:44Z
# Generated by dispatch.sh

1 | web-frontend | On branch feat/floor-v3b (PR 78), fix every BLOCK finding in the comment on PR 78 whose first line reads CRITIC FLOOR V3B BLOCK-FIX; read the whole comment first, rebuild its fixture in tmp and reproduce each finding before touching code. Known from the comment: B1 horizontal scroll at 400 caused by the needs-you note text overflowing, worse when gh is absent (wrap it, never let a note widen the page); B2 question 3 and the one action of NEEDS YOU are not reachable (every row carries one reachable action and the landed and failed lists answer question 3 fully); B3 strip figures scroll their section under the sticky header (offset anchors by the header height at both widths); B6 Nothing needs you must not render when the checks that matter were skipped (say instead that the checks were not run and why, as the data already states, and the strip count must not read as verified); plus any other BLOCK item the comment lists. Add a test per finding using the critic fixture and keep every existing test green. VERIFY: make experience builds clean and the page tests pass, real commands and exit codes in the PR body under a Round 2 heading, plus a fresh screenshot at 400 showing no horizontal scroll. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Commit and push. | feat/floor-v3b
2 | frontend-critic | READ-ONLY REVIEW ROUND 2 of PR 78 on branch feat/floor-v3b. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. Re-run the round 1 fixture and the five questions at 1280 and 400; measure scrollWidth at 400 with gh present and absent; click every strip figure and confirm the section heading is visible below the sticky header; confirm the NEEDS YOU empty state is honest when checks were skipped. Post ONE comment on PR 78 whose first line reads CRITIC FLOOR V3B ROUND 2 with SAFE-TO-MERGE or BLOCK-FIX and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/floor-v3b
