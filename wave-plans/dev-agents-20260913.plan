# Wave plan for dev-agents — 2026-09-13T18:52:56Z
# Generated by dispatch.sh

1 | devops | On branch feat/orchestrator-loop (PR 82), fix all nine findings of the comment on PR 82 whose first line reads CRITIC ORCHESTRATOR LOOP ROUND 2: BLOCK-FIX. Start by running the independent fixture script of the critic named in that comment (a run.sh under tmp, recreate it from the comment if it is gone) and paste its failing rows into the PR body before any change; finish by pasting the same run with zero failing rows. The nine, in short: a landing needs one SAFE record from every assigned critic seat by stem and a later BLOCK from any critic overrides an earlier SAFE; the checks rollup is bound to the head SHA and a run that was cancelled is never green; the land path refuses when the repo has no local checkout and records a stop; a BLOCK-FIX carrying any of the five escalation reason sentences is BLOCK-ESCALATE and fires nothing; a settled plan is spent after one fix round in the runner state regardless of header or suffix; an unreadable memory sensor keeps the previous state and never clears; a garbage sensor value (free share above 100 or below 0) is treated as unreadable; the stops file text goes through task_path, first_sentence and the full scrub with a test feeding a prompt, an absolute path and a secret. Add a test per finding to the producer suite and keep every existing test green. VERIFY: tests/run-queue-loop-tests.sh with real exit codes and the critic fixture run before and after, in the PR body under a Round 3 heading. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Commit and push. | feat/orchestrator-loop
2 | plan-critic | READ-ONLY REVIEW ROUND 3 of PR 82 on branch feat/orchestrator-loop. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. Re-run all nine round 2 fixtures and confirm each is closed, then try two new ways to make the runner act wrongly. Post ONE comment on PR 82 whose first line reads CRITIC ORCHESTRATOR LOOP ROUND 3 with SAFE-TO-MERGE, BLOCK-FIX or BLOCK-ESCALATE and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/orchestrator-loop
