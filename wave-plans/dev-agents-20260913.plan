# Wave plan for dev-agents — 2026-09-13T15:43:53Z
# Generated by dispatch.sh

1 | devops | On branch feat/orchestrator-loop (PR 82), fix all seven findings of the comment on PR 82 whose first line reads CRITIC ORCHESTRATOR LOOP BLOCK-FIX; read it in full and reproduce each with its fixture before touching code. In short: ONE, a landing requires one SAFE record from every assigned critic seat by stem, never a count of comments; TWO, the checks rollup is bound to the head SHA, a rollup on another commit is not green; THREE, the land path refuses to run when the repo has no local checkout, and logs it as a stop; FOUR, a BLOCK-FIX whose body carries any of the five escalation reason sentences from the critic charter is treated as BLOCK-ESCALATE and fires nothing; FIVE, a settled plan is spent after one fix round regardless of header or suffix, tracked in the runner state, so a re-dispatch of the original never fires a second round; SIX, when the memory sensor returns nothing the guard stays in its previous state, never clears, and logs sensor unavailable; SEVEN, the stops file text goes through task_path and first_sentence and the full scrub, so no prompt, absolute path or secret shape can reach it, with a test that feeds all three. Add a test per finding using the critic fixtures and keep every existing test green. VERIFY: the runner test suite with real exit codes in the PR body under a Round 2 heading; run the runner once with --dry-run against the live queue and paste the output. HOUSE STYLE: no long dash anywhere, no AI tool or vendor name, no Co-Authored-By trailer. Commit and push. | feat/orchestrator-loop
2 | plan-critic | READ-ONLY REVIEW ROUND 2 of PR 82 on branch feat/orchestrator-loop. No commits, no push, no review, no labels; tmp writes only. Record head SHA at start and at report. Re-run all seven round 1 fixtures and confirm each is closed; then try three new ways to make the runner act wrongly (a critic that posts SAFE then BLOCK in a later comment, a rollup that is green on the head but from a cancelled run, a memory sensor that returns garbage). Post ONE comment on PR 82 whose first line reads CRITIC ORCHESTRATOR LOOP ROUND 2 with SAFE-TO-MERGE, BLOCK-FIX or BLOCK-ESCALATE and citations. HOUSE STYLE: no long dash, no AI tool or vendor name. | feat/orchestrator-loop
