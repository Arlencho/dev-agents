# Calliope C3 - surface the tool-call stop reason. Issue #2171, epic #2168.
# Runs only after C2 merges: same apps/api/ scope.
# QA test-first: wave 1 must be RED before wave 2 runs.
1 | test-engineer | ADD failing tests for issue #2171 on branch feat/calliope-c3-stop-reason. Assert that a tool-call response truncated at the output token ceiling is distinguishable in the result and in the emitted log line from a response that was complete but failed schema decoding. Drive this with a stub sender returning a truncated payload plus the truncation stop reason, and a second returning a complete payload with a type mismatch. Tests MUST be RED, because the stop reason is currently discarded and read nowhere in the codebase. Write no production code. Read issue #2171 first. | feat/calliope-c3-stop-reason
2 | go-backend | IMPLEMENT stop-reason surfacing for issue #2171 on branch feat/calliope-c3-stop-reason so the wave 1 tests turn GREEN. The result extractor in apps/api/internal/ai/ reads content blocks and discards the response stop reason. Add it to the tool-use result type and include it on the ai_call log line alongside role, model and token counts. Do not change any degradation behaviour: this is observability only. Open a PR and link issue #2171. Read issue #2171 first. | feat/calliope-c3-stop-reason
3 | backend-critic | CRITIC ONLY on the PR for branch feat/calliope-c3-stop-reason issue #2171. Verify the stop reason is genuinely plumbed from the provider response rather than inferred, that it appears on the log line, and that no degradation or retry behaviour changed as a side effect. Executable findings only. Output VERDICT APPROVE or REVISE in handoff. Comment on the PR. Do not re-implement. | feat/calliope-c3-stop-reason
