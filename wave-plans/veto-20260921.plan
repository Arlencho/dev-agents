# Wave plan for veto — 2026-09-21T07:48:29Z
# Generated by dispatch.sh

1 | mobile | This is the final round before merge. Continue on the EXISTING branch feat/app-fleet-console and close the five findings in the review comment on PR 62, changing nothing else. F1: RefusalCard hardcodes the per-payment sentence whenever perTxMax is passed and never consults row.reason, so all ten reason codes render the same false copy. Derive the sentence from the reason the chain actually recorded, and let the override line appear only where an override could genuinely clear it, which is the per-payment case and nothing else. F2: the Decisions tab prints one date above rows spanning several days. Group by day or label each row, but do not put a heading over rows it does not describe. F3: the app says it is still trying after it has stopped trying; say what is true, which is that it gave up and the user can retry. F4: two screens declare a rule or a decision absent before the chain has been read, which is the same defect the previous round fixed elsewhere; apply the same four state treatment, and only read and empty may claim absence. F5: a reader on the phone cannot detect a ruleset stamp whose limits do not match the rule it is attached to, which is the one check that makes stamping honest rather than decorative; surface the mismatch where the stamp is shown, in the same plain voice as everything else. Re-run npx tsc --noEmit, npm test and npx expo config yourself and paste the results into the PR. Forbidden: touching programs/veto/src, restructuring the screens, weakening a test, styling a refusal as an error, claiming a ruleset is on chain. Acceptance: all five closed, tests green, PR 62 updated. No em dash anywhere. | feat/app-fleet-console
2 | frontend-critic | This is the final round before merge. Round 2 review of feat/app-fleet-console. Your round 1 review is a comment on PR 62; the structure, the export round trip and the stamping mechanism were accepted and are not in scope again unless they changed, and say whether they changed. Check that each of your five findings is genuinely closed rather than reworded. In particular, execute the refusal copy for every one of the ten reason codes and confirm each renders a sentence the chain actually supports, and that the override line appears only where an override could clear it. Then confirm no screen claims an absence it has not checked, that no date heading spans rows it does not describe, that the retry copy matches what the app is actually doing, and that a mismatched ruleset stamp is detectable on the phone. Run npx tsc --noEmit, npm test and npx expo config yourself. Your first line must carry the word CRITIC and exactly one verdict word per the fleet rule in CLAUDE.md. No em dash anywhere. | feat/app-fleet-console
