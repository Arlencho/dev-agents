# Wave plan for veto — 2026-09-21T15:09:42Z
# Generated by dispatch.sh

1 | go-backend | Fix issues 66, 80 and 81, branching from current main, and change nothing else. Read all three issues and the header of this plan file first. For 66, hold the property stated there: a rejection from a request whose answer the watcher no longer needs must not end the process, and the process must never exit before recording a decision it has already taken, including under the once command. Choose the shape yourself, but whatever catches a rejection must say loudly what it caught, must not swallow rejections that mean something else is wrong, and must leave the exit code truthful about whether the run did its job. Write a test that reproduces the real sequence: the send succeeds, the websocket confirms, the status poll is then throttled, and prove both that the process survives and that the decision is recorded. For 80, carry the signature of a stale nonce refusal into the record so a confirmed transaction on chain is not missing from the journal, without changing what the row says about the outcome, which the chain decides. For 81, remove the parameter that is never read and the zero passed to satisfy it. Run the watcher, indexer and tools suites and typechecks and paste the real numbers into the PR. Forbidden: touching programs/veto/src or watcher/src/feed.ts, a blanket handler that hides a rejection without reporting it, an exit code that says a run succeeded when it did not, changing how the watcher decides anything. Acceptance: three issues fixed with tests that failed first, PR against main. No em dash anywhere. | fix/watcher-crash-and-nits
2 | backend-critic | Review fix/watcher-crash-and-nits against issues 66, 80 and 81 and the plan header. Check 66 by driving the real sequence rather than a stub: send accepted, websocket confirmation, then a throttled status poll, and confirm the process survives, the decision is recorded, and the exit code tells the truth about what happened. Then check the thing that would be worse than the bug: raise a rejection that has nothing to do with a poll the watcher has finished with, and confirm it is not silently absorbed, that it is reported with enough detail to act on, and that a genuine programming error still surfaces rather than being turned into a log line nobody reads. Check the once command specifically, since exiting before the row is written loses the same history by another route. For 80, confirm the signature of a stale nonce refusal now reaches the record and that nothing about the outcome the row states has changed, since only the chain decides that. For 81, confirm the parameter is gone and no caller still passes a value for it. Confirm every test genuinely fails against the parent commit. Run the suites and typechecks yourself. Commit and push your fixtures before posting the verdict. Your first line must carry the word CRITIC and exactly one verdict word per the fleet rule in CLAUDE.md. No em dash anywhere. | fix/watcher-crash-and-nits
