# Lesson default

Cited by the shared default pack `evidence-first`, so that a *default* pack
genuinely qualifies as proven-loop evidence on the promotion path.

Load-bearing: this is what makes "shared default packs are excluded on purpose"
a testable claim instead of a vacuous one. If nothing cited a default pack, a
regression that counted default packs as P3 evidence would pass every test.
