# OpenAI Provider

Superseded. The Codex CLI is wired as the `codex` provider: launcher, setup and rate-cap notes live in [`providers/codex/`](../codex/README.md). This directory is kept only so old links resolve; nothing reads it.
